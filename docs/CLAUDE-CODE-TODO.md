# CLAUDE-CODE-TODO.md – Frontend-Tasks

> Aktuelle Aufgaben für Claude Code, nach Priorität.
> Erledigte Tasks abhaken, neue unten eintragen. Teil jedes Tasks: diese Datei aktuell halten.

---

## Aktiv

- [ ] Projekte: 2 Showcases (Blumen Lang, Naillery) + interaktive Detail-Ansicht. `SectionHeading` für die Überschrift, `Card`-Muster wo sinnvoll, gleicher Spacing-Rhythmus wie bestehende Sektionen, Dark-only, responsive, Akzent über `--accent`/Gradient-Tokens (Showcase via Gradient, sonst flach). Live-Links als änderbare Felder im Data-Layer (siehe CURRENT-SCHEMA: Blumen Lang / Naillery).

## Backlog (Phase 2 – Reihenfolge)

1. Impressum + Datenschutz: Inhalte einfügen (Platzhalter-Seiten + Footer-Links stehen bereits)

## Follow-ups: Statement-Sektion (Scroll-Reveal)

- [ ] **Statement im Browser VISUELL prüfen** (lokal `npm run preview` + live nach Deploy) – im grünen Build NICHT sichtbar: (1) Reveal beim Reinscrollen (zwei Zeilen nacheinander, blur+fade+y, edel/nicht verspielt); (2) `prefers-reduced-motion` → sofort voll sichtbar, KEIN Blur/Stagger; (3) keine Hydration-Warnung in der Konsole (Reload mit der Sektion im Viewport testen); (4) **Akzentwort „deine"**: während der Blur-Phase und im Endzustand sichtbar/korrekt geclippt (der Eltern-`filter:blur` darf den `background-clip:text` des Kind-Spans nicht killen – der AuroraText-Clip-Bug betraf nur animierte `background-position`, hier statisch, aber im echten Chromium gegenchecken; Fallback `var(--accent)` greift sonst). (Build-grün ≠ Browser-korrekt.)

## Follow-ups: Fließende CTA-Füllung + selektiver Lift

- [ ] **a11y (separat, vom Review aufgedeckt):** Der **mobile** Navbar-CTA (`<a class="… cta-gradient …">` im Mobil-Menü, `src/components/Navbar.tsx`) hat als EINZIGER CTA KEINE `focus-visible:ring-*`-Klassen → kein sichtbarer Tastatur-Fokus. Bewusst NICHT in diesem Task gefixt (Non-Goal: Button-Markup unangetastet); separat die gleichen `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 focus-visible:ring-offset-background`-Klassen wie die anderen 4 CTAs ergänzen.
- [ ] **Im Browser VISUELL prüfen** (lokal `npm run preview` + live) – im grünen Build NICHT sichtbar: (1) **Fluss**: langsam/edel, nicht grell/flackernd; gefüllter Look bleibt; auf allen 5 CTAs. (2) **Lesbarkeit**: weiße Schrift über den GANZEN fließenden Verlauf gut lesbar (≥4.5:1) – kein Moment, in dem die Füllung zu hell wird. (3) **Lift (09.06.2026 spürbarer)**: nur die großen CTAs (Hero, Kontakt-Submit, /möglichkeiten) heben bei Hover deutlich an (~4px) + bekommen einen weichen **violetten** Lift-Schatten (NICHT grau) und „drücken" sich beim Klick zurück (Schatten weg); spürbar, aber edel/nicht zu viel. **Die Navbar-CTAs (Desktop + Mobil) bleiben ruhig** (Sticky-Leiste nicht unruhig). (4) **Touch**: kein Hover-Hängenbleiben; Tap löst sauber aus. (5) **`prefers-reduced-motion`**: Füllung statisch (kein Fluss), kein Lift; Buttons normal sichtbar/klickbar. (6) **Keine Hydration-Warnung**. (Build-grün ≠ Browser-korrekt.)

## Follow-ups: CTA-Glow + Scroll-Fortschrittsbalken

- [ ] **Im Browser VISUELL prüfen** (lokal `npm run preview` + live) – im grünen Build NICHT sichtbar: (1) **CTA-Glow**: dezenter Violett-Glow im Ruhezustand, kräftiger bei Hover, am kräftigsten beim Klick (`:active`); konsistent mit dem `Card`-Glow, nicht zu grell; Intensitäten ggf. in `.cta-gradient` (`src/index.css`, 18/36/52 %) feinjustieren. (2) **Fokusring bleibt sichtbar**: mit Tab auf einen CTA – der `focus-visible`-Ring MUSS trotz Glow sichtbar sein (a11y). (3) **Touch**: kein Hover-Hängenbleiben auf Touch (Hover via `@media hover`). (4) **Scroll-Balken**: läuft links→rechts mit der Scroll-Tiefe, sanftes Nachfedern; oben am Rand, verdeckt die Navbar nicht; `prefers-reduced-motion` → folgt direkt ohne Federung, bleibt sichtbar. (5) **Keine Hydration-Warnung** in der Konsole (Reload an verschiedenen Scroll-Positionen). (Build-grün ≠ Browser-korrekt.)

## Follow-ups: Live-Visual (Dot-Globe + Sparkles) auf /möglichkeiten

- [ ] **Globe + Sparkles im Browser VISUELL prüfen** (lokal `npm run preview` + live nach Deploy) – im grünen Build NICHT sichtbar: (1) **WebGL**: Globus rendert (violette Punkte/Glow aus den Tokens, kein Blau), Auto-Spin sanft/edel, **Drag** rotiert; (2) **Sparkles**: aufsteigende violette Funken, Dichte angenehm niedrig; (3) **Perf**: kein Ruckeln/Hitze, eine rAF-Schleife (Globe) + eine (Sparkles), beim Tab-/Route-Wechsel sauber gestoppt (kein Leak – nach dem Verlassen keine laufenden rAFs/Listener, cobe-`<style>` aus dem `<head>` entfernt); (4) **`prefers-reduced-motion`**: Globus statisch (kein Spin), Sparkles ganz aus; (5) **keine Hydration-Warnung** in der Konsole; (6) Globe-Look ggf. feinjustieren (`mapBrightness`/`diffuse`/`opacity` in `Earth.tsx`). (Build-grün ≠ Browser-korrekt.)
- [ ] `cobe` ist als Dependency in `package.json`/Lockfile committet → auf einer frischen Maschine `npm install` ausführen (zieht cobe automatisch). Vercel installiert es beim Build selbst.

## Follow-ups: /möglichkeiten (stille Showcase-Seite)

- [ ] **`/möglichkeiten` im Browser VISUELL prüfen** (lokal `npm run preview` + live nach Deploy): Tilt-Neigung subtil/edel, CoolMode-Partikel + sauberer Abbau beim Wegnavigieren (kein Leak), Marquee-Lauf + pauseOnHover, `prefers-reduced-motion` (Tilt aus, Partikel aus, Marquee statisch), keine Hydration-Warnungen. (Build-grün ≠ Browser-korrekt.)
- [ ] „Diese Seite selbst"-Bento-Zelle: echte Performance-Zahl(en) messen und den Platzhalter („—" / „Messwerte folgen") ersetzen (z.B. Lighthouse-Score + Ladezeit).
- [ ] Tilt-Karte: den neutralen Platzhalter durch ein echtes Projekt-Bild/-Screenshot ersetzen (sobald ein Projekt zeigbar ist).
- [ ] Optional weitere echte Demos in den Bento aufnehmen, sobald vorhanden (keine erfundenen Daten).
- [ ] Navbar-Link zu `/möglichkeiten` setzen, SOBALD die Seite „fertig"/zeigbar ist (aktuell bewusst still: Route in `App.tsx` + `prerender.mjs`, NICHT in `navItems`).

## Erledigt

- [x] CTA-Hover-Lift spürbarer gemacht (nur `src/index.css`, Vorbild `hover:-translate-y-1` + `hover:shadow-md` – nur Bewegung + Schatten, Farben über Tokens). Hover der großen Content-CTAs (`main .cta-gradient`: Hero, Kontakt-Submit, /möglichkeiten – Navbar weiter ohne Lift) hebt jetzt `translateY(-4px)` (statt 2px) UND bekommt einen zusätzlichen weichen Akzent-Lift-Schatten „oben drauf": additiver box-shadow-Layer `var(--cta-lift-shadow)` (default `0 0 #0000` → unsichtbar; auf großem Hover `0 12px 24px -8px color-mix(in oklab, var(--accent) 30%)` + Lightning-CSS-Token-Fallback). Glow (18/36/52 %), fließende Füllung und weiße Schrift bleiben UNVERÄNDERT (Lift rein additiv). `:active` drückt zurück auf die Grundlinie (`translateY(0)`) + Lift-Schatten weg. box-shadow per State direkt deklariert → animiert verlässlich (kein Snap); Transition bleibt unlayered; Hover touch-sicher (`@media (hover:hover)`), `:active` ungated. reduced-motion: kein Lift (`transform:none` + `--cta-lift-shadow:0 0 #0000`), Füllung statisch. Kein graues/blaues Hex, kein JS/magnetisches Folgen/Tilt, keine Navbar-Bewegung. Adversariell reviewt (7 Findings → 3 bestätigt, alle „verified correct"/keine Änderung nötig: Scope auf 3 große CTAs + Navbar ausgeschlossen, reduced-motion korrekt, Glow/Füllung/Text unverändert). `npm run build` grün; `-4px`/Layer/`main`-Scope/`color-mix(var(--accent))`+Fallback im `dist`-CSS verifiziert. Browser-Check siehe Follow-up.
- [x] Fließende CTA-Füllung + selektiver Lift (nur `src/index.css`, kein Button-Markup). **Fluss:** die 5 Primär-CTAs (`.cta-gradient`) bekommen eine langsam fließende Gradient-Füllung – registrierte `@property --cta-pos` + `background-size:200% 100%` + `background-position:var(--cta-pos) 50%`, animiert via `@keyframes cta-flow` (9s ease-in-out alternate). Bewusst der AuroraText-Weg (NIE `background-position` direkt animieren = Clip-Drop-Bug; hier unkritisch da kein `background-clip:text`, aber aus Konsistenz/sauberer Interpolation gleicher Weg). Füllung NUR `--accent-gradient-strong` (dunkle Stops) → weiße Schrift über den GANZEN Fluss ≥4.5:1 (helle Stops NICHT eingezogen); Glow (Ruhe/Hover/`:active`) unverändert. **Selektiver Lift:** nur die großen Content-CTAs (Hero, Kontakt-Submit, /möglichkeiten) heben bei Hover an (`transform: translateY(-2px)`) und „drücken" sich beim Klick auf die Grundlinie zurück (`:active → translateY(0)`); Scope via Selektor `main .cta-gradient` (große CTAs in `<main>`, Navbar-CTAs in `<header>` → ohne Lift, ruhige Sticky-Leiste). Hover touch-sicher (`@media (hover:hover)`), `:active` ungated; `transform` über die `.cta-gradient`-Transition weich. **reduced-motion:** Füllung statisch (`animation:none` + `background-position:0% 50%`) UND kein Lift (`transform:none`). Rein CSS-`@property`/`@keyframes` → SSR-sicher, Buttons im prerenderten HTML unverändert. Kein Wander-Rahmen (`HoverBorderGradient` bewusst nicht gebaut), kein Puls-Glow. Adversariell reviewt (10 Findings → 4 bestätigt: 2 Kommentar-Härtungen angewandt; vorbestehende mobile-CTA-Fokusring-Lücke als separater a11y-Follow-up notiert [Non-Goal: Button-Markup unangetastet]). `npm run build` grün; `@property`/`@keyframes`/`main`-Scope/`--accent-gradient-strong` im `dist`-CSS verifiziert, kein blaues Hex. Browser-Check siehe Follow-up.
- [x] Primär-CTA-Violett-Glow + Scroll-Fortschrittsbalken. **(1) CTA-Glow** zentral in `.cta-gradient` (`src/index.css`) – dezenter akzent-getönter Glow in DERSELBEN Sprache wie der `Card`-Hover-Glow (`color-mix(in oklab, var(--accent) x%, transparent)`): Ruhe leicht (~18 %), Hover kräftiger (~36 %), `:active` am kräftigsten (~52 %); KEIN Puls/Dauer-Effekt, Hover touch-sicher (`@media (hover:hover)`), `:active` ungated (Tap). Greift automatisch für alle 5 Primär-CTAs (Navbar Desktop+Mobil, Hero, Kontakt-Submit, /möglichkeiten) – KEIN Button-Markup angefasst. `.cta-gradient` ist unlayered → `box-shadow` bindet `var(--tw-ring-offset-shadow)`/`var(--tw-ring-shadow)` mit ein, damit der `focus-visible`-Ring sichtbar bleibt (a11y; Glow + Ring komponieren). Nur Tokens, kein Hex. **(2) `ScrollProgress`** (`src/components/ScrollProgress.tsx`, in `Layout.tsx` ganz oben): schmaler Balken `fixed top-0 inset-x-0 h-[3px] z-[60]` über der Sticky-Navbar (`z-50`, `pointer-events-none`, nur 3px → verdeckt keinen Navbar-Inhalt), Füllung `var(--accent-gradient)`, `scaleX`(origin-left) an framer `useScroll().scrollYProgress`, `useSpring` (mit `skipInitialAnimation`) zum Nachfedern. SSR-sicher (Scroll erst nach Mount; `scaleX` startet 0 → leerer Balken im prerenderten HTML, `transform:scaleX(0)`); reduced-motion via `useReducedMotionSafe` direkt an `scrollYProgress` gebunden (kein Spring, bleibt sichtbar = Info). Adversariell reviewt (11 Findings → 4 bestätigt: `skipInitialAnimation` gefixt; opaker color-mix-Fallback [konsistent mit Card, expliziter Fallback würde Hex hardcodieren] + nun überschriebenes `shadow-sm`/`transition-opacity` bewusst belassen [harmlos, Sektionen unangetastet]). `npm run build` grün; Balken + token-basierter Glow im `dist` verifiziert. Browser-Check siehe Follow-up.
- [x] Live-Visual auf `/möglichkeiten` (eigener Block zwischen Tilt und Marquee, NICHT auf der Startseite): interaktiver Dot-Globe `Earth` (`src/components/ui/Earth.tsx`, WebGL via **`cobe`** v2 – neue Dependency, in `package.json`/Lockfile committet; Drag-to-rotate + sanfter Auto-Spin) + aufsteigende Funken `Sparkles` (`src/components/ui/Sparkles.tsx`, hand-gebautes 2D-Canvas, kein Paket). Reine Können-Demo „ich baue WebGL/Canvas-Visuals". **NUR Violett** aus den Tokens (`--accent`/`--accent-solid` zur Laufzeit gelesen, für cobe per Hex→[r,g,b]; ALLE blauen cobe-Defaults + englische Template-Copy raus). Deutsch/„Du", Label „Live im Browser gerechnet", KEIN „global"-/Reichweiten-Claim, keine erfundenen Zahlen. SSR-/prerender-sicher (stabile Canvas-Hülle im Render, cobe dynamisch importiert/code-split, nie im Prerender); reduced-motion (Globus statisch ohne Spin, Sparkles ganz aus); leak-frei beim Unmount (rAF/Listener/`ResizeObserver`/`IntersectionObserver` abgebaut, `globe.destroy()` + `WEBGL_lose_context`). `cobe` v2 hat eine andere API als die Vorlage (kein `onRender`, `update()`+eigene rAF-Schleife, Canvas-Re-Parenting) → aus der Quelle verifiziert, eigenes Wrapper-`<div>` gegen React-`removeChild`-Fehler. Adversariell reviewt (17 Findings → 6 echte gefixt: leerer Globus unter reduced-motion wg. async Map-Textur, `ResizeObserver` statt window-resize, `WEBGL_lose_context`, off-screen-rAF-Pause). `npm run build` grün; `/möglichkeiten` prerendert mit Label + 2 Canvas-Hüllen verifiziert. Browser-Check siehe Follow-up.
- [x] Statement-Sektion (Scroll-Reveal) direkt vor „Kontakt" (`src/sections/Statement.tsx`, in `Home.tsx` zwischen `Prozess` und `Kontakt` eingehängt): zwei nacheinander einblendende Zeilen „Nicht noch eine Seite von der Stange." / „Sondern deine." – „deine" im Akzent-Gradient über den zentralen Showcase-Helper `.accent-gradient-text` (nichts hardcoden), Rest flach. Reveal-Mechanik in neuer wiederverwendbarer `TextAnimation`-Komponente (`src/components/TextAnimation.tsx`): Vorlagen-API nachgebaut (`text`/`as`/`letterAnime`/`lineAnime`/`direction`/`variants`/`classname`), blur + fade + y, an Repo-Conventions angepasst (kein Next/`'use client'`, `@/`-Alias, **kein** lowercase/capitalize-Transform). SSR-/hydration-sicher: `useReducedMotionSafe` (nicht framers `useReducedMotion`), Orchestrierung `useInView` + gesteuertes `animate` (nicht `whileInView`), Container `initial="hidden"` mit propagierten Kind-Varianten; Text steht als echter Knoten im prerenderten HTML (below-the-fold nur `opacity:0`). reduced-motion → sofort voll sichtbar, kein Blur/Stagger. Bewusst NICHT above-the-fold/Hero; andere Sektionen unangetastet, Dark-only/Responsive. `npm run build` grün; beide Zeilen + `<span class="accent-gradient-text">deine</span>` im prerenderten `dist/index.html` (vor `#kontakt`) verifiziert. Browser-Check siehe Follow-up.
- [x] Echtes Porträt in „Über mich": den 4∶5-Platzhalter („LL" / „Porträt folgt") durch das echte Foto als `object-cover`-`<img>` ersetzt. Quelle `Leon-me.png` (1122×1402, 4∶5) fürs Web optimiert → `public/leon-portrait.webp` (768×960, WebP q80, ~20 KB statt 1,78 MB PNG; `sharp` einmalig per `--no-save`, KEINE neue Dependency). 4∶5-Box behalten (kein Layout-Shift), `loading="lazy"` + `decoding="async"`, `width`/`height`, deutscher Alt-Text „Porträtfoto von Leon Lang". Statisches `<img>` → SSR-/prerender-safe; Dark-only/Layout/Responsive unverändert. KEIN Logo/Favicon (separat). `npm run build` grün; `<img>` im prerenderten `dist/index.html` + WebP in `dist/` verifiziert.
- [x] Stille Showcase-Route `/möglichkeiten` („Was möglich ist") + 4 neue UI-Komponenten in `src/components/ui/`. Seite NICHT in der Navbar verlinkt (Route in `App.tsx` + `ROUTES`/`META` in `scripts/prerender.mjs`, eigener Title „Möglichkeiten – Leon Lang" + Description; react-router v7 dekodiert den Pathname → Umlaut-Route matcht auch percent-encoded). Aufbau: `SectionHeading`-Kopf + Intro → **Bento-Grid** (große Zelle „Diese Seite selbst" = handgebaute STATISCHE Mono-Grafik mit sichtbarem Perf-Platzhalter „—"; drei Zellen via `ServiceDiagram` wiederverwendet: KI-Chat/Redesign/Browser) → „verspielt vs. seriös" (zwei Buttons) → subtile 3D-Tilt-Karte (Platzhalter-Leistung, kein Fake-Foto) → `Marquee` mit echtem Tech-Stack (React/TypeScript/Tailwind/Framer Motion/Vercel). Komponenten: `Marquee` (CSS-Keyframes/Utilities in `index.css` ergänzt), `InteractiveHoverButton` (Tokens gemappt), `CoolMode` (Klick-Partikel; bunte hsl-Zufallsfarben als einzige gekapselte Ausnahme; SSR-safe; reduced-motion=aus; `#_coolMode_effect`+rAF leak-frei abgebaut), `Tilt` (Aceternity-Mechanik on-brand, subtil). Alles SSR-/reduced-motion-/hover-safe; `Card`/`SectionHeading` wiederverwendet; keine Startseiten-Änderung, keine echten Projekte, keine erfundenen Daten. `npm run build` grün; `/möglichkeiten` prerendert mit eigenem `<head>`-Title/Meta + vollem Inhalt verifiziert. Follow-ups siehe oben.
- [x] Per-Route-`<head>` (Title/Description/Basis-OpenGraph): jede prerenderte Route (`/`, `/impressum`, `/datenschutz`) bekommt eigenen `<title>` + `<meta name="description">` + `og:title`/`og:description`/`og:type` im **statischen** `<head>`. **Ansatz:** Route→Meta-Map (`META`) in `scripts/prerender.mjs` (dependency-frei) – NICHT React-19-Component-Metadata, da `renderToString(<App/>)` nur den `#root`-Body liefert → die Tags lägen im Body statt im `<head>`; kein react-helmet, kein Framework-Mode. Geteilter Default-Title/-Description aus dem Template entfernt, pro Route injiziert (jeder Wert genau einmal, `&` escaped). `og:image`/`og:url` bewusst weggelassen (kein Logo/keine finale Domain). Source-`index.html` auf den Home-Default angeglichen (Dev/Fallback). Kein Inhalts-/Layout-Change, SSG/AuroraText unangetastet. `npm run build` grün; Title + Description in allen 3 dist-HTML-`<head>`s unterschiedlich verifiziert.
- [x] AuroraText-Clip-Bugfix: Hero-Akzentwort „spürbar" rendete live transparent/unsichtbar (Violett-Gradient nicht auf die Glyphen geclippt, obwohl `color:transparent` griff). **Root-Cause:** die DIREKT animierte `background-position` auf einem `background-clip:text`-Element lässt Chromium die geclippte Glyphen-Maske fallen. Ausgeschlossen (empirisch): Lightning-CSS-Shorthand-Merge (kein `background`-Shorthand, Clip-Paar bleibt), Prefix-/Reihenfolge (das funktionierende `.accent-gradient-text` nutzt dasselbe Paar), Versions-Differenz (`package-lock.json` committet → live==lokal). Das durchgestrichene `-webkit-background-clip:text` in DevTools ist nur ein harmloses Chromium-Alias-Artefakt. **Fix:** Clip-Technik 1:1 wie `.accent-gradient-text`; Flow über registrierte `@property --aurora-pos` (typisiert `<percentage>`) statt `background-position` direkt animieren + `display:inline-block` (stabile Paint-Box). Nur `--accent-gradient`/`--accent`, reine CSS-@keyframes (SSR-safe), reduced-motion=statisch sichtbar, Fallback nie unsichtbar, graceful degradation. `npm run build` grün; `@property` + Clip-Paar im gebauten CSS verifiziert. (Diagnose via Multi-Agent-Workflow + empirischem Lightning-CSS-Test.)
- [x] SSG/Prerendering (CSR → statisches HTML): alle 3 Routen (`/`, `/impressum`, `/datenschutz`) fallen beim Build als statisches HTML mit echtem Inhalt raus – H1 + Sektions-Text ohne JS im Quelltext (LCP/SEO). **Verfahren:** eigenes `renderToString` + `StaticRouter` (react-router v7 Library-Mode) + `scripts/prerender.mjs`; NICHT `vite-react-ssg` (peert auf react-router-dom ^6), NICHT Framework-Mode (Migration), NICHT Puppeteer (killt Reveals). `build` = `tsc -b && vite build && vite build --ssr src/entry-server.tsx --outDir dist-ssr && node scripts/prerender.mjs`. Neu: `src/entry-server.tsx`, `scripts/prerender.mjs`, `src/hooks/useReducedMotionSafe.ts`; `src/main.tsx` macht `hydrateRoot` (prerendert) bzw. `createRoot` (Dev); `dist-ssr/` gitignored. **Hydration-sicher (Server-Frame = erstes Client-Frame):** `useReducedMotionSafe` (erstes Render `false` wie Server, echter Wert nach Mount) in Hero/Terminal/ServiceDiagram/Prozess; reduced = instant Endzustand (`transition:{duration:0}` bzw. `key`-mit-reduced-Remount). Hero-H1 ohne Entrance-Opacity, generativer BG deterministisch, kein `window`/Zufall im Render. `npm run build` grün; H1 + Sektions-H2 + Impressum/Datenschutz-H1 statisch im `dist`-HTML verifiziert; Reveal/reduced-motion bleiben.
- [x] AuroraText: animiertes „Aurora-Text"-Akzentwort (Magic-UI-Stil) am Hero-Wort „spürbar". Wiederverwendbare Inline-Komponente `src/components/AuroraText.tsx` (API `<AuroraText>Wort</AuroraText>`, Konvention wie `CodeTag`/`SectionHeading`). Effekt = **reine CSS-@keyframes** (`aurora-flow`: `background-position` über einen verbreiterten Gradient `background-size:200%`, sanftes `alternate`/ease-in-out, 8 s – langsam/edel, kein Flackern/kein Seam), **NICHT** Framer Motion → SSR-/hydration-sicher (kein JS-State; das Wort steckt im prerenderten HTML, durchgehend sichtbar = LCP). Ausschließlich Violett: `--accent-gradient` (Source-of-Truth) verbatim, kein Blau/Cyan/Pink, nichts hardcoden. Soliden Fallback (`var(--accent)` via `@supports`) behalten; `prefers-reduced-motion` → der jetzige statische Gradient. Bewusst NUR am Hero-Wort – Eyebrows, Hero-Zähler und andere Gradient-Stellen bleiben statisch via `.accent-gradient-text`. `npm run build` läuft.
- [x] Infrastruktur-Setup (Repo, Vercel, .gitignore, .env.local, CLAUDE.md, docs/)
- [x] Projekt-Grundgerüst: Vite + React + TS + Tailwind (v4) + Framer Motion + react-router-dom, Ordnerstruktur (components/pages/sections/hooks/lib/data/types), Theme-Toggle (Dark default) über zentrale CSS-Variablen inkl. `--accent`-Platzhalter, Navbar (Logo „LL", Anker-Nav, Toggle, CTA „Projekt anfragen") + Footer (Impressum/Datenschutz/Instagram), Routen `/`, `/impressum`, `/datenschutz` (Platzhalter). `npm run build` läuft.
- [x] Hero-Sektion: volle Höhe, Headline („Veränderungen, die spürbar werden.") + Unterzeile, 4 Zähler die beim Sichtbarwerden hochzählen (2 · 3 · 100 % · 1, mit „%"-Suffix; „Person, voller Stack" als Label), CTA „Projekt anfragen" → `#kontakt`. Dark + Light, responsive, Akzent nur über `--accent`, Framer Motion subtil (Stagger-Einblendung, reduced-motion-sicher). `npm run build` läuft.
- [x] Problem-Sektion: Überschrift „Kommt dir das bekannt vor?", 4 Schmerzpunkt-Karten (Titel + ein Satz, je mit dezentem Icon). Dark + Light, responsive (Mobil 1 Spalte, ab `md` 2 Spalten), Akzent nur über `--accent`, Framer Motion subtil (Fade-up + Stagger beim Scrollen). Wiederverwendbare `SectionHeading`-Komponente angelegt. `npm run build` läuft.
- [x] Feinschliff Problem-Karten: wiederverwendbares `Card`-Hover-Muster (`src/components/Card.tsx`) – dezentes Anheben + Akzent-Rand + weicher Glow, smooth (~200 ms), touch-sicher (`@media (hover: hover)`) + `motion-safe`. `npm run build` läuft.
- [x] Leistungen-Sektion: Überschrift „Das baue ich für dich." + Unterzeile, 4 Karten im 2×2-Grid (mobil 1 Spalte), Rhythmus wie Problem-Sektion. `Card` + `SectionHeading` wiederverwendet, je Karte ein dezentes Icon. Karte 4 (KI-Integration) als Highlight via neuer `highlight`-Prop (Akzent dauerhaft aktiv, kein Badge). Dark + Light, responsive, Akzent nur über `--accent`. `npm run build` läuft.
- [x] „Über mich"-Sektion: zweispaltiges Layout (Foto-Platzhalter links / Text rechts, mobil gestapelt mit Foto oben), `id="ueber-mich"`. Eyebrow + Überschrift via `SectionHeading`, 3 Absätze. Porträt-Platzhalter als gerahmte Hülle im Hochformat (4∶5), Initialen „LL" + „Porträt folgt", bereit für echtes Bild in Phase 3. Dark + Light, responsive, Akzent nur über `--accent`, Fade-up beim Scrollen. `npm run build` läuft.
- [x] „Prozess"-Sektion: 4 nummerierte Schritte als sichtbare Abfolge 1→4 (`<ol>`/`<li>`-Stepper, kein loses Card-Grid), `id="prozess"`. Verbindungslinie zwischen den Schritten: Desktop horizontal, Mobil vertikal. Akzent-getönte Nummern, Linie baut sich progressiv auf (Stagger), reduced-motion-sicher. Überschrift „So entsteht dein Projekt." + Unterzeile via `SectionHeading`, Content im Data-Layer (`src/data/process.ts`). Dark + Light, responsive, Akzent nur über `--accent`. `npm run build` läuft.
- [x] „Kontakt"-Sektion: zweispaltig (Formular links / „Lieber direkt?" + 3 Buttons rechts, mobil gestapelt mit Formular oben), `id="kontakt"`. Formspree-Versand (`VITE_FORMSPREE_ENDPOINT`) mit State-Machine idle→sending→success/error, Client-Validierung (Pflichtfelder + E-Mail), Erfolg leert das Formular. Direkte Buttons: Mail/WhatsApp/Instagram (geteilte `BrandIcons`, Footer darauf umgestellt). `--destructive`-Token (beide Modes) für Fehler. a11y-Härtung nach adversariellem Review: Fokus springt zum ersten Fehlerfeld, Fokus ins Erfolgs-Panel, Doppelsende-Guard, Placeholder-Kontrast. Dark + Light, responsive, Akzent nur über `--accent`, Fade-up. `npm run build` läuft.
- [x] Finale Akzentfarbe (Violett): Platzhalter #c8a96a über die zentralen CSS-Variablen ersetzt (nichts hardcoden). Per-Mode-Tokens `--accent` (Dark #a78bfa / Light #6d28d9) + neues `--accent-solid` für gefüllte Buttons (Dark #6d4dff / Light #6d28d9, weiße Schrift); 4 Buttons auf `bg-accent-solid`. Light-Mode-Kontrast-Bug geschlossen (Akzent-als-Text + Fokusring ~7.1:1, ≥ WCAG AA), verifiziert. `npm run build` läuft.
- [x] Hero-Umbau: generativer, animierter Violett-Hintergrund (code-basiert: `color-mix`-Blobs über `--accent`/`--accent-solid` + statisches SVG-`feTurbulence`-Korn, CSS-Keyframe-Drift) + „Terminal" das echten Build-Output tippt (4 Tabs install/build/deploy/whoami, Default build, kein test; Zeilen-Tippen, blinkender Cursor, Pfeiltasten-Tabs, keine Refresh-/Download-Buttons). Terminal in beiden Modes dunkel (`.dark`-Scope). Inhalt erhalten (Claim/Unterzeile/4 Zähler/CTA), 2-spaltig → mobil gestapelt. Engine selbst (Framer Motion + CSS), kein neues Package. reduced-motion = statisches Frame. Adversariell reviewt (5 Fixes: H1-LCP, reduced-motion-Gate, Tabpanel-Fokus, Tab-Flash, will-change). `npm run build` läuft.
- [x] Leistungen-Upgrade: je Karte ein lebendes Schaubild statt nur Icon (Webseiten = Browser-Layout baut auf, Web-Apps = Toggle/Häkchen/Ladebalken, Redesign = Vorher/Nachher-Wisch, KI = Mini-Chat streamt). Alle ehrlich, keine erfundenen Zahlen. Ein gemeinsamer ruhiger Rahmen (`src/components/ServiceDiagram.tsx`), Animation läuft beim Reinscrollen einmal + Hover-Replay, KEINE Dauerloops, reduced-motion = statisches End-Frame. Engine Framer Motion + CSS (kein neues Package), Akzent nur über `--accent`/`--accent-solid`. Card/SectionHeading/2×2 + Titel/Texte unverändert. Adversariell reviewt (0 Findings). `npm run build` läuft.
- [x] Dark-only-Umbau + Violett-Gradient-Akzent (Foundation): Light Mode/Theme-Toggle + dead Light-Code entfernt (ein `:root`-Token-Satz, `color-scheme: dark`, ThemeProvider/useTheme/ThemeToggle/Theme-Typ + Terminal-`.dark`-Scope raus). Zentraler `--accent-gradient` (Showcase-Text-Clip, mit Fallback) + `--accent-gradient-strong` (CTA-Füllung, weiße Schrift ≥4.5:1 = min. 5.05:1 verifiziert); flaches `--accent`/`--accent-solid` + Fokusring bleiben flach. Gradient nur auf Hero-Akzentwort, 4 Hero-Zahlen, primäre CTAs, Eyebrow. Terminal/4 Schaubilder/Hero-BG/Counter/a11y/Card unverändert; reduced-motion bleibt. Adversariell reviewt (0 Findings). `npm run build` läuft. (Code-Tag-Politur folgt separat.)
- [x] Code-Tag-Politur (Stil-Pass): wiederverwendbare `CodeTag`-Inline-Komponente (Mono, flacher Akzent-Hintergrund/-Rahmen `bg-accent/10`+`border-accent/25`, kein Gradient, nowrap, vertikal mittig, rein dekorativ) + `withCodeTags`-Helper (markiert im Render, ändert Daten nicht). Sparsam auf vorhandene Begriffe: Hero `Web-Apps`, Leistungen `Buchungssystem`/`Dashboards`, Prozess `Code`. Keine erfundenen Claims/Zahlen, nichts erzwungen. Headlines/Eyebrows/CTAs/Schaubilder/Terminal/Hero-BG unangetastet, reduced-motion/a11y unverändert. `npm run build` läuft.
