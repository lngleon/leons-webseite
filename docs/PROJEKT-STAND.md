# PROJEKT-STAND.md – Leons Webseite

> Dieses Dokument ist die zentrale Wissensdatei des Projekts.
> 🔒 = Feststehend | 🔄 = Kann sich ändern
> Letzte Aktualisierung: 26.08.2026

---

## 🔒 Branding

Markenname: Leon Lang
Tagline: „Veränderungen, die spürbar werden."
Logo: „LL"-Monogramm – noch zu erstellen (via ChatGPT-Bildgenerator), in heller + dunkler Variante. **Favicon ✅** (27.07.2026): kompletter Satz für dunklen Hintergrund (`favicon.svg` primär + `favicon.ico` Fallback + PNG 16/32 + `apple-touch-icon.png`) in `public/`, im `<head>` aller Routen verlinkt, dazu `theme-color #0a0a0a`.
Design-System: edel-zurückhaltend (Near-Black/Weiß/Grau), **Dark-only** (kein Light/Toggle), Akzent **Violett** – flach via `--accent`/`--accent-solid` + **Violett-Gradient** (`--accent-gradient`/`--accent-gradient-strong`) auf Showcase-Flächen; zentral als CSS-Variablen, siehe Design-Regeln

---

## 🔒 Projektbeschreibung

Premium Personal-Brand-Website für Leon Lang. Sie stellt Leon als Person im Netz dar UND dient als Schaufenster für seine Dienstleistungen. Kein Shop, kein Login, kein Checkout – reines Frontend. Die Seite selbst ist der Beweis des Könnens: Mundpropaganda (Freunde, Familie, kleine Unternehmen) führt Interessenten her, die durch die Qualität der Seite überzeugt werden und Kontakt aufnehmen.

- **Zielgruppe:** Privatpersonen, Selbstständige und kleine Unternehmen (zunächst Freundes-/Familienkreis und deren Empfehlungen). Positionierung nach oben offen (Firmen, Startups später möglich).
- **Markt:** Deutschland, deutschsprachig
- **Währung:** – (keine Preise auf der Seite)
- **Sprachen:** Deutsch, durchgängig „Du"-Ansprache
- **Geschäftsmodell:** Dienstleistungen (Webseiten, Web-Apps/Tools, Redesign, KI-Integration). Schaufenster → Kontaktanfrage → Abschluss persönlich/per individuellem Angebot. KEIN Online-Verkauf, KEINE Bezahlung auf der Seite.
- **Architektur:** Single-Page-Website (reines Frontend) + 2 rechtliche Unterseiten
- **Regulatorik:** Impressum (Pflicht bei geschäftlicher Nutzung) + Datenschutzerklärung (DSGVO, wegen Kontaktformular). Platzhalter-Seiten eingeplant, Inhalte fügt User später aus Generator ein.

---

## 🔒 Benutzerrollen

Reine öffentliche Website – keine Benutzerkonten, keine Logins, keine Rollen, keine geschützten Bereiche.

- **Besucher (öffentlich):** sieht die komplette Single-Page, kann durch Projekte klicken, das Kontaktformular nutzen und über direkte Buttons (E-Mail, WhatsApp, Instagram) Kontakt aufnehmen.

---

## 🔒 Tech Stack

- **Frontend:** **Next.js (App Router)** + React + TypeScript + Tailwind v4 + Framer Motion. **Seit 24.08.2026 – vorher Vite + react-router-dom** (beides entfernt).
- **Rendering:** statisches Rendering von Next selbst – `next build` gibt alle fünf Routen als `○ (Static)` aus (`/`, `/impressum`, `/datenschutz`, `/moeglichkeiten`, `/demo/cafe`, dazu `_not-found`). KEIN SSR zur Laufzeit, kein ISR/Revalidate, keine Route Handlers, keine Server Actions, keine Middleware. Der selbstgebaute Prerender (`renderToString` + `StaticRouter` + `scripts/prerender.mjs`) ist ersatzlos entfallen. Interaktive Teile sind Client-Komponenten (`'use client'`), werden aber trotzdem beim Build zu HTML gerendert und im Browser nur hydriert.
- **Meta/SEO (Head):** über die **Metadata API**. Single Source of Truth ist **`src/data/meta.ts`** (`routeMeta` + Helper `pageMetadata`) – Nachfolgerin der `META`-Map aus `scripts/prerender.mjs`, Werte inhaltlich unverändert. Jede `page.tsx` exportiert `export const metadata = pageMetadata(routeMeta.<route>)`; das Root-Layout (`src/app/layout.tsx`) hält Defaults, den Favicon-Satz (`metadata.icons`) und den `viewport`-Export (`themeColor` + `colorScheme`). Neue Route/Texte ändern → in `src/data/meta.ts`. `og:image`/`og:url` weiterhin nicht gesetzt (kein Logo, keine finale Domain).
- **sitemap.xml + robots.txt (neu, 24.08.2026):** über die Next-Dateikonventionen `src/app/sitemap.ts` / `src/app/robots.ts`, beim Build erzeugt. Routenliste = `routes` in `src/data/site-url.ts`. Die absolute Basis-URL kommt aus `siteUrl` (ebenfalls dort – bewusst getrennt von `site.ts`, weil das von Client-Komponenten importiert wird und Server-Env-Variablen im Browser-Bundle immer `undefined` sind): `NEXT_PUBLIC_SITE_URL` → Vercels `VERCEL_PROJECT_PRODUCTION_URL` → `http://localhost:3000`. Keine Domain hardcodiert.
- **Demo-Seiten (seit 25.08.2026):** stille Schaufenster-Seiten unter `/demo/*` in einer eigenen Route-Gruppe `(demo)` – **ohne** Navbar/Footer/ScrollProgress/Analytics, mit eigenen, auf `.demo-scope` gescopten Design-Tokens (`src/app/(demo)/demo.css`). `noindex`, nicht in der Sitemap, nicht verlinkt. Inhalt je Betrieb in EINER typisierten Datei unter `src/data/demo/`; die Komponenten in `src/components/demo/` sind rein datengetrieben. Erste Seite: `/demo/cafe` („Café Klee", erfunden).
- **Bilder:** `next/image` für die statischen Bilder aus `public/`: Porträt via `fill`, die zwei Projekt-Previews mit intrinsischer `width`/`height` + `sizes`, dazu die vier Demo-Fotos aus `public/demo/` (über `DemoPhoto`, `fill` + `sizes`). Auslieferung über Vercel Image Optimization (`/_next/image`).
- **Backend/DB:** keins – reines Frontend, KEIN Supabase, KEINE Datenbank, KEINE SQL/RLS/Migrations/Edge Functions
- **Auth:** keins
- **Hosting:** Vercel (Auto-Deploy bei Push), Framework-Preset **Next.js**
- **Analytics:** Vercel Web Analytics (`@vercel/analytics` v2, keine Runtime-Deps) – `<Analytics />` aus **`@vercel/analytics/next`** (App-Router-Variante, seit 24.08.2026; vorher `/react`) seit 25.08.2026 in `SiteChrome` (genutzt von `src/app/(site)/layout.tsx` + `not-found.tsx`) statt im Root-Layout gemountet – erfasst alle Routen der `(site)`-Gruppe inkl. Client-Navigation (Auto-Track); die Demo-Seiten unter `/demo/*` werden bewusst NICHT getrackt. **Cookielos** (kein Cookie-Banner). SSG-sicher: kein Markup im statischen HTML, Insights-Script erst im Browser. Muss im Vercel-Dashboard aktiviert sein; Daten erst nach Deploy. Non-Goals: kein Speed Insights, keine Custom Events.
- **AI-Provider:** keiner zur Laufzeit der Seite (KI ist eine angebotene Leistung, kein Feature der Website)
- **Zahlungen:** keine
- **E-Mail/Formular:** Formspree (extern, nimmt Kontaktformular entgegen und leitet als E-Mail weiter). Endpoint aus der Env-Variable **`NEXT_PUBLIC_FORMSPREE_ENDPOINT`** (seit 24.08.2026; vorher `VITE_FORMSPREE_ENDPOINT`) – nie hardcoden.
- **Build/Tooling:** `npm run build` = **`next build`** (Turbopack) – macht Typecheck, Bundling und statisches Rendern in EINEM Schritt (die alte vierstufige Kette `tsc -b && vite build && vite build --ssr … && node scripts/prerender.mjs` ist weg). Tailwind v4 über `@tailwindcss/postcss` (`postcss.config.mjs`) statt `@tailwindcss/vite`. Globales CSS: `src/app/globals.css` (vorher `src/index.css`). Lokal ansehen: `npm run build && npm run start` (vorher `npm run preview`).
- **Weitere:** `cobe` (winzige WebGL-Dot-Globe-Lib, ^2.0.1) – erste Feature-Dependency außerhalb des Kerns, NUR auf `/moeglichkeiten`, dynamisch importiert (eigener Code-Split-Chunk, nicht im Haupt-Bundle, nie beim statischen Rendern ausgeführt). Sonst weiterhin code-basierte Effekte ohne Paket bevorzugen.
- **Repo:** `lngleon/leons-webseite` (GitHub, Branch `main`) – gebunden an SSH-Alias `github-lngleon`
- **Design-System:** Regeln in dieser Datei (Abschnitt Design-Regeln); separate DESIGN-SYSTEM.md vorerst nicht nötig
- **Claude Code Config:** CLAUDE.md im Repo-Root

---

## Phasen-Status

| Phase | Beschreibung | Status |
|-------|-------------|--------|
| 0 | Discovery (Interview) | ✅ Abgeschlossen |
| 1 | Infrastruktur-Setup (GitHub, Vercel, Formspree, lokale Umgebung) | ✅ Abgeschlossen |
| 2 | Aufbau & Sektionen via Claude Code | ✅ Abgeschlossen (alle Sektionen stehen, inkl. Projekte seit 09.06.2026 und Statement) |
| 3 | Content & Feinschliff (Logo, Texte, Recht, Akzentfarbe) | 🔄 Akzentfarbe ✅ (Violett), Favicon ✅; Logo („LL"-Monogramm) + Impressum/Datenschutz-Texte offen |
| 4 | Testing | ⬜ Offen |
| 5 | Launch / Domain | ⬜ Offen |

---

## Infrastruktur-Status

| Komponente | Status | Details |
|------------|--------|---------|
| GitHub | ✅ | Repo `lngleon/leons-webseite`, Branch `main` (SSH-Alias `github-lngleon`) |
| Vercel | ✅ | Mit Repo verbunden, Auto-Deploy bei Push auf `main`. Projekt `leons-webseite` im Vercel-Account „leon's projects" (Dashboard maßgeblich; CLI kann im falschen Account-Kontext stehen). Verifiziert 07.06.2026: Deployment 19c48be („Kontakt") = Ready/Production. |
| Formspree | ✅ | Endpoint **`NEXT_PUBLIC_FORMSPREE_ENDPOINT`** (bis 24.08.2026 `VITE_FORMSPREE_ENDPOINT` – bei Vercel umbenennen!) muss an ZWEI Stellen gesetzt sein: lokal in `.env.local` UND als Vercel-Env-Variable (alle Environments). `.env.local` ist gitignored → gelangt NICHT automatisch zu Vercel; fehlt die Vercel-Variable, funktioniert das Formular live nicht. End-to-end bestätigt (lokal + live, Mail kommt an). |
| Lokale Umgebung | ✅ | VS Code + Claude Code Extension, Repo geklont |
| .env (Formspree-Endpoint) | ✅ | `.env.local` mit **`NEXT_PUBLIC_FORMSPREE_ENDPOINT`** hinterlegt (seit 24.08.2026; vorher `VITE_FORMSPREE_ENDPOINT`) |

---

## Phasen-Details

### Phase 1: Infrastruktur-Setup
- GitHub Private Repo anlegen, Repo-Name eintragen, lokal clonen
- Vercel mit Repo verbinden (Auto-Deploy)
- Formspree-Account anlegen, Formular erstellen, Endpoint kopieren
- Lokale Umgebung: VS Code, Claude Code Extension, ggf. `.env`
- CLAUDE.md + docs/ im Repo anlegen
- Erster Claude-Code-Prompt: Projekt-Grundgerüst (Vite, Tailwind, Framer Motion, Ordnerstruktur, Dark-Basis)
- Ablauf folgt INFRASTRUKTUR-SETUP.md, ein Schritt pro Nachricht

### Phase 2: Aufbau & Sektionen via Claude Code
- Reihenfolge: Grundgerüst (Layout, Navigation, Footer) → Hero → restliche Sektionen
- Sektionen: Hero · Problem · Leistungen · Über mich · Prozess · Projekte · Kontakt
- Dark-only; Responsive bei jeder Sektion prüfen
- Impressum- + Datenschutz-Seiten als Platzhalter anlegen, im Footer verlinken

### Phase 3: Content & Feinschliff
- Favicon-Satz einbinden ✅ (27.07.2026: `favicon.svg` primär + `.ico` Fallback + PNG 16/32 + `apple-touch-icon`, `theme-color #0a0a0a`, im `<head>` aller Routen)
- Logo „LL" einbinden (für dunklen Hintergrund – Dark-only; Navbar zeigt noch den „LL"-Text)
- Akzentfarbe festlegen und zentrale CSS-Variable füllen ✅ (08.06.2026: Violett, per-Mode-Tokens)
- Impressum- + Datenschutz-Texte einfügen (vom User generiert)
- Projekt-Links auf finale Domains umstellen, sobald verfügbar
- Optional: stimmungsvolle Bewegtbild-/Glow-Elemente

### Phase 4: Testing
- TESTCHECKLISTE.md anlegen (erst jetzt)
- Pro Sektion: Desktop + Mobil, Animationen (inkl. reduced-motion), Formular-Versand
- Bug-Tabelle mit Status

### Phase 5: Launch / Domain
- Eigene Domain registrieren/verbinden (Start vorerst auf Vercel-Adresse)
- Finaler Check: Formular kommt an, alle Links korrekt, Recht vollständig

---

## Datenmodell-Übersicht

Kein Datenmodell – reines Frontend ohne Datenbank.

- Inhalte (Leistungen, Projekte, Texte, Links, Kontaktdaten) liegen als statische Daten/Konstanten im Code.
- Kontaktformular-Daten gehen direkt an Formspree und werden NICHT in einer eigenen Datenbank gespeichert.
- Bilder/Videos liegen als statische Assets im Repo (z.B. `public/`).

---

## Workflows

### Besucher-Flow (Scroll durch die Single-Page)
Hero → Problem-Sektion → Leistungen → Über mich → Prozess → Projekte → Statement (Scroll-Reveal) → Kontakt → Footer (Impressum/Datenschutz)

### Kontakt-Flow
- **Variante A (Formular):** Besucher füllt Name, E-Mail, Nachricht aus → Klick „Senden" → Daten gehen still an Formspree → Formspree leitet als E-Mail an Leon (`leonlang95@gmail.com`) → Besucher sieht Erfolgsmeldung („Danke, ich melde mich!").
- **Variante B (direkte Buttons):** Klick auf E-Mail / WhatsApp / Instagram öffnet das jeweilige Programm direkt.

### Projekt-Detail-Flow
Klick auf einen der zwei Showcase-Projekte → interaktive Detail-Ansicht öffnet sich (mehr Infos, was gemacht wurde, ggf. weitere Bilder, „Live ansehen"-Button).

---

## Navigation

Single-Page mit Anker-Navigation + Smooth Scroll.

- **Header/Navbar:** Logo „LL" (links) · Nav-Punkte: Leistungen · Über mich · Prozess · Projekte · Kontakt · CTA-Button „Projekt anfragen" (Gradient-Füllung, scrollt zu Kontakt). Kein Theme-Toggle (Dark-only).
- **Footer:** Name/Kurzinfo · Links zu Impressum + Datenschutz · ggf. Social (Instagram)
- **Unterseiten:** Impressum, Datenschutz (eigene Routen)

---

## Design-Regeln

- **Stil:** futuristisch, clean, edel, premium. Referenzen: adrianziegler.de (Dark) + farisschmidt.de (Light)
- **Dark-only:** kein Light Mode, kein Theme-Toggle (08.06.2026 umgestellt). EIN Token-Satz in `:root` (Near-Black/Weiß/Grau), `color-scheme: dark`, `html` ohne Klasse.
- **Farbschema:** edel-zurückhaltend (Near-Black/Weiß/Grau) + EIN Violett-Akzent, zentral über CSS-Variablen, NIEMALS Farbwerte hardcoden:
  - `--accent` #a78bfa / `--accent-solid` #6d4dff – FLACH, für Ränder, Icons, kleine UI und den **Fokusring** (Ring nie Gradient).
  - `--accent-gradient` `linear-gradient(135deg,#c4b5fd,#a78bfa,#7c5cff)` (Source-of-Truth) – NUR Text-Clip auf Near-Black: Headline-Akzentwörter (nicht ganze Headlines), die 4 Hero-Zahlen, Sektions-Eyebrow. Mittelstop darf leicht ins Pink-Violett; frei innerhalb Violett nachjustierbar.
  - `--accent-gradient-strong` `linear-gradient(135deg,#6d4dff,#6d28d9)` – Füllung **primärer CTAs**; trägt weiße Schrift ≥4.5:1 über den GANZEN Verlauf (verifiziert min. 5.05:1). Die hellen Stops NICHT für CTAs.
  - Text-Gradient via `background-clip:text` MIT solidem Fallback (`var(--accent)`, nie unsichtbar). Gradient nur auf Showcase-Flächen, Body-Text bleibt flach.
- **Code-Tags (Fließtext):** wiederverwendbare Inline-Komponente `CodeTag` (`src/components/CodeTag.tsx`) für echte Tech-/Fach-Begriffe im Fließtext – Mono, dezenter FLACHER Akzent-Hintergrund/-Rahmen (`bg-accent/10` + `border-accent/25`, KEIN Gradient), klein, vertikal mittig, kein Zeilenumbruch. Rein dekorativ (kein Button, keine Semantik/aria). Sparsam (≤1–2 pro Absatz), NUR Begriffe die ohnehin im Text stehen und zu Leon passen – keine erfundenen Claims/Zahlen, kein Marketing; ohne passenden Begriff: nichts erzwingen. Helper `withCodeTags(text, terms)` markiert im Render, ohne die Datenquelle zu ändern. NICHT in Headlines/Eyebrows/CTAs/Schaubildern/Terminal.
- **UI-Sprache:** Deutsch, „Du"-Ansprache
- **Ton:** premium, aber nahbar – nie abgehoben, nie billig
- **Responsive:** Desktop-first, Mobil voll funktionsfähig
- **Animationen (Framer Motion):** Scroll-Effekte, hochzählende Zähler, smoothe Übergänge bei Hover/Klick. Subtil und edel, nie verspielt oder kitschig
- **Optional:** stimmungsvolle Bewegtbild-/Glow-Elemente (kein Muss; code-basierte Effekte bevorzugen vor eingebettetem Video wegen Ladezeit). Keine Screen-Recordings.
- **Keine Preise** auf der Seite (Positionierung: Ziel ist das Gespräch)
- Die Seite selbst ist das beste Portfolio-Stück – Qualität des Handwerks ist sichtbares Verkaufsargument

---

## Prompt-Architektur

Entfällt – keine KI-Features auf der Seite selbst. KI ist eine angebotene Dienstleistung, kein Bestandteil der Website-Funktion.

---

## Export

Entfällt.

---

## Benachrichtigungen

Formspree leitet eingehende Kontaktanfragen per E-Mail an Leon weiter (`leonlang95@gmail.com`, später änderbar). Keine weiteren Benachrichtigungen.

---

## Storage Buckets

Entfällt (kein Backend). Bilder, Logo, Favicon und optionale Videos liegen als statische Assets im Repo (`public/`).

---

## Feature-Roadmap

### MVP-Features (Phase 2)

| Feature | UI | Status |
|---------|----|--------|
| Grundgerüst (Layout, Navigation, Footer) | ✅ | Fertig |
| Hero (Claim + Unterzeile + 4 animierte Zähler) | ✅ | Fertig – 2-spaltig mit animiertem Hintergrund + Terminal (tippt Build-Output) + fließendes Akzentwort „spürbar" (`AuroraText`) |
| Problem-Sektion (4 Schmerzpunkte) | ✅ | Fertig |
| Leistungen (4 Karten) | ✅ | Fertig – je Karte ein lebendes Schaubild (Browser/App/Redesign/KI-Chat), Karte 4 Highlight |
| Über mich | ✅ | Fertig (2-spaltig, echtes Porträt als optimiertes WebP, `object-cover`) |
| Prozess (4 Schritte) | ✅ | Fertig (nummerierte Abfolge mit Verbindungslinie) |
| Projekte (2 Showcases) | ✅ | Fertig – zwei große Karten (Blumen Lang / Naillery) mit Vorschaubild + Typzeile + Link „zur Website" (Live-URLs im Data-Layer). Detail-Ansicht/Overlay bewusst geparkt (keine Tabs) |
| Statement (Scroll-Reveal vor Kontakt) | ✅ | Fertig – zwei nacheinander einblendende Zeilen („Nicht noch eine Seite von der Stange." / „Sondern deine."), „deine" im Akzent-Gradient; via wiederverwendbarer `TextAnimation` (blur+fade+y), SSR-/reduced-motion-sicher |
| Kontakt (Formspree-Formular + direkte Buttons) | ✅ | Fertig (Formular + 3 Direkt-Buttons, a11y-geprüft) |
| Responsive Layout | 🔄 | Grundgerüst responsive; pro Sektion mitprüfen |
| Impressum + Datenschutz (Platzhalter-Seiten) | ✅ | Platzhalter-Seiten + Footer-Links stehen (Inhalt offen) |

### Phase-2-Features (nach Launch)

| Feature | Beschreibung | Priorität |
|---------|-------------|-----------|
| Akzentfarbe setzen | ✅ Violett (08.06.2026): flach `--accent`/`--accent-solid` + `--accent-gradient(-strong)` (Dark-only) | erledigt |
| Logo einbinden | „LL"-Monogramm (für dunklen Hintergrund, Dark-only); **Favicon ✅ 27.07.2026** (Satz im `<head>` verlinkt) | Hoch |
| Eigene Domain | statt Vercel-Adresse | Mittel |
| Professionelle E-Mail | ersetzt Gmail im Formular + Button | Mittel |
| Resend statt Formspree | optionaler Umstieg, sobald eigene Domain steht | Niedrig |
| Stimmungsvolle Bewegtbild-/Glow-Elemente | optionaler visueller Feinschliff | Niedrig |
| Weitere Projekte / Kategorie-Tabs | wenn mehr als 2 Projekte vorhanden | Niedrig |
| Personen-Video / Hero-Hintergrund-Video | optional, Vertrauen/Atmosphäre | Niedrig |

---

## Erkenntnisse & Regeln

- Die Seite selbst ist der wichtigste Verkaufsbeweis – die Qualität des Handwerks IST das Verkaufsargument.
- Positionierung „premium, aber nahbar": Premium-Optik + „Du"-Ansprache + keine Preise. Weder Enterprise-abgehoben noch billig.
- Struktur folgt dem verkaufspsychologischen Aufbau der Vorbilder (Hero → Problem → Lösung/Leistungen → Vertrauen → Prozess → Beweise → CTA).
- Social Proof (viele Kunden/Logos/Testimonials) fehlt noch → bewusst durch persönliche Geschichte + 2 starke Showcases ersetzt. Keine hohlen Versprechen oder erfundenen Zahlen.
- Akzent final: **Violett**, seit 08.06.2026 **Dark-only** (ein Token-Satz, kein Toggle). Flach `--accent` #a78bfa / `--accent-solid` #6d4dff (Ränder, Icons, kleine UI, Fokusring – nie Gradient) + Violett-Gradient `--accent-gradient` (Text-Clip auf Near-Black, mit solidem Fallback) und `--accent-gradient-strong` (CTA-Füllung, weiße Schrift ≥4.5:1 = min. 5.05:1) nur auf Showcase-Flächen. Das frühere Light-Mode-Kontrast-Thema ist damit gegenstandslos.
- Bei nur 2 Projekten keine Kategorie-Tabs (würden leer wirken) → zwei große Showcases mit Detail-Ansicht. Tabs nachrüstbar, wenn mehr Projekte da sind.
- Reines Frontend, kein Backend/DB – Formular läuft über Formspree.
- Karten nutzen das zentrale `Card`-Muster (`src/components/Card.tsx`): einheitlicher, dezenter Hover (Anheben + Akzent-Rand + weicher Glow), touch-sicher und reduced-motion-freundlich. Neue Karten-Sektionen (z.B. Leistungen) dieses Muster wiederverwenden, statt den Hover neu zu bauen. Entrance-Animation gehört auf ein umschließendes motion-Element, nicht in die Card.
- Animationen mit `staggerChildren` über MEHRERE Verschachtelungsebenen (z.B. der Prozess-Stepper: `ol → li → Linien`) NICHT über die `whileInView`-Geste auslösen – sie propagiert den tiefen Variant-Baum unzuverlässig, wenn das Element beim Laden bereits im Viewport liegt (Reload). Stattdessen `useInView` + `animate={inView ? 'show' : 'hidden'}` verwenden; das propagiert die Varianten verlässlich. Einfache, einstufige Sektionen (eine Ebene Kinder) können `whileInView` weiter nutzen.
- **Scroll-Reveal-Text via `TextAnimation` (`src/components/TextAnimation.tsx`, 09.06.2026):** wiederverwendbare Reveal-Komponente (blur + fade + y, zeilen- oder buchstabenweise gestaffelt) – Nachbau einer Vorlagen-API (`text`/`as`/`letterAnime`/`lineAnime`/`direction`/`variants`/`classname`), an dieses Repo angepasst (kein Next/`'use client'`, `@/`-Alias, **kein** lowercase/capitalize-Transform – Text 1:1). SSR-/hydration-sicher nach den bekannten Regeln: `useReducedMotionSafe` (nicht framers `useReducedMotion`), Orchestrierung über `useInView` + gesteuertes `animate` (nicht `whileInView`), Container `initial="hidden"` mit propagierten Kind-Varianten → der Text steht als echter Knoten im prerenderten HTML (below-the-fold nur `opacity:0`, im `dist` verifiziert). reduced-motion → sofort voll sichtbar, kein Blur/Stagger. Für reichen Zeilen-Inhalt (z.B. ein Akzentwort) die Zeilen als echte `<span>`-**Kinder** übergeben (nicht `text`, nicht Fragmente – `React.Children.toArray` flacht Fragmente ab). Erstmals genutzt von der **Statement-Sektion** (`src/sections/Statement.tsx`) direkt vor „Kontakt": zwei Zeilen, „deine" im statischen `.accent-gradient-text` (kein animierter `background-position` → nicht vom AuroraText-Clip-Bug betroffen). Bewusst NICHT above-the-fold/Hero.
- **Client-Env-Variablen aus `.env.local` gelangen NICHT automatisch zu Vercel** (früher `VITE_*`, seit der Next-Migration `NEXT_PUBLIC_*`) (`.env.local` ist gitignored). Bei JEDEM env-abhängigen Feature die Variable ZUSÄTZLICH im Vercel-Dashboard hinterlegen (alle Environments: Production/Preview/Development) und neu deployen. **Symptom, wenn vergessen:** Das Feature schlägt fehl OHNE Network-Request (z.B. `fetch` gegen eine `undefined`-URL → sofortiger Fehler, kein HTTP-Status), nicht als 4xx/5xx. Aufgedeckt beim Live-Schalten des Kontaktformulars (Formspree): lokal lief alles, live nichts.
- **Animierter Gradient-Text: `background-position` NICHT direkt animieren (08.06.2026):** Auf einem `background-clip:text`-Element mit transparentem Text-Fill lässt Chromium bei DIREKT animierter `background-position` die geclippte Glyphen-Maske fallen → das Wort wird unsichtbar (rein statische Clips wie `.accent-gradient-text` sind nicht betroffen, nur der animierte Fall). Den „Fließ"-Effekt deshalb über eine **registrierte `@property`** (z.B. `--aurora-pos`, `syntax:"<percentage>"`, interpolierbar) animieren, die `background-position` speist (erzwingt sauberen Repaint pro Frame), zusätzlich `display:inline-block` für eine stabile Paint-Box. Das durchgestrichene `-webkit-background-clip:text` in DevTools ist ein harmloses Chromium-Alias-Artefakt (erscheint auch im funktionierenden Fall) und KEIN Hinweis auf die Ursache. Diagnostik-Lehre: erst Ground-Truth (gebautes CSS gegen `origin/main`, Lockfile, Lightning-CSS-Output empirisch) prüfen, bevor man eine Vermutung umsetzt – hier waren die naheliegenden Verdächtigen (Minifier/Prefix/Deploy) allesamt falsch.
- **SSG/Prerendering statt reinem CSR (08.06.2026).** ⚠️ **Der Werkzeug-Teil dieses Punktes ist seit 24.08.2026 überholt** (Next macht das statische Rendern jetzt selbst) – **die Hydration-Regel darunter gilt unverändert weiter und ist Projektgesetz.** Historischer Kontext: die Seite wurde beim Build prerendert statt rein client-seitig gerendert; Werkzeugwahl damals bewusst eigenes `renderToString` + `StaticRouter` + `scripts/prerender.mjs` – NICHT `vite-react-ssg` (peerte auf react-router-dom ^6, wir waren auf v7), NICHT Framework-Mode (wäre eine Migration gewesen – genau die kam dann 24.08.2026) und NICHT Puppeteer (ein Snapshot würde die Reveal-Animationen einfrieren/killen). **Goldene Hydration-Regel (weiterhin gültig): Server-Frame muss = erstes Client-Frame sein.** Werkzeug dafür ist der Hook `useReducedMotionSafe` – er liefert im ERSTEN Render immer `false` (= wie der Server, der kein `matchMedia` hat) und übernimmt den echten Wert erst nach dem Mount. `useReducedMotion` aus framer-motion deshalb NIE direkt im Render benutzen (liefert client-seitig sofort den echten Wert → Mismatch bei reduce-Usern). Reduced-User erreichen den Endzustand instant nach dem Mount (per State-Set bzw. reduced-Varianten mit `transition:{duration:0}`, ServiceDiagram via `key`-mit-reduced-Remount). **Weitere Regeln:** nichts Zufälliges/Generatives im Render (`window`/`document`/`Math.random`/`Date` verboten – Hero-BG ist deterministisch: CSS-Blobs + statisches SVG-`feTurbulence` seed 0, fixe `noiseId`); das LCP-Element (Hero-H1) darf KEINE Entrance-Opacity haben (steht als sichtbares `<h1>` im HTML); below-the-fold-Reveals dürfen `opacity:0` starten – der Text steht trotzdem im Quelltext (SEO ok). Build-Pipeline damals: `tsc -b && vite build && vite build --ssr src/entry-server.tsx --outDir dist-ssr && node scripts/prerender.mjs` (`dist-ssr/` gitignored) – **heute: `next build`.**
- **Stille (unverlinkte) Routen (08.06.2026, auf Next aktualisiert 24.08.2026):** Eine Seite kann existieren, statisch gerendert und per direkter URL erreichbar sein, OHNE in der Navigation aufzutauchen – es genügt ein Ordner unter `src/app/` mit `page.tsx` + Eintrag in `routeMeta` (`src/data/meta.ts`) und `routes` (`src/data/site-url.ts`, für die Sitemap), aber NICHT in `navItems`/Footer. Beispiel: `/moeglichkeiten`. **ACHTUNG, unter Next gilt das Gegenteil der alten react-router-Regel:** siehe den eigenen Punkt „Umlaut-Routen unter Next" weiter unten – ein Ordnername mit Sonderzeichen müsste percent-encodet werden – genau deshalb heißt die Route seit 25.08.2026 ASCII `/moeglichkeiten`; neue stille Routen bitte gleich ASCII benennen (Hintergrund im Punkt weiter unten).
- **Marquee/Animations-Utilities (Tailwind v4) + CoolMode-Cleanup (08.06.2026):** Die Magic-UI-Marquee braucht die Keyframes `marquee`/`marquee-vertical` + die Utilities `animate-marquee(-vertical)`; beides ist in der Komponenten-Vorlage NICHT enthalten und muss in `src/app/globals.css` über den `--animate-*`-Theme-Namespace ergänzt werden, das reduced-motion-Gate (`animation:none`) als unlayered Regel (sticht die layered Utility). Bei **CoolMode** (Klick-Partikel, bunte hsl-Zufallsfarben als einzige gekapselte Ausnahme vom Violett-System) MUSS der Cleanup den globalen `#_coolMode_effect`-Layer + die rAF-Schleife restlos abbauen: im Cleanup zuerst `autoAddParticle=false` (sonst laufen bei „Unmount-während-Klick" neue Partikel nach → Leak), dann auslaufen lassen und bei leerem Feld rAF canceln + Layer entfernen (Instanz-Zähler → 0).
- **WebGL/Canvas-Visuals SSR-sicher (Dot-Globe + Sparkles, 09.06.2026):** Auf `/moeglichkeiten` als reine Können-Demo: `Earth` (`src/components/ui/Earth.tsx`, Dot-Globe via **`cobe`**, WebGL) + `Sparkles` (`src/components/ui/Sparkles.tsx`, hand-gebaute 2D-Canvas-Funken, KEIN Paket). Beide NUR Violett: Farben werden zur Laufzeit aus `--accent`/`--accent-solid` gelesen (für cobe per Hex→[r,g,b]-Parser) – kein hardcodiertes/blaues Hex, keine cobe-Default-Blautöne. **SSR/Prerender:** im Render nur eine stabile `<canvas>`-Hülle; alle window-/WebGL-/Canvas-Zugriffe erst im `useEffect` nach dem Mount; **cobe wird dynamisch importiert** (`import('cobe')` im Effect → eigener Chunk, nie im Prerender ausgeführt; der `Globe`-Typ via `import type` ist erased). reduced-motion (im Effect via `matchMedia`, wie CoolMode): Globe **kein Auto-Spin** (ein statischer Frame, KEINE laufende rAF-Schleife – Drag bleibt nutzer-initiiert möglich), Sparkles **ganz aus** (Schleife startet nicht). Leak-frei wie CoolMode: rAF cancel, `globe.destroy()` (entfernt auch cobe's in den `<head>` injiziertes `<style>`), alle Listener entfernt, `ResizeObserver.disconnect()`. **cobe-v2-Fallstricke (verifiziert aus der Quelle):** v2 hat KEIN `onRender`/keine interne Schleife mehr → man treibt selbst eine rAF-Schleife mit `update({phi})`; `update({width,height})` realloziert den GL-Buffer (`canvas.width=…`) → NUR bei Resize aufrufen, nie pro Frame; **`createGlobe` hängt die `<canvas>` in einen selbst eingefügten Wrapper-`<div>` um** → die Komponente rendert ein EIGENES Wrapper-`<div>`, damit React beim Unmount nur dieses entfernt und nicht über die umgehängte Canvas stolpert (`removeChild`-Fehler). Perf: Sparkles-`density` bewusst niedrig (Default 64, flächenskaliert gedeckelt – NICHT 800), `devicePixelRatio` auf 2 gedeckelt; beide Schleifen via **`IntersectionObserver`** pausiert, solange der Block NICHT im Viewport ist (kein rAF off-screen). Namens-Kollision beachtet: `Sparkles` (lucide-Icon) vs. Komponente → als `SparklesCanvas` importiert. **Adversariell reviewt (17 Findings → 8 bestätigt, davon 2 reine „verified safe"-Notizen; alle 6 echten gefixt):** (1) reduced-motion zeigte einen LEEREN Globus, weil cobe die Map-Textur async lädt (`Image.onload`) und ohne Schleife nie nachzeichnet → unter reduced ein paar terminierte Redraws nachgeschoben (danach statisch); (2/3/4) Earth nutzt jetzt `ResizeObserver` (statt nur `window`-resize) → fängt reine Container-Resizes + 0-Breite beim Start ab; (5) WebGL-Context beim Unmount aktiv via `WEBGL_lose_context` freigegeben (cobe.destroy() tut das nicht → sonst Context-Limit bei vielen Routenwechseln); (6) rAF off-screen pausiert (s.o.).
- **CTA-Glow + Scroll-Fortschrittsbalken (09.06.2026):** (1) **Primär-CTA-Glow** zentral in `.cta-gradient` (`src/app/globals.css`) – ein weicher, akzent-getönter Schatten in DERSELBEN Sprache wie der `Card`-Hover-Glow (`color-mix(in oklab, var(--accent) x%, transparent)`, wie Tailwinds `shadow-accent/x`), Ruhezustand leicht (~18 %), Hover kräftiger (~36 %), `:active` am kräftigsten (~52 %); KEIN Puls/Dauer-Effekt, nur State-Transition. Hover via `@media (hover: hover)` touch-sicher, `:active` bewusst ungated (Tap). Greift automatisch für ALLE 5 Primär-CTAs (Navbar ×2, Hero, Kontakt-Submit, /moeglichkeiten) – KEIN Button-Markup angefasst. **Wichtig (Cascade):** `.cta-gradient` ist *unlayered* und gewinnt damit gegen Tailwinds *layered* Utilities – die `box-shadow` bindet deshalb `var(--tw-ring-offset-shadow)` + `var(--tw-ring-shadow)` mit ein, sonst würde der `focus-visible:ring-*` (Fokusring, a11y!) überschrieben. So komponieren Glow + Fokusring. Lightning-CSS erzeugt automatisch einen soliden `var(--accent)`-Fallback + die `color-mix`-Variante im `@supports`. (2) **`ScrollProgress`** (`src/components/ScrollProgress.tsx`, seit 25.08.2026 ganz oben in `SiteChrome` gemountet – also nur in der `(site)`-Gruppe, nicht auf den Demo-Seiten): schmaler Balken `fixed top-0 inset-x-0 h-[3px] z-[60]` (über der Sticky-Navbar `z-50`, `pointer-events-none`, nur 3px → verdeckt keinen Navbar-Inhalt), Füllung `var(--accent-gradient)`, `scaleX` (origin-left) an framer `useScroll().scrollYProgress`. **SSR-sicher:** `useScroll` liest erst nach Mount, `scaleX` startet 0 → Balken im prerenderten HTML leer (`transform:scaleX(0)`, dekorativ, nicht LCP), Server-Frame = erstes Client-Frame. **reduced-motion** (`useReducedMotionSafe`): ohne Spring direkt an `scrollYProgress` gebunden (bleibt sichtbar, ist Info), sonst `useSpring` zum sanften Nachfedern.
- **Fließende CTA-Füllung + selektiver Lift (09.06.2026):** Die 5 Primär-CTAs (`.cta-gradient`) haben eine langsam FLIESSENDE Gradient-Füllung (gefüllter Look bleibt, nur der Verlauf wandert). **Technik = der bewährte AuroraText-Weg:** registrierte `@property --cta-pos` (`<percentage>`) + `background-size:200% 100%` + `background-position:var(--cta-pos) 50%`, animiert via `@keyframes cta-flow` (9s ease-in-out alternate) – es wird NUR die `@property` animiert, NIE `background-position` direkt (genau der Chromium-Clip-Drop-Bug; hier unkritischer, da der Button KEIN `background-clip:text` hat, aber derselbe sichere Weg). Füllung bewusst NUR `--accent-gradient-strong` (dunkle Stops) → weiße Schrift bleibt über den GANZEN Fluss ≥4.5:1; die hellen Stops NICHT einziehen. Glow (Ruhe/Hover/`:active`) unverändert. **Selektiver Lift (09.06.2026 spürbarer gemacht, Vorbild `hover:-translate-y-1` + `hover:shadow-md`):** nur die großen Content-CTAs (Hero, Kontakt-Submit, /moeglichkeiten) heben bei Hover spürbar an (`transform: translateY(-4px)`) UND bekommen einen zusätzlichen weichen Akzent-Lift-Schatten „oben drauf" – realisiert als additiver box-shadow-Layer `var(--cta-lift-shadow)` (default `0 0 #0000`, nur auf großem Hover gesetzt: `0 12px 24px -8px color-mix(--accent 30%)` + Lightning-CSS-Token-Fallback); NUR Bewegung + Schatten aus der Vorlage, Schatten über Akzent-Tokens (kein graues Hex). Beim Klick „drückt" es sich zurück auf die Grundlinie und der Lift-Schatten verschwindet (`:active → translateY(0)` + `--cta-lift-shadow:0 0 #0000`); die Navbar-CTAs bleiben ruhig. Scope ohne Button-Markup-Änderung über den Selektor **`main .cta-gradient`** (die großen CTAs liegen in `<main>`, die Navbar-CTAs in `<header>`; der Layer existiert bei allen, bleibt für Navbar aber unsichtbar). Hover touch-sicher (`@media (hover:hover)`), `:active` ungated (Tap); `transform` + `box-shadow` laufen über die `.cta-gradient`-Transition weich (die direkt pro State deklarierte box-shadow animiert verlässlich). **reduced-motion:** Füllung statisch (`animation:none`, `background-position:0% 50%`) UND kein Lift (`transform:none`, `--cta-lift-shadow:0 0 #0000`). Rein CSS-`@keyframes`/`@property` (kein JS) → SSR-sicher, Button-Markup im prerenderten HTML unverändert. KEIN Wander-Rahmen (`HoverBorderGradient` bewusst nicht gebaut – wir bleiben gefüllt), kein Puls-Glow. Nur in `src/app/globals.css`.
- **NIEMALS zwei Claude-Code-Sessions parallel auf dasselbe Repo (08.06.2026):** Zwei gleichzeitig laufende Sessions am selben Repo sind sich in die Quere gekommen – die `AuroraText`-Arbeit einer Parallel-Session lag teils nur im Arbeitsbaum (uncommittet) und verursachte einen Push-/Merge-Konflikt sowie das „Wort live unsichtbar"-Symptom (Teile fehlten auf `origin/main`). Regel: pro Repo immer nur EINE aktive Session; zu Beginn `git fetch`/`git status` prüfen und Arbeit zügig committen + pushen, statt parallel zu arbeiten.
- **Build-grün ≠ im-Browser-korrekt (08.06.2026, verschärft 24.08.2026):** Ein grüner `npm run build` garantiert NICHT, dass die Seite im Browser stimmt. Zwei belegte Fälle: (a) der AuroraText-Clip-Bug war build-grün UND live unsichtbar; (b) bei der Next-Migration war `/möglichkeiten` build-grün, wurde als `○ Static` gelistet, die HTML-Datei lag im Build – und die Route lieferte trotzdem **404** (Ordnername-Encoding, s.u.). **Konsequenz: „im Build-Output vorhanden" ist NICHT dasselbe wie „per URL erreichbar".** Zur Abnahme gehört daher beides: Augenschein im Browser für visuelle/interaktive Features (Gradient-Clip, Tilt, Partikel, Marquee, reduced-motion, Hydration-Warnungen) UND ein HTTP-Statuscheck jeder Route gegen den echten Server (`npm run build && npm run start`, dann `curl` auf jede Route) – nicht nur ein Blick in `.next/server/app/`.
- **Keine echten Infra-Identifier im öffentlichen Hero (18.06.2026):** Das Hero-„Terminal" ist Schaufenster, kein echtes Log – im öffentlich sichtbaren Output gehören KEINE realen Konto-/Projekt-/Deploy-Identifier (GitHub-Handle `lngleon`, Repo/Projekt `leons-webseite`, Deploy-URL `…vercel.app`, E-Mail). Stattdessen sprechende Dummies (`dein-projekt`, `https://deine-seite.de`), die den Look/Lerneffekt erhalten, aber nichts Privates/Angreifbares preisgeben (kein Account-Enumeration-/Doxxing-Vektor). Marke/Vorname „Leon Lang" + Rolle dürfen bleiben (das ist gewollte Personal-Brand-Copy, kein Infra-Identifier). Terminal-Inhalt liegt zentral in `src/data/hero.ts` – bei künftigen „echten Output"-Tabs denselben Anonymisierungs-Check fahren.
- **Template-Head-Kommentare tag-literal-frei halten (27.07.2026):** Der finale `<head>` entsteht aus dem Vite-gebauten Template + Regex-Injektion in `scripts/prerender.mjs`; der Prerender strippt u.a. `<title>…</title>` und das `description`-Meta per Regex. Ein HTML-Kommentar, der den Literal-Text `<title>` (oder `<meta name="description">`) als Fließtext enthält, wird mitgefressen: im Favicon-Task nahm `/<title>[\s\S]*?<\/title>/` vom `<title>` IM Kommentar bis zum echten `</title>` alles dazwischen mit – und löschte die Favicon-`<link>`s aus dem Build (build-grün, aber Icons weg). Regel: in `index.html`-Head-Kommentaren keine echten Tag-Literale schreiben (Klartext „Title"/„Description" statt `<title>`).
- **Umlaut-/Sonderzeichen-Routen unter Next – am Ende bewusst VERMIEDEN (24.–25.08.2026):** ⚠️ **Nachtrag 25.08.2026: die Route heißt jetzt `/moeglichkeiten` (ASCII), damit ist der ganze Absatz unten nur noch Hintergrundwissen für den Fall, dass je wieder eine Route mit Sonderzeichen aufkommt.** Die Lehre in einem Satz: **unter Next gehören Sonderzeichen nicht in ein Routen-Segment** – ein ASCII-Segment hat das Problem gar nicht erst, kostet nichts und spart den ganzen Encoding-Zirkus. Was passiert war: Next übernimmt statische Ordnernamen **1:1** in die Route-Regex des Build-Manifests. Der Ordner `src/app/möglichkeiten/` erzeugt also `^/möglichkeiten$` – Browser senden im Request-Pathname aber IMMER die encodete Form `/m%C3%B6glichkeiten`. Die beiden matchen nicht → **404**, obwohl der Build grün ist, die Route als `○ Static` gelistet wird und `.next/server/app/möglichkeiten.html` existiert (reproduziert mit `next start` UND `next dev`). **Fix:** den Ordner selbst encodet benennen – `src/app/m%C3%B6glichkeiten/`; die Regex lautet dann `^/m%C3%B6glichkeiten$` und trifft den echten Request (lokal verifiziert: 200 + korrekter Inhalt). Für den Nutzer ändert sich nichts (die Adresszeile zeigt weiter „möglichkeiten", der Browser encodet nur beim Senden). Als Zwischenlösung lag zusätzlich ein zweiter Ordner `src/app/möglichkeiten/` daneben (1-Zeilen-Re-Export), weil nicht verifizierbar war, ob Vercels Routing-Layer den Pfad encodet oder dekodiert an den Next-Router weitergibt. Ein `rewrite` in `next.config.ts` wurde geprüft und verworfen (dessen Regex trägt den Umlaut roh und trifft denselben Fall nicht). **Das ist die exakte Umkehrung der alten react-router-Regel** (v7 dekodierte den Pathname, dort war der Raw-Ordner richtig) – beim Migrieren also nicht ungeprüft übernehmen. **Aufgelöst am 25.08.2026:** beide Ordner durch EINEN ASCII-Ordner `src/app/moeglichkeiten/` ersetzt – kein Encoding, keine Doppelung, keine offene Frage Richtung Vercel. Kein Redirect von der alten URL, weil die Route nie verlinkt war. Titel/Description behalten den Umlaut; der gehört in den sichtbaren Text, nicht in die URL.
- **Vite → Next: was die Migration wirklich kostet (24.08.2026):** Der Umbau war weitgehend mechanisch, aber an diesen Stellen inhaltlich: **(1) Client/Server-Grenze** – alles mit Hooks, Framer Motion, Browser-APIs oder Event-Handlern braucht `'use client'`; das ist KEIN Verlust an statischem HTML (Client-Komponenten werden beim Build trotzdem zu HTML gerendert und im Browser nur hydriert). Server-Komponenten bleiben nur Layout, `page.tsx` und rein präsentative Bausteine (`Card`, `SectionHeading`, `CodeTag`, `BrandIcons`, `Footer`, `AuroraText`, `Marquee`, `Statement`). **(2) Metadata API statt Regex-Injektion** – die `META`-Map wanderte 1:1 nach `src/data/meta.ts`; damit ist die ganze alte Klasse von Head-Bugs weg (Regex frisst Favicon-Links, doppelte Titles), inklusive der Regel „Template-Head-Kommentare tag-literal-frei halten", die damit gegenstandslos ist. **(3) Env-Präfix** – `VITE_` → `NEXT_PUBLIC_`, muss auch im Vercel-Dashboard umbenannt werden, sonst schlägt das Formular live fehl (bekanntes Symptom: Fehler ohne Network-Request). **(4) Echter 404** – unbekannte Pfade liefern jetzt `src/app/not-found.tsx` mit HTTP-404 statt Vercels Default-Seite. **(5) Scroll-nach-oben** – der `useEffect` aus dem alten `Layout` entfiel ersatzlos, der App Router scrollt selbst. Nicht angefasst (Non-Goals eingehalten): Formspree, Design/Tokens, Sektionen, `og:image`.
- **String.replace + `$`-Sequenzen (24.08.2026, zum zweiten Mal):** Beim Doku-Update hat ein Node-Einzeiler die CURRENT-SCHEMA verdreifacht (201 → 590 Zeilen). Ursache: der Ersetzungstext enthielt ein Dollarzeichen direkt vor einem Backtick (aus dem in Backticks gesetzten Regex-Literal ^/möglichkeiten$) – diese Sequenz ist in `String.prototype.replace` ein Sonderzeichen und fügt **den gesamten Text VOR dem Treffer** ein. Genau davor warnte schon der Kommentar im alten `scripts/prerender.mjs` (dort war es der Terminal-Prompt `$`). **Regel: bei `String.replace` IMMER einen Funktions-Replacer verwenden** (`s.replace(a, () => b)`), sobald der Ersetzungstext aus Inhalt stammt und nicht handverlesen ist. Erkannt, weil nach dem Schreiben die Zeilenzahl geprüft wurde – solche Schreibskripte gehören danach verifiziert (`wc -l`, `grep -c` auf eine Überschrift).
- **Mount-Animationen lassen sich NICHT per Hook auf reduced-motion gaten – das muss CSS machen (25.08.2026):** `useReducedMotionSafe` liefert im ERSTEN Render bewusst `false` (Hydration-Regel „Server-Frame = erstes Client-Frame"). Framer Motion dispatcht eine Mount-Animation (`initial` → `animate`) aber genau in diesem ersten Render. Kommt der echte reduced-Wert einen Tick später an, läuft die Animation längst – ein geänderter `transition`-Wert bricht sie nicht mehr ab. **Folge:** Navbar und Hero fuhren auch bei `prefers-reduced-motion: reduce` ein, obwohl der Code „gegated" aussah. Das ist kein Flüchtigkeitsfehler, sondern ein echter Zielkonflikt zwischen Hydration-Regel und Motion-Gating. **Lösung (ohne die Hydration-Regel anzufassen):** ein CSS-Gate. Framer schreibt seine Werte als INLINE-Style (`style="opacity:0;transform:translateY(-24px)"` – steht so auch im statisch gerenderten HTML); eine Author-Regel mit `!important` sticht einen Inline-Style ohne `!important`, und zwar **ab dem ersten Paint** → kein Flash, kein Sprung. Umgesetzt als `.entrance-anim` in `globals.css`, komplett innerhalb `@media (prefers-reduced-motion: reduce)` → für alle anderen Nutzer ändert sich nachweislich nichts (die Klasse kommt im gebauten CSS **nur** in dieser Query vor, geprüft). **Faustregeln:** (1) Die Klasse gehört an das Element, das Opacity/Transform TRÄGT – nicht an einen Stagger-Container mit leerer `hidden`-Variante (der bekommt gar keinen Inline-Style). (2) Bewusst KEIN Nachfahren-Selektor (`.entrance-anim *`) – der träfe auch legitime Transforms (Terminal-Cursor, CTA-Lift). (3) **Nicht überall anwenden:** wo der Endzustand NICHT „opacity 1 / kein Transform" ist, wäre das Gate falsch – `ServiceDiagram` z.B. hat `show`-Zustände wie `translateX(18px)`, `width: 82%` und `clipPath`; dort löst der bestehende `key`-Remount bei reduced-Flip das Problem korrekt und das CSS-Gate hätte den Toggle-Knopf verschoben. (4) Scroll-Reveals (`whileInView`/`useInView`) haben die Race nicht – der IntersectionObserver-Callback läuft asynchron NACH dem Mount-Effect, `reduce` ist dann schon korrekt. Sie brauchen trotzdem ein Gate, wenn ihre Varianten gar nicht auf `reduce` schauen (war bei Problem/Leistungen/Über-mich/Kontakt und dem Reveal auf /moeglichkeiten der Fall).
- **Ein Eltern-Layout lässt sich nicht abwählen – Hülle gehört nach unten (25.08.2026):** In Next umschließt `app/layout.tsx` ausnahmslos JEDE Route. Wer eine einzelne Seite ohne Navbar/Footer bauen will, kann das von unten nicht „abbestellen": Route-Gruppen (`(site)`, `(demo)`) ändern nur die Ordnerstruktur, nicht die URL – das Root-Layout bleibt trotzdem darüber. **Konsequenz:** ins Root-Layout gehört nur, was WIRKLICH für alles gilt (`<html>`, `<body>`, Head). Alles Sichtbare wandert in die Gruppe, die es braucht – hier `SiteChrome` in `(site)/layout.tsx`. `not-found.tsx` muss an der App-Wurzel liegen (Next verlangt das für den globalen 404) und rendert die Hülle deshalb selbst. **Preis der Umstellung:** eine zusätzliche Layout-Ebene vergrössert die RSC-Payload spürbar im Rohtext (~6,5 KB/Seite), gzip-komprimiert aber nur um **~160–180 Byte** – vor dem Erschrecken also immer die komprimierte Grösse ansehen. Die Hauptseite selbst blieb markup-identisch (nur ein interner `useId`-Präfix verschob sich, weil er von der Tiefe im Komponentenbaum abhängt – hydration-sicher, weil Server und Client denselben Baum rendern).
- **Fremde Tokens sauber scopen statt `:root` anfassen (25.08.2026):** Eine Seite mit komplett eigenem Look (helle Demo statt Dark/Violett) braucht KEINE zweite Utility-Ebene. Es genügt, DIESELBEN Variablennamen (`--background`, `--foreground`, `--accent` …) in einem eigenen Scope neu zu belegen – die bestehenden Tailwind-Utilities (`bg-background` …) lösen dort dann automatisch auf die neuen Werte auf, weil `@theme inline` in `globals.css` sie auf genau diese Variablen abbildet. Zwei Details: (1) Der Block steht auf `html:has(.demo-scope), .demo-scope` – das `html:has()` sorgt dafür, dass auch `<body>` und die Scrollbar mitziehen (Overscroll!), der zweite Selektor ist der Fallback ohne `:has()`. (2) Die Datei wird NUR vom Demo-Layout importiert und landet dadurch in einem eigenen CSS-Chunk – die Hauptseite lädt sie nicht (verifiziert).
- **Grosse Versalien + deutsche Komposita = garantierter Überlauf (25.08.2026):** „ÖFFNUNGSZEITEN" in einer Display-Serife bei `clamp(…, 9vw, …)` war auf einem 390-px-Handy **363 px breit bei 350 px Container** – die Seite liess sich horizontal wegschieben. Im Build und im HTML ist davon NICHTS zu sehen; aufgefallen ist es erst im echten Browser (`documentElement.scrollWidth > clientWidth`). **Zwei Lehren:** (1) Bei Versalien-Display-Typo im Deutschen IMMER `hyphens: auto` + `overflow-wrap: break-word` als Netz setzen – deutsche Komposita sind länger als englische Vergleichswörter. (2) Die passende Grösse nicht schätzen, sondern messen (`canvas.measureText` mit der berechneten Schrift) – hier ergab das exakt 33 px als Obergrenze, also `8.2vw` statt `9vw`.
- **`overflow-x` greift auf einem Flex-Item nicht ohne `min-width: 0` (26.08.2026):** Ein Flex-Item hat per Default `min-width: auto` – es darf also **nicht unter seine Inhaltsbreite schrumpfen**. Steht auf so einem Item `overflow-x: auto`, passiert deshalb nichts: statt zu scrollen wächst das Item auf seinen Inhalt und schiebt die ganze SEITE horizontal auf. Belegt an der Navigations-Pille der Café-Demo – vier Einträge brauchen 401 px bei 288 px Platz; erst `min-width: 0` am `<nav>` macht aus dem Seitenüberlauf einen Scroll-Container. **Wichtig: im Build ist davon nichts zu sehen** (kein Fehler, kein Warnhinweis, das HTML ist identisch); messbar ist es ausschliesslich im echten Browser über `documentElement.scrollWidth > clientWidth`. **Regel:** Jedes Flex- (und Grid-)Item, das scrollen, kürzen (`text-overflow`) oder umbrechen soll, braucht ausdrücklich `min-width: 0` (in einer Spalte entsprechend `min-height: 0`) – und der Beweis, dass es wirkt, kommt aus dem Browser, nicht aus `npm run build`. Zweiter Fall derselben Familie wie „Grosse Versalien + deutsche Komposita" darüber und ein weiterer Beleg für „Build-grün ≠ im-Browser-korrekt".
- **Datei-Icons in Next verlieren gegen ein `metadata.icons` in der Kette – die Doku sagt das Gegenteil (26.08.2026, gemessen an **Next 16.3.2** – bei einem Next-Upgrade neu prüfen, das ist Verhalten aus der Paket-Interna und keine zugesicherte API):** Die offizielle Doku behauptet „File-based metadata has the higher priority and will override the `metadata` object". Für **Icons stimmt das nicht.** Beleg im installierten Paket, `node_modules/next/dist/esm/lib/metadata/resolve-metadata.js`: die aus Dateien gesammelten Icons werden ganz am Ende nur übernommen, wenn `resolvedMetadata.icons` **falsy** ist – `if (leafSegmentStaticIcons.icon.length > 0 …) { if (!resolvedMetadata.icons) { … } }`. Ein `metadata.icons` IRGENDWO oberhalb (hier: im Root-Layout) macht den Wert truthy, und `icon.svg` wird **still verworfen**: kein Fehler, keine Warnung, die Datei wird sogar als eigene Route gebaut und ist abrufbar – sie taucht nur in keinem `<head>` auf. Genau deshalb sah es beim ersten Build wie ein Erfolg aus (`○ /icon-12dbtu.svg` in der Routenliste), war aber keiner. **Lösung:** `icons: null` im Layout der Gruppe, die ihr eigenes Icon will – typisiert erlaubt (`icons?: null | IconURL | Array<Icon> | Icons`), setzt den geerbten Wert zurück und öffnet den Weg für die Datei. **Zwei Lehren:** (1) Bei Metadata NIE aus der Doku schließen, was im `<head>` landet – im gebauten HTML nachsehen (`.next/server/app/**/*.html`), und zwar für JEDE Gruppe getrennt. (2) Eine gebaute Route ist kein Beweis, dass die Ressource auch verlinkt ist. Verwandt mit „Build-grün ≠ im-Browser-korrekt" – hier ist sogar die Routenliste grün und trotzdem falsch.
- **Mount-Animation vs. Interaktions-Animation – zwei verschiedene reduced-motion-Werkzeuge (26.08.2026):** Seit Session 16 gibt es das CSS-Gate `.entrance-anim`, weil `useReducedMotionSafe` im ERSTEN Render bewusst `false` liefert (Hydration-Regel) und Framer Mount-Animationen exakt dann dispatcht – der Hook kommt dort einen Tick zu spät. Daraus darf man NICHT „CSS-Gate immer“ machen. **Erste Trennlinie: der Startzeitpunkt.** Läuft die Animation beim Mount, muss CSS gaten. Startet sie erst auf eine Nutzeraktion (Klick, Toggle, Öffnen), ist der Hook richtig – bis dahin sind viele Frames vergangen und der echte Wert steht bereit. **Zweite Trennlinie: der Endzustand.** `.entrance-anim` erzwingt `opacity: 1; transform: none` – das ist der Endzustand einer Entrance-Animation und lässt sich für `height: auto` (ein berechneter Layout-Wert) gar nicht formulieren; ein `height: auto !important` würde das Zuklappen mit zerstören. `transition: none` greift bei Framer ohnehin nur teilweise, weil Framer den Inline-Style pro Frame neu schreibt statt CSS-Transitions zu nutzen. **Regel: Mount → CSS-Gate. Interaktion → Hook, und dort die DAUER auf 0 setzen statt den Zielwert zu überschreiben** – so bleiben Struktur, Layout und der `AnimatePresence`-Unmount unangetastet. Belegt am Mobil-Menü der Navbar (`height: 0 → auto`).
- **Angaben zur Barrierefreiheit sind Zusagen, keine Beschreibungen (26.08.2026):** „Ebenerdig", „ohne Stufe", „Rollstuhl kommt herein" liest sich wie beschreibender Fließtext, ist aber eine **Zusage**, auf die sich jemand verlässt, der ohne sie gar nicht erst losfährt. Stimmt sie nicht, ist der Schaden für den Gast real und für den Betrieb rufschädigend – anders als bei einem falsch gesetzten Komma. **Regel: dieselbe Behandlung wie die Allergene.** Wir schreiben solche Angaben nicht aus eigener Anschauung, aus einem Foto oder aus Plausibilität hinein; sie stammen vom Betrieb und werden vor dem Livegang von ihm bestätigt. Für die Demo `cafe-klee.ts` ist die Angabe erfunden wie Name und Adresse und steht deshalb im TODO auf der Liste der Werte, die vor einem echten Einsatz ersetzt bzw. bestätigt werden müssen. Gilt genauso für Angaben zu Toiletten, Aufzug, Assistenzhund und Parkplatz.
- **Die OFL-Lizenzdatei muss JEDE Kopie der Codebasis begleiten (26.08.2026):** `src/app/(demo)/fonts/OFL-Fraunces.txt` ist kein Beiwerk, sondern Bedingung. Die SIL Open Font License 1.1 erlaubt in der Präambel ausdrücklich, die Schrift einzubetten und mit Software weiterzugeben – Bedingung 2 verlangt dafür aber, dass **jede Kopie** den Copyright-Vermerk und den Lizenztext mitführt. Solange nur dieses eine Repo existiert, ist das erledigt. **Es wird in dem Moment scharf, in dem eine Kundenseite als Kopie entsteht** – und genau so ist die Demo gebaut („ein zweiter Betrieb = Kopie der Datendatei + Kopie des Ordners"). Wer den Ordner `fonts/` beim Kopieren auslässt oder die `.txt` als „im Build nicht gebraucht" wegräumt, liefert die Schrift unlizenziert aus. **Regel: Die Lizenzdatei wandert mit der Schriftdatei mit, in jedes Repo und jedes Kundenprojekt. Nicht in `.gitignore`, nicht beim Aufräumen löschen.** Dasselbe gilt für jede weitere Schrift, die später dazukommt. Beim Übergang zu einem echten Kundenprojekt zusätzlich prüfen: gibt es einen **Reserved Font Name**? Fraunces hat keinen (geprüft, Begründung in `(demo)/font.ts`) – eine andere Schrift kann einen haben, und dann darf eine subgesetzte, also modifizierte Datei den Originalnamen nicht mehr führen.
- **Animationsdauern NIE über `requestAnimationFrame` messen – der Prüf-Browser lief bei ~1 fps (26.08.2026):** Beim Nachweis des reduced-motion-Fixes am Mobil-Menü sahen alle Frame-Messungen so aus, als sei die Animation sofort fertig: Das Panel sprang binnen zwei bis drei rAF-Callbacks von 0 auf die Endhöhe. Die Erklärung war nicht der Code, sondern das Messgerät. Nachgemessen über 20 aufeinanderfolgende rAF-Callbacks: **mittlerer Frame-Abstand 1009 ms** (Einzelwerte 999–1017 ms) – also rund ein Bild pro Sekunde statt 16,7 ms. `document.visibilityState` war dabei `visible` und `document.hasFocus()` lieferte `true`, die naheliegenden Erklärungen greifen also nicht; das Fenster rendert schlicht off-screen. **Folgen, die man kennen muss:** (1) Eine 250-ms-Animation ist nach EINEM Frame vorbei – jede Dauer misst sich zu „0". (2) Framer Motion animiert auf rAF, läuft in so einem Browser also nicht in Zeitlupe, sondern in ein bis zwei Sprüngen. (3) Auch `setTimeout`-Sampling in einer engen Schleife hilft nicht, es hungert die rAF-Schleife zusätzlich aus. **Regel: Animationsdauer nicht am Frame-Verlauf messen, sondern an der Quelle** – bei Framer die `transition`-Props am React-Fiber des Knotens (`__reactFiber$…` → `memoizedProps.transition`) auslesen, bei CSS die `transition-duration`/`animation-duration` aus `getComputedStyle` oder `element.getAnimations()`. Wo wirklich der zeitliche Verlauf zählt, gehört die Messung auf ein Gerät mit echtem Compositor. **Und: den Frame-Abstand einmal mitmessen, bevor man einer Zeitmessung glaubt.**
- **Bei Demo 2: statische Schnitte gegen die Variable-Datei NEU abwägen (26.08.2026):** Für `/demo/cafe` fiel die Entscheidung klar zugunsten des statischen Fraunces-72pt-SemiBold – **18,2 KB gegen 122,2 KB** für die Variable-Datei nach demselben Latin-Subset, weil deren vier Achsen (`opsz`, `wght`, `SOFT`, `WONK`) hier keine gebraucht wird und trotzdem mitgeliefert würde. **Diese Rechnung gilt aber nur für EIN Gewicht.** Sie kippt, sobald ein Betrieb zwei oder drei Schnitte braucht – etwa Regular für Fliesstext plus SemiBold für Display, oder eine kursive Auszeichnung. Drei statische Schnitte liegen bei ~55 KB und drei getrennten Requests; die Variable-Datei bleibt bei 122,2 KB und **einem** Request, deckt dafür aber jedes Gewicht dazwischen ab und erlaubt Feinabstufungen, die mit statischen Schnitten gar nicht gehen. **Regel: Die Frage bei jedem neuen Betrieb neu stellen und wieder MESSEN statt zu übernehmen** – Anzahl wirklich gebrauchter Schnitte × Grösse des statischen Schnitts gegen die Variable-Datei, jeweils nach Subsetting. Und wenn es die Variable-Datei wird: nur die tatsächlich benötigten Achsen behalten, `SOFT` und `WONK` sind bei Fraunces reine Stilachsen und lassen sich beim Subsetten instanziieren. Verwandt mit der Regel „eine Referenz überträgt Form, nicht Informationsumfang" – auch hier gilt nicht die Entscheidung von Demo 1, sondern die Messung für Demo 2.
- **Visuelle Referenz überträgt Form, nicht Informationsumfang (26.08.2026):** Die Referenz dr-smusy.com (`docs/REFERENZ-DR-SMUSY.md`) zeigt auf `/menu` nur Bild + Produktname – keine Preise, keine Beschreibungen, keine Allergene. Das trägt dort vermutlich, weil `/menu` als Schaufenster einer Filialkette gedacht ist und nicht als Bestellliste (so lesen wir es – warum dr-smusy die Preise weglässt, wissen wir nicht). Für einen Gastro-Kunden ist genau diese Weglassung **nicht** übertragbar: der Gast erwartet den Preis auf der Karte, und was bei der Allergenkennzeichnung (LMIV / EU-VO 1169/2011) konkret Pflicht ist, hängt am jeweiligen Betrieb und ist mit ihm zu klären – wir beraten nicht rechtlich. `/demo/cafe` hat deshalb beides: `price: number` ist in `MenuItem` (`src/data/demo/types.ts`) **nicht optional**, jeder der 17 Posten trägt einen Preis; dazu Kürzel-Allergene mit Legende (A/C/G/H) und Hinweiszeile. **Nicht verwechseln:** Die Projektregel „keine Preise" gilt für **Leons eigene** Seite – sie ist eine Positionierungs-Entscheidung für eine Dienstleistung, keine Gestaltungsregel und damit auf Kundenseiten unter `/demo/*` nicht anwendbar. **Regel: Von einer Referenz übernimmst du Rhythmus, Hierarchie, Typo und Weißraum – nie den Informationsumfang. Der folgt dem Geschäftsmodell und dem rechtlichen Rahmen des Kunden.**
- **Eine Komponente darf fragen, ob ein FELD da ist – nie, ob es ein Café oder ein Restaurant ist (27.08.2026, verschärft am selben Tag):** Das ist die Grenze, an der die Demo-Bausteine skalieren oder sich verdoppeln. Erlaubt ist `if (!business.booking) return null` und `{category.sections ? … : null}` – dieselbe Form, in der `note` seit jeher gerendert wird. Verboten ist jede Frage nach dem Betrieb: `slug === 'restaurant'`, `kind.includes(…)`, `isRestaurant`. Die erste Form kostet beim dritten und vierten Betrieb nichts, die zweite verdoppelt sich mit jedem. Praktisch geprüft an zwei Runden: die Karte trägt Café-Kategorien und Restaurant-Karten mit Gängen und Menü-Bündeln, die Reservierungs-Attrappe hängt an einem optionalen `booking` – und keine einzige Komponente kennt den Betrieb. Wo ein Feld dafür optional werden muss, gehört eine **exklusive Union** darum (`items` XOR `sections`), sonst erkauft man die Flexibilität mit hinschreibbaren Fehlformen.
- **Eine neue Betriebsart gehört ins Datenmodell, nicht als Zweig in die Komponente (27.08.2026):** Beim Restaurant war die Versuchung, `DemoMenu` fragen zu lassen, ob gerade ein Café oder ein Restaurant rendert. Die tragfähige Grenze ist eine andere: Eine Komponente darf fragen „ist dieses FELD da?" – genau so wird `note` seit jeher gerendert –, aber nie „welche Art Betrieb ist das?". Das erste ist datengetrieben und skaliert auf den dritten und vierten Betrieb, das zweite verdoppelt sich mit jedem. Praktischer Nebeneffekt: wo ein Feld optional wird, gehört eine **exklusive Union** darum herum (`items` XOR `sections`), sonst erkauft man die Flexibilität mit hinschreibbaren Fehlformen – ein Eintrag ohne Inhalt, ein Bündel ohne Gänge.
- **Tailwind v4 liest die Quellen als TEXT – auch Kommentare erzeugen Utilities (27.08.2026):** Die Attrappe sollte bewusst keine Tailwind-Klasse einführen, die es im Repo nicht schon gab; Tailwind baut EINEN Utility-Chunk für die ganze App, und eine neue Klasse ändert dessen Inhalts-Hash – also auch die Auslieferung von Leons Hauptseite, die denselben Chunk lädt. Die Klassen wurden durch eigene `demo.css`-Regeln ersetzt, der Chunk blieb trotzdem verändert: Der KOMMENTAR, der erklärte, welche Utilities man hier vermeidet, nannte sie ausgeschrieben – und Tailwind fand sie dort als Kandidaten wieder. Erst als die Namen aus den Kommentaren verschwanden, war der Chunk sauber. Nachweisverfahren, das sich bewährt hat: alle Klassenregeln aus dem gebauten Chunk ziehen und jede gegen den vollständigen Quelltext des Vorgänger-Commits prüfen – 494 Regeln, keine neue.
- **Ein warnender Kommentar darf die Namen nicht enthalten, vor denen er warnt (27.08.2026):** Tailwind v4 liest die Quelldateien als **Text**, nicht als Code – es gibt keinen Parser, der einen Kommentar von echtem Markup unterscheidet. Ein Hinweis der Form „diese Utilities hier bewusst NICHT verwenden: …" schreibt sie damit genau in den gemeinsamen Utility-Chunk, ändert dessen Inhalts-Hash und damit die Auslieferung der **ganzen** App; der Kommentar bewirkt buchstäblich das Gegenteil dessen, was er sagt, und er tut es lautlos. **Regel: umschreiben statt nennen** („die Abstands-Utilities", „die Schriftgrößen-Utilities") – oder den Hinweis dorthin legen, wo der Scanner nicht liest (`docs/`). Sinngemäß gilt das für **jedes** Werkzeug, das Quellen scannt statt sie zu parsen (Klassen-Extraktoren, Secret-Scanner, Feature-Flag-Sucher): Was der Scanner findet, hat er gefunden – ob es im Kommentar stand, weiß er nicht. Mechanik und Nachweisverfahren stehen im Eintrag „Tailwind v4 liest die Quellen als TEXT" direkt darüber.
- **Eine JS-Ausnahme in einem JS-freien Baum wird benannt, begrenzt und nachgemessen (27.08.2026):** Die Reservierungs-Attrappe ist die erste Stelle unter `/demo/*`, die eigenen Client-Code braucht. Drei Dinge machen sie erträglich. **(1) Benennen:** die Zusage in `CURRENT-SCHEMA.md` heisst jetzt nicht mehr „ausschliesslich Server-Komponenten", sondern nennt die eine Datei. **(2) Begrenzen:** `'use client'` färbt nur nach unten ab – der Rahmen (`DemoBooking`), die Hülle und alle zwölf anderen Seiten bleiben serverseitig, und die Insel bekommt statt `business` nur die vier Props, die sie braucht (sonst läge die komplette Speise- und Weinkarte als Flight-Payload im HTML dieser Seite). **(3) Nachmessen statt behaupten:** die Chunk-Sätze der übrigen Seiten vor/nach dem Umbau vergleichen – byte-gleich – und im Insel-Chunk gegen die verbotenen Namen greppen (`framer-motion`, `next/link`, `fetch`, `localStorage`, `Date`, `Math.random`: je null Treffer). Die Zusage „ohne JS bedienbar" wird dabei ehrlicherweise zu „ohne JS **verständlich**": ein `<noscript><style>` blendet die Strecke aus und einen Erklärblock mit den echten Kontaktwegen ein.
- **Eine Attrappe muss auf JEDEM Schritt als solche lesbar sein, mit je anderem Satz (27.08.2026):** Ein Hinweis am Anfang reicht nicht – wer im dritten Schritt Uhrzeiten antippt, hat ihn längst vergessen. Fünf Ebenen tragen: „(Vorschau)" im Browser-Tab, ein klebendes Band, der Schrittzähler, ein Satz unter der Überschrift und einer unter dem Knopf. Der entscheidende Punkt ist, dass der Satz unter der Überschrift **je Schritt ein anderer** ist und etwas Wahres über genau diesen Schritt sagt; fünfmal derselbe Satz wird ab dem zweiten Mal nicht mehr gelesen. Dazu ein Signal ohne Text (gestrichelter Rahmen, sonst hat die Demo nur durchgezogene) und drei Dinge, die es NICHT gibt: keine Vorgangsnummer, keinen Restplatzzähler, kein „wir haben dir eine Bestätigung geschickt". Genau die machen aus einer erkennbaren Attrappe eine glaubhafte Buchung.
- **Reihenfolge, die schon im Array steckt, kein zweites Mal als Feld schreiben (27.08.2026):** Für „Gänge als Reihenfolge statt Kategorien als Sammlung" lag ein `position`- oder `order`-Feld nahe. Es ist keins entstanden: Ein Array IST eine Folge, und ein Feld daneben ist eine zweite Wahrheit, die beim nächsten Umsortieren auseinanderläuft. Wo die Reihenfolge wirklich eine eigene Aussage ist – welche Gänge in welcher Folge ein Menü ergeben –, steht sie an genau einer neuen Stelle: `MenuBundle.courses`. Und: „Gang-Sein" wurde als **Rolle** modelliert (Gang ist, was ein Bündel aufzählt), nicht als Eigenschaft des Abschnitts – sonst hätte die Weinkarte ein „ist kein Gang"-Feld gebraucht.
- **Regressionsfreiheit an bestehenden Seiten per Byte-Vergleich des gebauten HTML belegen (27.08.2026):** Die Zusage „`cafe-klee.ts` bleibt unverändert" ist wenig wert, wenn sich die Ausgabe trotzdem verschiebt. Vor dem Umbau ein `npm run build` und die sechs Café-HTMLs weggelegt, nach dem Umbau erneut gebaut und verglichen – mit Normalisierung der beiden Dinge, die sich bei jedem Build ohnehin ändern (Asset-Hashes und Build-ID). Ergebnis: sichtbares Markup und JSON-LD identisch, übrig blieb `,null,null` in der RSC-Payload einer Seite (zwei nicht vorhandene Optionalblöcke). Das ist ein Nachweis, keine Vermutung – und er kostet zwei Minuten.
- **Keine Verzeichnis-Verlinkungen ins Repo und keine Zweitbauten zur Beweisführung (27.08.2026, nach einem Schaden):** Um zwei Stände nebeneinander zu vergleichen, lag es nahe, den Vorgänger-Commit in einen zweiten Ordner zu legen und `node_modules` per **Junction** (`mklink /J`) hineinzuspiegeln, statt zweimal zu installieren. Eine Junction ist aber keine Kopie, sondern ein zweiter Name für **dieselben** Dateien: Aufräumen im Zweitordner greift durch sie hindurch und hat `node_modules` des Hauptrepos beschädigt. Gerettet hat allein `package-lock.json` – `npm ci` stellte den exakten Stand wieder her; ohne Lockfile wäre es ein Ratespiel um Versionen gewesen. **Regel: keine Junctions, Symlinks oder Hardlinks in oder auf `node_modules` und den Repo-Baum, und kein zweiter Checkout/Build nur, um etwas zu belegen.** Ein Beweis wird an **gebauten Artefakten** geführt, nicht an einem zweiten Baum: vor der Änderung `npm run build`, die interessierenden Dateien aus `.next/` in einen Ordner **außerhalb** des Repos wegkopieren, nach der Änderung neu bauen und vergleichen (Verfahren im Eintrag „Regressionsfreiheit an bestehenden Seiten per Byte-Vergleich" direkt darüber). Das kostet einen zweiten Build statt eines zweiten Baums – und kann nichts kaputtmachen, weil eine Kopie nirgendwo zurückzeigt.
- **Ein gemeinsamer Baum trägt Daten weiter als ein fremdes VOKABULAR (27.08.2026, Demo 3):** Beim Friseur nahm das Datenmodell alles auf – Dauer, Preisspannen, Mitarbeiter –, ohne dass eine Komponente einen Betriebs-Zweig bekam. **Genau eine Stelle trug nicht: das schema.org-Markup der Preisliste.** `Menu`/`MenuSection`/`MenuItem` ist dort ausdrücklich als „food or drink items available from a FoodEstablishment" definiert; für Dienstleistungen heisst dasselbe Konzept `OfferCatalog` → `Offer` → `Service` und ist keine andere Schreibweise, sondern eine andere FORM (`Offer` umschliesst das `Service`, statt selbst der Posten zu sein). Die Versuchung war, `buildMenuSchema` eine Weiche zu geben – dann hätte an jeder zweiten Zeile gestanden, welches Vokabular gerade gilt, und das ist die Verzweigung, die dieser Baum nicht haben soll. **Entschieden: ein zweiter Aufbauer daneben, und die SEITE wählt** – so, wie sie ohnehin wählt, welche Datendatei sie hineinreicht. `buildMenuSchema` blieb Zeichen für Zeichen unverändert. **Die Regel dahinter:** ein Feld darf verallgemeinert werden, ein fremdes Vokabular nicht – ihm gehört die Unterscheidung, nicht uns. Erkennungszeichen für den Fall: die Verallgemeinerung würde nur Namen tauschen, aber die Verschachtelung ändern. Was sich dabei doch verallgemeinern liess, sind die reinen NAMEN: der Betriebstyp (`seo.schemaType`, Vorgabe `Restaurant`) und die Eigenschaft, die auf die Preisliste zeigt (`hasMenu` vs. `hasOfferCatalog`) – letztere als Tabelle, weil das eine Tatsache über schema.org ist und keine über unsere Betriebe.
- **Ein Zusatzfeld muss an einem MERKMAL hängen, nicht an einer Branche – und darf die Fehlform nicht zulassen (27.08.2026, Demo 3):** Der Friseur brachte drei Dinge mit, die es vorher nicht gab. Jedes wurde als optionales Feld an ein Merkmal gebunden, das ein Betrieb hat oder nicht hat: `durationMinutes` (er verkauft Zeit), `team` (er führt Mitarbeiter), `theme` (er bringt eigene Farben mit). Umgekehrt wurden `allergens` und `seo.servesCuisine` **optional statt neutral umbenannt** – beides IST gastro-spezifisch und soll es bleiben; fehlt `allergens`, entfällt der Legendenblock von selbst, und die Überschrift „Allergene" darf deshalb weiter im Klartext in der Komponente stehen. **Der zweite Teil ist der wichtigere:** unscharfe Preise wurden als exklusive Union modelliert (`priceTo` XOR `priceOpen`), nicht als ein `priceTo?: number | null` mit `null` für „offen". Ein einzelnes Feld wäre kürzer gewesen und hätte einen stillen Fehler ermöglicht – `null` und `undefined` verwechselt man mit `??` oder einem Wahrheitswert-Test unfallfrei, und dann rendert ein Ab-Preis als Festpreis: falsche Zahl, keine Fehlermeldung. Zwei benannte Felder können das nicht. Gegenprobe, dass die Union richtig geschnitten ist: schema.org macht dieselbe Unterscheidung (`price` gegen `minPrice`/`maxPrice`).
- **Was eine Höhe braucht, muss vorgeschrieben werden – und CSS kann selbst zählen (27.08.2026):** Sobald das Navigationsband der Demo umbricht, ist seine Höhe veränderlich. Drei andere Stellen brauchen sie aber (die Kategorie-Leiste klebt darunter, das Vorschau-Band der Attrappe ebenso, Anker-Sprünge halten darüber Abstand), und **CSS kann eine gerenderte Höhe keinem anderen Selektor mitteilen**: `anchor-size()` gilt nur in Inset- und Grössen-Eigenschaften absolut positionierter Elemente, nicht in `scroll-margin-top` und nicht als `top` eines `sticky`; Container-Queries wirken nur auf Nachfahren, und die Kategorie-Leiste ist ein Geschwister-Teilbaum. **Die Lösung ist, die Höhe nicht zu messen, sondern vorzuschreiben** – und dafür eine Geometrie zu wählen, in der sie berechenbar ist: ein Gitter mit FESTER Spaltenzahl, dessen Zeilenzahl `ceil(n/2)` ist und damit allein an der Anzahl hängt. Mit `flex-wrap` wäre der Umbruchpunkt von den Beschriftungen abhängig gewesen, und die sind Daten. **Und die Anzahl liest CSS selbst**: `:has(> li:nth-child(n))` heisst „es gibt mindestens n Kinder" – damit steht die Zahl nirgends im Markup und kann nicht veralten. Ein `items.length` als Inline-Style wäre genau die zweite Wahrheit gewesen, die dieses Projekt sonst überall vermeidet. Nachgewiesen an drei bis sieben Zielen bei vier Breiten: vorgeschriebene Zeilenzahl = gerenderte, Formel = Bandhöhe auf 1 px.
- **Der Kommentar-Nebeneffekt ist keine Theorie – er hatte real eine Regel gebaut (27.08.2026, Beleg zur Regel darüber):** Beim Umbau der Demo-Navigation verkleinerte sich der gemeinsame Tailwind-Chunk um exakt 33 Byte: die Utility fuer waagerechtes Ueberlaufen verschwand. Die Klasse stand **nirgends im Repo als echtes `className`** – ihr einziger Fundort war der Kommentarkopf von `DemoNav.tsx`, der erklärte, dass die Pille sie scrolle horizontal, und den Klassennamen dabei aussprach. Der Kommentar hat die Utility also mitgebaut und in jede Auslieferung von Leons Hauptseite gelegt, die denselben Chunk lädt. **Zwei Lehren:** (1) Der Nebeneffekt ist messbar und dauerhaft, nicht bloss möglich. (2) Wer den Chunk auf „byte-gleich" prüft, muss den Unterschied ANSEHEN, statt ihn als Verstoss zu buchen: hier war die Abweichung eine Verbesserung – eine tote Regel weniger, keine neue dazu. **Der Befund hat sich beim AUFSCHREIBEN sofort wiederholt:** derselbe Klassenname landete beim Dokumentieren in `docs/CLAUDE-CODE-TODO.md` – und die Regel war zurueck, der Chunk exakt so gross wie vorher, bis auf das Byte. **Tailwind v4 scannt also auch die Markdown-Dateien unter `docs/`.** Markdown ist damit kein sicherer Ort, um vor einem Namen zu warnen; es gilt dieselbe Regel wie im Quelltext: umschreiben statt nennen.
- **Vercel Hobby = nur nicht-kommerziell (28.07.2026):** Der kostenlose Hobby-Plan ist laut Vercel-Fair-Use ausdrücklich für private/nicht-kommerzielle Nutzung. Diese Seite ist eine Personal-Brand-/Akquise-Site (kommerzieller Zweck) → spätestens beim Aufschalten einer **eigenen Domain** den Wechsel auf **Vercel Pro** prüfen (auch wg. Web-Analytics-Kontingenten). Kein Blocker fürs Preview/Testing, aber vor dem „echten" Launch klären.

---

## Session-Protokoll

| Datum | Zusammenfassung |
|-------|----------------|
| 27.08.2026 | **Session 24: `DemoNav` trägt jetzt über vier Einträge hinaus – Umbruch statt Scrollen, ohne Burger und ohne JS.** **Erst bewertet, dann gebaut.** Vier Kandidaten standen im TODO, fünf unabhängige Gutachten (Barrierefreiheit, CSS-Mechanik, Layout bei 320 px, Skalierung, Verkaufsgespräch) haben sie gegen fünf harte Kriterien geprüft – und **alle vier verworfen**: (a) *Zweizeiliger Umbruch* wurde zwar die Grundlage, hat aber mit `flex-wrap` einen Umbruchpunkt, der an den Beschriftungen hängt, also an Daten; (b) *Priorisierung mit Block im Fuss* fällt an Kriterium 1, weil der aktive Eintrag einer der herabgestuften sein kann; (c) *`:target`-Aufklappung* zahlt mit dem Zurück-Knopf und versteckt Ziele; (d) *Unterseiten zu Abschnitten* rührt den Bruch gar nicht an. Zwei zusätzlich vorgeschlagene `<details>`-Muster (nativ, JS-frei) fielen an der Vorgabe „ohne Burger" – ein Aufklapper ist auch nativ ein Aufklapper. **Gebaut wurde: Gitter mit fester Spaltenzahl, Zeilenzahl von CSS selbst gezählt** (`:has(> li:nth-child(n))` auf `.demo-scope`), `--demo-nav-h` als Formel statt Zahl. Unter 640 px zwei Spalten, darüber eine Zeile mit natürlichen Breiten wie bisher; ab sechs Zielen drei Spalten. **Gemessen statt gerechnet:** der Bruch lag bei 286 px sichtbarer Breite gegen 380/390/422 px Inhalt (Café/Restaurant/Friseur) – der aktive Eintrag lag in jeder Messreihe ausserhalb. Nachher: vorgeschriebene = gerenderte Zeilenzahl für drei bis sieben Ziele bei 320/640/768/1280 px, Formel = Bandhöhe auf 1 px, bei **sechs** Zielen auf 320 px drei Zeilen mit allem sichtbar und nichts scrollend. Der Preis steht im TODO: das klebende Band wächst von 68 auf 116 px, auf `/karte` sind das 31 % des Schirms (bei sechs Zielen 40 %); das Band ab drei Zeilen nicht mehr kleben zu lassen wäre der Hebel, widerspräche aber Kriterium 1. **Nebenbei dieselbe Fehlerklasse geschlossen:** Kategorie-Chips 32 → 44 px, Rechtslinks im Fuss 18 → 44 px, beides über ein neues Token `--demo-tap`; `.demo-anchor` rechnet jetzt aus zwei Token statt aus einer geschätzten Zahl (an sechs Sprungzielen nachgemessen: exakt 8 px unter der Klebekante). **Regression:** sichtbares Markup ausserhalb der Navigation auf allen **19** Demo-Seiten byte-gleich. Der Tailwind-Chunk wurde um genau eine Regel KLEINER – die Utility fuer waagerechtes Ueberlaufen, die nur existierte, weil der alte Kommentarkopf von `DemoNav.tsx` den Klassennamen nannte. Die Regel aus Session 23 hat sich damit an einem echten Fall belegt – und gleich ein zweites Mal, als derselbe Name beim Dokumentieren in einer Markdown-Datei landete und die Regel sofort wieder entstand: **Tailwind v4 scannt auch `docs/`.** |
| 27.08.2026 | **Session 23: Dritte Demo – Friseursalon „Salon Wirbel" unter `/demo/friseur`, erste Stufe; davor zwei neue Regeln und ein TODO.** **Doku zuerst.** Zwei Regeln in `Erkenntnisse & Regeln`: (1) **keine Verzeichnis-Verlinkungen ins Repo und keine Zweitbauten zur Beweisführung** – eine Junction auf `node_modules` hatte den Hauptbaum beschädigt, gerettet hat allein `package-lock.json` (`npm ci`); Beweise laufen über gebaute Artefakte, die nach ausserhalb des Repos kopiert werden. (2) **Ein warnender Kommentar darf die Namen nicht enthalten, vor denen er warnt** – Tailwind v4 liest Quellen als Text, ein „diese Utilities NICHT verwenden"-Hinweis schreibt sie genau dadurch in den gemeinsamen Chunk. Dazu ein neuer TODO-Punkt: **`DemoNav` trägt über vier Einträge hinaus nicht**, Muster ohne Burger und ohne JS gesucht – neu gemessen bei echten 320 px: 286 px sichtbar, Café braucht 380 px, der Friseur 422 px. **Der Friseur.** Sechs Seiten, JS-frei, 0 Fremd-Requests, `noindex`, kein Buchungsflow, erfundener Betrieb inklusive der drei Personen im Team; eigene Tokens (`theme: 'friseur'` → `.demo-scope--friseur`, kühles Porzellan + Pflaume, WCAG durchgerechnet), eigenes Icon-Paar „W", eigenes `themeColor`. **Die eigentliche Frage war, ob Restaurant und Friseur dieselben Komponenten teilen.** Antwort: ja, mit genau EINER benannten Ausnahme. Drei Eigenheiten, drei Typ-Entscheidungen: **Dauer** als Zahl (`durationMinutes`, `formatDuration()` macht ab einer Stunde „1 Std. 30 Min." daraus), **unscharfe Preise** als exklusive Union (`priceTo` XOR `priceOpen` → „52,00 – 68,00 €" bzw. „ab 49,00 €"), **Mitarbeiter** als eigene Ebene (`team` + neue `DemoTeam`, gerendert aus `DemoAbout` heraus – die Blöcke dort erzählen nacheinander, Mitarbeiter sind gleichrangig und gehören ins Raster). `allergens` und `seo.servesCuisine` wurden optional; fehlt `allergens`, entfällt die Legende, ohne dass `DemoMenu` die Branche kennt. **Nicht getragen hat das schema.org-Vokabular der Preisliste:** `Menu` ist dort für Speisen definiert, Dienstleistungen sind `OfferCatalog` → `Offer` → `Service` – andere Form, nicht nur andere Namen. Statt einer Weiche in `buildMenuSchema` steht ein zweiter Aufbauer daneben, die Seite wählt; `buildMenuSchema` blieb unverändert, `buildRestaurantSchema` heisst jetzt `buildBusinessSchema` und liest den Typ aus `seo.schemaType`. Zwei kleinere Kanten gemeldet und bewusst nicht angefasst: das Routen-Segment heisst für alle `karte`, und `DemoMenuBundles` verdrahtet „Menü"/„Festpreis" fest. **Regression beider Vorgänger ausgeschlossen, nicht behauptet:** 13 gebaute HTML-Dateien vor/nach verglichen – sichtbares Markup und JSON-LD überall identisch, sieben Seiten vollständig byte-gleich, der Rest ausschliesslich `null`-Einträge in der RSC-Payload (fehlende Dauer je Preiszeile, fehlender Team-Block je „Über uns"). Der gemeinsame **Tailwind-Chunk ist byte-gleich geblieben** (gleicher Dateiname, 50.428 B) – keine neue Utility, keine veränderte Auslieferung für Leons Hauptseite; der `demo.css`-Chunk wuchs um exakt 608 rein additive Zeichen. |
| 27.08.2026 | **Session 22: Reservierungs-Attrappe für `/demo/restaurant` plus die zwei offenen Restposten.** **(1) Die Attrappe.** Fünf Schritte – Personenzahl · Tag · Uhrzeit · Kontaktdaten · Bestätigung – unter `/demo/restaurant/reservieren`, die **nichts** tun: kein Netzwerk-Request, keine Mail, kein Speichern, auch nicht im Browser. Im echten Browser durchgeklickt und dabei gemessen: 0 Requests, `localStorage` und `sessionStorage` durchgehend leer. Gekennzeichnet ist sie **vierfach auf jedem Schritt** – „(Vorschau)" im Browser-Tab, ein klebendes Akzentband in demselben Slot, den die Kategorie-Leiste auf `/karte` nutzt (gemessen bei 68 px, auf allen fünf Schritten im Bild), der Zähler „Schritt n von 5" in der Überschrift und ein **je Schritt anderer** Satz darunter, dazu einer unter dem Weiter-Knopf. Der je Schritt andere Satz war die eigentliche Anforderung: fünfmal derselbe wäre ab Schritt zwei Bannerblindheit. **Verfügbarkeiten plausibel, ohne echt zu wirken:** das Zeitraster wird aus `hours.entries` gerechnet – `booking.services[].hoursLabel` zeigt auf ein Label dort, `buildBookingWeek()` rechnet in Minuten auf „HH:MM"-Strings, ohne `Date` und ohne Zeitzone. Ruhetage ergeben sich dadurch von selbst, Samstag bietet folgerichtig nur abends etwas an. Erfunden ist allein die Belegung, und die Legende darüber sagt das im Klartext. Kein Datum, sondern eine Beispielwoche aus Wochentagen; keine Vorgangsnummer, kein Restplatzzähler, kein „wir haben dir eine Bestätigung geschickt". Der Abschluss steht im Konjunktiv und listet darunter in vier Punkten, was NICHT passiert ist. **Die JS-Ausnahme ist benannt und begrenzt:** `DemoBookingFlow.tsx` ist die einzige Datei im Demo-Baum mit `'use client'`. Nachgewiesen an den gebauten Seiten – die **JS**-Chunk-Sätze der zwölf übrigen Demo-Seiten sind vor/nach byte-gleich, `/reservieren` bekommt genau EINEN zusätzlichen Chunk (2,7 KB gzip, ohne framer-motion/next/link/fetch/Date/Math.random). Beim **CSS** gilt das nicht: `demo.css` ist eine Datei für die ganze Gruppe und wächst mit. Leons Hauptseite bleibt dennoch unberührt – im globalen Tailwind-Chunk ist keine der 494 Klassenregeln neu, weil die Attrappe keine neue Utility einführt. Dabei einmal hineingetappt: Tailwind findet Klassennamen auch in KOMMENTAREN, der erklärende Kommentar erzeugte die vermiedenen Utilities prompt wieder. Ohne JavaScript blendet ein `<noscript><style>` die Strecke aus und einen Erklärblock mit Telefon und Mail ein, umgeschaltet rein per CSS, ohne Aufblitzen in beiden Richtungen. **Vor der Umsetzung liefen drei unabhängige Gutachten** (Verkaufsgespräch, Next/Hydration, A11y+320 px) und eine Synthese, die sie gegen den Code prüfte. Daraus übernommen und selbst nicht gesehen: keine Auto-Advance-Radios (Pfeiltasten würden zwangsweise weiterschieben, WCAG 3.2.2), `fieldset { min-width: 0 }` gegen dieselbe Überlauf-Mechanik wie damals am `<nav>`, `font-size: 1rem` an Eingabefeldern gegen den iOS-Safari-Zoom, die Fokusring-Lücke in der Farbe des Grundes statt der Füllung (auf einer gefüllten Kachel wäre `--ring` sonst unsichtbar, weil `--ring` = `--accent`), belegte Zeiten und Ruhetage als Nicht-Bedienelemente statt `disabled` – und der Fund, dass der Datenschutz-Absatz von Glut („Es gibt kein Formular und kein Buchungssystem") durch die Strecke nachprüfbar falsch wird. Der Absatz ist neu formuliert. **(2) Favicons je Betrieb.** `icon.svg` und `apple-icon.png` sind von der Gruppen-Ebene nach `(demo)/demo/<slug>/` gewandert – das Café behält sein „K" (per `git mv`, byte-gleich), das Restaurant bekommt ein „G". Beide Dateien des „G" stammen aus EINER Geometrie-Beschreibung; das 180×180-PNG wurde ohne Bildwerkzeug über ein Distanzfeld gerastert. Vier Varianten vorher als 16-px-Pixelraster verglichen: die erste mit kurzem Querbalken las sich als „C mit Klumpen". Nebenwirkung wie angekündigt: die Icon-URL der Café-Seiten ändert sich, die Bilddatei nicht. **(3) Die ausgefallene 320-px-Messung nachgeholt – gemessen, nicht gerechnet.** Das DevTools-MCP-Profil war erneut gesperrt, deshalb ein eigener headless-Chrome mit eigenem Profilordner über das Chrome DevTools Protocol (Node 24 bringt `WebSocket` mit, nichts installiert). **63 Messpunkte, grösster Überlauf 0 px:** dreizehn Seiten × 320/390/1280 px, auf `/reservieren` zusätzlich alle neun Zustände der Strecke. Café-Regression per HTML-Vergleich ausgeschlossen: sichtbares Markup auf allen sechs Seiten unverändert. |
| 27.08.2026 | **Session 21: Zweite Demo – À-la-carte-Restaurant „Restaurant Glut" unter `/demo/restaurant`, erste Stufe.** Sechs Seiten wie beim Café, JS-frei, 0 Fremd-Requests, `noindex`, eigene Tokens über `.demo-scope`, erfundener Betrieb (reservierter Berliner Nummernblock, `.example`-Domain). Kein Buchungsflow, keine Bilder – sieben Platzhalter mit den Seitenverhältnissen der späteren Fotos. **Der eigentliche Auftrag war nicht die Seite, sondern die Frage: trägt `types.ts` beide Betriebe?** Antwort: ja, und zwar ohne Kompromiss an der Café-Seite – `cafe-klee.ts` ist Zeichen für Zeichen unverändert, keine Komponente hat einen „ist das ein Restaurant?"-Zweig bekommen. Drei Eigenheiten brachte das Restaurant mit: **(1) Mehrere Karten nebeneinander.** Gelöst, indem die oberste Ebene `menu.categories` ihre Bedeutung präzisiert hat – sie ist der *Gliederungspunkt der Kartenseite*, beim Café eine Kategorie, beim Restaurant eine ganze Karte. Damit zählt `DemoCategoryRail` ohne eine Zeile Änderung die drei Karten auf. **(2) Gänge als Reihenfolge.** Neuer Typ `MenuSection` eine Ebene tiefer; die Reihenfolge steckt allein im Array – bewusst kein `position`/`order` und auch keins im JSON-LD, weil ein zweites Feld neben der Array-Reihenfolge eine zweite Wahrheit wäre. **(3) Menü-Bündel zum Festpreis.** Neuer Typ `MenuBundle` mit `courses: string[]` (`id`s der Gänge in Servierreihenfolge). `MenuCategory` ist dafür eine **exklusive Union** geworden: entweder `items` oder `sections`, und `bundles` nur im `sections`-Zweig. Das ist die eigentliche Pointe – `items` musste optional werden (die einzige Aufweichung), aber die Union fängt sie wieder ein: ein Eintrag ohne jeden Inhalt und ein Bündel ohne Gänge lassen sich gar nicht erst hinschreiben. **Drei Komponenten angefasst, alle verallgemeinert statt verzweigt:** `DemoMenu` (rendert zusätzlich `sections` und `bundles` – an das Vorhandensein des Feldes gebunden, genau wie `note` seit jeher), `DemoMenuTeaser` (neuer Helfer `leadItems()` statt `category.items`), `buildMenuSchema` (verschachtelte `MenuSection`, Bündel als `MenuItem`). Neu dazu: `DemoMenuBundles.tsx` und `menu.ts`. Unangetastet blieben unter anderem `DemoCategoryRail`, `DemoShell`, `DemoNav`, `DemoHours`, `routes.ts` und `demo.css`. **Ohne Typänderung durchgekommen:** getrennte Servicezeiten (Mittag 12–14:30, Abend 18–23) als zwei `hours.entries` mit verschiedenen Labels – `entries` war schon eine freie Liste, und schema.org will überlappende Tage ohnehin zweifach. Dazu die neue Regel, dass `label` eindeutig sein muss (React-Key). **Bewusst NICHT modelliert:** Weinpreise pro Glas UND pro Flasche; das hätte `MenuItem.price` zu einer Preisliste gemacht und `MenuRow` umgebaut – stattdessen zwei Abschnitte mit `note` „0,2 l" bzw. „Flasche 0,75 l", wie es gedruckte Karten auch lösen. **Regression ausgeschlossen, nicht behauptet:** das gebaute HTML aller sechs Café-Seiten wurde vor und nach dem Umbau byteweise verglichen – sichtbares Markup identisch, JSON-LD der Café-Karte identisch. Einziger Unterschied im ganzen Build: `,null,null` in der RSC-Payload von `/demo/cafe/karte`, die beiden nicht vorhandenen Optionalblöcke, rund zehn Byte. **Zwei Restposten offen:** das Demo-Favicon liegt auf Gruppen-Ebene, `/demo/restaurant` trägt deshalb das „K" des Cafés (der Fall war im TODO vorhergesagt; die Auflösung – Icons je Slug – hätte die Icon-URL der Café-Seiten geändert und die Byte-Gleichheit gebrochen), und die 320-px-Überlaufmessung im Browser konnte nicht laufen, weil das DevTools-Profil von einer bereits laufenden Browser-Instanz gesperrt war. Rechnerisch ist Luft, gemessen ist es nicht. |
| 26.08.2026 | **Session 20: Die drei gemeldeten Widersprüche in `cafe-klee.ts` aufgelöst – plus eigenes Favicon für die Demo-Gruppe, die Browser-Check-Sammelrunde und die lokale Demo-Schrift.** Alle drei Entscheidungen kamen vom User; Session 19 hatte sie bewusst nur gemeldet. **(1) Röstung – der Betrieb röstet selbst:** Der Block „Was im Becher landet" beginnt jetzt mit „Geröstet wird eine Straße weiter, zwei Sorten im Wechsel, dazu eine Filterkanne für alle, die es milder mögen." statt mit „Der Kaffee kommt von einer kleinen Rösterei …". Damit stimmen `intro` („rösten unseren Kaffee eine Straße weiter"), Laufband („Hausröstung aus der Nachbarschaft") und die Kartenkategorie („Hausröstung, auch als Hafer- oder Sojavariante") wieder mit „Über uns" überein – der Rest des Blocks blieb unverändert. **(2) Lage – `location.note` an das Foto angeglichen:** „Zweiter Hinterhof" passte weder zum Lead („in dieser Straße") noch zu `cafe-aussenansicht.webp`; das Foto zeigt eine Ladenfront **direkt am Gehweg** unter heller Markise, zwei Holzstühle davor, Tür ohne Stufe. Neue Fassung: „Ebenerdig direkt an der Straße, unter der hellen Markise – Kinderwagen und Rollstuhl kommen ohne Stufe herein. Bei gutem Wetter stehen ein paar Stühle draußen." Die Barrierefreiheits-Aussage der alten Fassung bleibt damit erhalten, die Markise liefert die Landmarke zum Finden, und die Stühle draußen sind eine Angabe, die dem Gast tatsächlich nützt. **(3) Halber Preis – als Datenfeld statt nur als Prosa:** Der Satz „Was am Abend übrig ist, geht in der letzten Stunde zum halben Preis raus." stand nur auf „Über uns"; wer nur die Karte las, erfuhr es nicht. Er ist jetzt zusätzlich **wortgleich** die `note` der Kategorie `backstube`. Kleinste saubere Stelle, weil dafür **keine Zeile Komponenten- oder Typ-Code** nötig war: `MenuCategory.note` existiert seit Session 17, `DemoMenu` rendert es bereits unter dem Kategorietitel, und `buildMenuSchema` gibt es bereits als `MenuSection.description` aus – die Aussage landet also gratis auch im JSON-LD. Der Karten-Auszug auf der Startseite zeigt Kategorie-Notizen bewusst nicht, dort bleibt es aus. Bewusst KEIN neues Feld und kein zweiter Prosa-Absatz in einer Komponente. **(4) Doku-Zählung geradegezogen:** die bisherigen Einträge „Session 19" und „Session 20" waren EINE Chat-Session und stehen jetzt als ein Session-19-Eintrag im Aufbau (1)/(2)/… – ohne das wäre die Zählung dauerhaft um eins verschoben geblieben. **(5) Neue Erkenntnis aufgenommen:** `overflow-x` greift auf einem Flex-Item nicht ohne `min-width: 0` (Fundstelle Session 19, jetzt unter „Erkenntnisse & Regeln" statt nur im Sessionprotokoll). **(6) Eigenes Favicon für `/demo/*`:** Die Demo trug bis dahin Leons LL-Monogramm im Tab – über einem Auftritt, der wie eine eigenständige Café-Seite wirken soll, das falsche Signal. Neu ist `src/app/(demo)/icon.svg`: ein handgeschriebenes SVG ohne externe Assets und ohne Bildwerkzeug, ein „K" aus drei Stroke-Pfaden in `#b4441e` auf `#f6f1e7` mit Hairline `#dbcfba` – alle drei Werte 1:1 die Tokens aus `(demo)/demo.css`. Der Buchstabe ist bewusst als PFADE gezeichnet und nicht als `<text>`: ein Favicon-SVG darf sich nicht auf eine Systemschrift verlassen, sonst sieht es auf jedem Betriebssystem anders aus. **Der eigentliche Fund steckte im Framework:** die Datei allein bewirkt nichts. Next übernimmt dateibasierte Icons nur, wenn `resolvedMetadata.icons` falsy ist; das `metadata.icons` des Root-Layouts machte den Wert truthy, also wurde `icon.svg` zwar als Route gebaut (`○ /icon-12dbtu.svg` stand in der Routenliste, die Datei war abrufbar) – tauchte aber in KEINEM `<head>` auf. Erst `icons: null` im Demo-Layout macht den Weg frei. Genau der Punkt, den die Aufgabe zu prüfen statt anzunehmen verlangte, und die Session-19-Beobachtung „theme-color überschreibt, icons nicht" ist damit erklärt statt nur notiert (eigene Regel unter „Erkenntnisse & Regeln"). Bewusst KEIN `apple-icon`: die Bild-Konvention kennt dafür nur JPG/PNG, ein PNG-Satz war ausgeschlossen – die Demo-Seiten tragen deshalb gar kein `apple-touch-icon`-Tag mehr. Was iOS beim „Zum Home-Bildschirm" daraus macht, ist NICHT geprüft und im Layout-Kommentar ausdrücklich als offen markiert statt behauptet (eine erste Fassung des Kommentars hatte dort „Safari nimmt einen Screenshot" stehen – eine Vermutung, die eine adversarielle Gegenprüfung zu Recht angegriffen hat; sie ist rausgeflogen). **(7) Doku:** neue Regel „Angaben zur Barrierefreiheit sind Zusagen, keine Beschreibungen" – die `location.note` aus (2) sagt „ebenerdig, ohne Stufe", und das ist eine Zusage, auf die sich jemand verlässt, der ohne sie gar nicht losfährt. Behandlung wie die Allergene: der Betrieb bestätigt sie vor dem Livegang, wir befüllen sie nicht selbst. Steht als Erkenntnis in dieser Datei und operativ im TODO bei den Werten, die vor einem echten Einsatz zu ersetzen sind. **(8) Browser-Check-Sammelrunde auf Leons Hauptseite, plus die zwei offenen Motion-Punkte gefixt.** **Fix A – `scroll-behavior`:** Die Regel `html { scroll-behavior: smooth }` in `globals.css` stammt aus der Vite-Zeit und war nie gegatet; für `prefers-reduced-motion: reduce` ist ein selbstlaufender Scroll über die halbe Seite genau die Bewegung, die die Präferenz meint. Jetzt als Rücknahme im selben `@layer base` (`@media (prefers-reduced-motion: reduce) { html { scroll-behavior: auto } }`) statt als `no-preference`-Variante – so bleibt das Standardverhalten auch in Browsern erhalten, die die Query nicht kennen, und die Datei behält ihre Reihenfolge „normal definieren, danach für reduce zurücknehmen“. Gemessen: computed `scrollBehavior` springt `smooth` → `auto`, sobald der Block scharf ist. **Fix B – Mobil-Menü:** über `useReducedMotionSafe` gelöst, NICHT über `.entrance-anim`. Begründung als eigene Regel unter „Erkenntnisse & Regeln“ festgehalten (Mount- vs. Interaktions-Animation): Der Hook funktioniert hier, weil die Animation erst auf Klick startet, viele Frames nach dem Mount – und das CSS-Gate passt nicht, weil sein erzwungener Endzustand `transform: none; opacity: 1` lautet, der Endzustand hier aber `height: auto` ist, ein berechneter Layout-Wert. Struktur bleibt 0 → auto (damit `AnimatePresence` sauber unmountet), nur die DAUER fällt auf 0. **Messmethode ehrlich benannt:** Frame-Timing war im Prüf-Browser nicht messbar – `requestAnimationFrame` lief dort bei ~1 fps (1009 ms mittlerer Frame-Abstand über 20 Frames, trotz `visibilityState: visible` und `hasFocus: true`), damit sieht JEDE Animation „sofort fertig“ aus. Gemessen wurde deshalb das, was der Fix tatsächlich ändert: die `transition`-Props, die React an Framer reicht, ausgelesen über den React-Fiber am Panel-Knoten – ohne reduce `{ duration: 0.25, ease: 'easeOut' }`, mit reduce (`matchMedia` per `initScript` auf reduce gepatcht) `{ duration: 0 }`. Endzustand in beiden Fällen `height: auto; opacity: 1` bei 277 px; Zuklappen entfernt das Panel weiterhin aus dem DOM, `duration: 0` bricht `AnimatePresence` also nicht. **Sicht-Checks, alle bestanden** (Dark **und** OS-Light, 320/390/1280 px): Fokusringe an Logo, Nav-Link, Navbar-CTA, Footer-Impressum/Datenschutz, Projekt-Link, mailto und „← Zurück zur Startseite“ durchgehend `rgb(167,139,250)` = `#a78bfa` = `--accent`/`--ring`, 2 px Ring auf 2 px Offset in `--background`; nach Mausklick kein Ring. Hero-CTA-Hover `translateY(-4px)` mit Lift-Schatten aus `color-mix(--accent 30%)`, Navbar-CTA bleibt `transform: none` (nur Opazität 0,9 + Glow 22→30 px) – Sticky-Leiste ruhig, wie vorgesehen. Projekte-Karten: Lift `translate: 0px -6px` aus `motion-safe:hover:-translate-y-1.5` (also bereits reduce-gegatet), Rand `--accent`/50 %, Glow `--accent`/20 %, Links mit `target="_blank"`, `rel="noopener noreferrer"` und `aria-label` inkl. „(öffnet in neuem Tab)“. Favicon auf allen vier Routen je 5 Tags, alle fünf Dateien 200 mit korrektem `content-type`. Keine Hydration-Warnung – einzige Konsolenmeldung ist der erwartete 404 auf `/_vercel/insights/script.js`. Dark-only hält bei OS-Einstellung *light*: `color-scheme: dark`, Hintergrund `rgb(10,10,10)`. 0 px horizontaler Überlauf auf allen drei Breiten, auch bei offenem Mobil-Menü. Fünf echte `reduce`-Blöcke im gebauten CSS geprüft (Hero-Blobs/Terminal-Cursor, `.entrance-anim`, `.aurora-text`, `.cta-gradient` inkl. Lift-Rücknahme, Marquee) – alle greifen. **Zwei Beobachtungen bewusst NICHT gefixt** (nicht Teil des Auftrags, hier als Fundstelle): (a) **CLS 0,0021** auf `/` – 15 Verschiebungen à 0,00013 aus der Hero-Zählerzeile, weil die hochzählenden Ziffern nicht tabellarisch gesetzt sind und die Zeile beim Stellenwechsel um 1 px wandert; das sind 2 % des „gut“-Budgets von 0,1, der Fix wäre eine Zeile Tabellenziffern wie in der Demo-Preisspalte. (b) **Tap-Flächen im Mobil-Menü 36 px** hoch (`py-2` + `text-sm`) – über dem WCAG-2.2-AA-Minimum von 24 px, aber unter den 44 px, die die Demo-Seiten erreichen. **(9) Lokale Display-Schrift für `/demo/*` und das offene apple-icon.** **Schrift-Auswahl aus drei Kandidaten, nach gemessenen Kriterien statt Geschmack.** *Lizenz:* alle drei liefern eine `OFL.txt` mit; die Texte sind bis auf die Copyright-Zeile **byte-identisch** (per `diff` geprüft) – SIL Open Font License 1.1, deren Präambel Einbettung und Weitergabe wörtlich erlaubt („can be bundled, embedded, redistributed and/or sold with any software"). Damit schied niemand aus. *Zeichensatz:* Ä Ö Ü ä ö ü ß, Versal-ẞ, €, Gedankenstrich und deutsche Anführungszeichen sind bei allen dreien vorhanden (cmap 624 / 567 / 333 Zeichen) – schied ebenfalls niemand aus. *WOFF2 nach Latin-Subset:* Fraunces 72pt SemiBold **18,1 KB**, Instrument Serif 21,4 KB, Young Serif 26,6 KB, Fraunces **variabel** 122,2 KB. *Wirkung bei grossen Versalien:* im Specimen mit den echten Demo-Strings bei 33 px in einer 280-px-Box verglichen – Young Serif am kräftigsten, aber auch am breitesten; Instrument Serif elegant, bei 400 aber zu zart für die Plakat-Rolle; Fraunces trägt als Optical-Size-Schnitt für Displaygrössen. **Entschieden hat am Ende das GEWICHT:** `.demo-display` setzt `font-weight: 600`, und nur Fraunces liefert dafür einen echten Schnitt (`usWeightClass 600`); Young Serif und Instrument Serif gibt es ausschliesslich in 400, der Browser hätte den Fettschnitt rechnen müssen (Faux Bold) oder das Design hätte sein Gewicht ändern müssen. Gewählt ist der **statische** 72pt-Schnitt, nicht die Variable-Datei: deren vier Achsen (`opsz`, `wght`, `SOFT`, `WONK`) braucht hier keine, kosten aber das 6,7-Fache. **Die OFL verlangt, dass jede Kopie die Lizenz mitführt** – `fonts/OFL-Fraunces.txt` liegt deshalb neben der WOFF2 im Repo. **0 Fremd-Requests halten:** `next/font/local` emittiert die Datei nach `/_next/static/media/`, im Browser nachgezählt sind es 14 Requests auf `/demo/cafe/kontakt`, **alle** auf die eigene Herkunft. **`8.2vw` → `9vw` nachgezogen:** die alte Zahl war für die System-Serife gemessen. Neu bei 320 px am längsten vorhandenen Display-String ermittelt („ÖFFNUNGSZEITEN", Container 280 px): Fraunces braucht **9,198 px Breite je px Schriftgrösse**, die alte Serife 10,34 – also 11 % weniger. Sweep in echtem Layout (Canvas-Messung und `Range.getClientRects()` stimmten auf 0,1 px überein): 8.2vw → 257,6 px, 9.0vw → 264,9 px (15,1 px Reserve, gewählt), 9.4vw → 276,7 px (3,3 px), ab 9.6vw bricht das Wort auf drei Zeilen. Nicht die Obergrenze genommen, weil 1,2 % Reserve keine Rendering-Unterschiede zwischen Engines überleben. Nebenbefund: bei 320 px gewinnt bis 8.75vw die clamp-UNTERGRENZE (1.75rem), darunter ändert jeder Wert dort gar nichts – und selbst bei 9.6vw bleibt der Seitenüberlauf 0 px, weil `hyphens: auto` + `overflow-wrap` aus Session 17 greifen. **CLS 0 auf allen sechs Demo-Seiten** bei 320, 390 und 1280 px gemessen (`PerformanceObserver` je Seite in einem iframe, nach `document.fonts.ready`). Möglich macht das `adjustFontFallback: 'Times New Roman'` – Next rechnet daraus eine metrisch angeglichene Ersatzschrift: `size-adjust: 108.79%`, `ascent-override: 89.9%`, `descent-override: 23.44%`, `line-gap-override: 0%`. Spender ist bewusst Times New Roman und nicht der Vorgabewert Arial, weil Fraunces eine Serife ist. **apple-icon nachgeholt:** das frühere Non-Goal „kein Bildwerkzeug" war falsch – mit `sharp` (per `npm install --no-save`, `package.json` und Lockfile per md5 vorher/nachher als unverändert bestätigt) ist das vorhandene K-SVG zu `src/app/(demo)/apple-icon.png` gerastert, 180×180, 1,7 KB. Bewusst NICHT 1:1 das Tab-Icon: vollflächig **opak** und ohne Eckenrundung (iOS legt seine eigene Maske darüber, transparente Ecken würden schwarz), ohne die Haarlinie (die liegt genau im Beschnitt) und mit zentrierter, auf 0,92 skalierter Glyphe. Damit tragen die Demo-Seiten jetzt beide Tags – `rel="icon"` auf das SVG und `rel="apple-touch-icon"` auf das PNG – und der stille Rückfall auf Leons `/apple-touch-icon.png` ist geschlossen. Leons vier `(site)`-Routen plus `_not-found` tragen unverändert ihren Fünfer-Satz und laden weder die Schriftdatei noch ein `@font-face`. **(10) Vier kleine offene Punkte abgeräumt, drei davon aus den eigenen Berichten dieser Session.** **Lizenzfrage geklärt – Fraunces hat KEINEN Reserved Font Name.** Die OFL definiert den Begriff als „any names specified as such after the copyright statement(s)"; die Copyright-Zeile lautet vollständig „Copyright 2018 The Fraunces Project Authors (…)", danach folgt nur der Standard-Lizenztext, und die Zeichenkette `with Reserved Font Name` kommt in der Datei **null Mal** vor. Das war zu prüfen, weil unsere subgesetzte Datei eine „Modified Version" im Sinne der Lizenz ist und Klausel 3 solchen Versionen das Führen der Reserved Font Names verbietet. Da es keine gibt, greift die Klausel nicht – **keine Umbenennung nötig**. Zusätzlich entschärft: `next/font` vergibt der Familie im ausgelieferten CSS ohnehin den Namen `demoDisplay`, der Originalname wird den Nutzern also gar nicht präsentiert, und genau darauf schränkt Klausel 3 sich ein („only applies to the primary font name as presented to the users"). Antwort und Beleg stehen jetzt im Kopf von `(demo)/font.ts`, damit die Frage erledigt bleibt. **CLS auf `/` geschlossen: 0,00211 → 0,000009.** Root Cause war nicht, wie zuerst vermutet, die Ziffernbreite, sondern die **Ziffernanzahl**: Der Zähler „100%" zählt von 0 hoch, die Zahl wächst von einer auf drei Stellen, und das `%` wandert dabei mit. Tabellenziffern allein haben das deshalb **nicht** behoben – gegengemessen 0,00036 statt 0,00031 vorher, also unverändert. Geschlossen hat es erst die feste Breite: Die Ziffern laufen jetzt in einer `inline-block`-Box mit `min-width: <Stellenzahl>ch`, und dafür braucht es die Tabellenziffern, weil `1ch` die Vorschubbreite der „0" ist und erst mit `tabular-nums` JEDE Ziffer exakt so breit ist. Im Browser verifiziert: reservierte Breite 59,959 px für den dreistelligen Zähler, tatsächliche Breite 59,95 px – die Box wächst also nicht mehr, und **kein einziger Layout-Shift geht mehr von den Zählern aus**. Was bleibt, sind zwei Shifts des Terminal-Cursors (zusammen 0,000009), der beim Tippen mitwandert – das ist der Animation eigen und liegt vier Grössenordnungen unter der „gut"-Schwelle von 0,1. Gemessen mit demselben Ablauf wie die Ausgangsmessung (langsam durch die ganze Seite scrollen). **Tap-Flächen im Mobil-Menü 36 → 44 px.** Die Demo-Seiten erreichen 44–64 px, die Hauptseite ist das Portfolio-Stück und darf nicht schlechter sein. Drei Stellen in `Navbar.tsx`: die sechs Menü-Einträge und der Menü-CTA von `py-2` auf `py-3` (12 + 20 + 12 = 44), der Burger-Button von `h-9 w-9` auf `h-11 w-11` (36×36 → 44×44). Der Burger war in der Meldung vorher nicht enthalten, ist aber dieselbe Fehlerklasse und die Schaltfläche, die das Menü überhaupt öffnet. Nachgemessen bei 320 **und** 390 px: Burger 44×44, alle sechs Einträge exakt 44 px hoch, Navigationsleiste weiterhin 64 px hoch, 0 px horizontaler Überlauf, dark-only hält auch bei OS-Light. **Drei neue Regeln** unter „Erkenntnisse & Regeln": die OFL-Lizenzdatei muss jede Kopie der Codebasis begleiten (scharf, sobald Kundenseiten Kopien sind); Animationsdauern nie über `requestAnimationFrame` messen, weil der Prüf-Browser bei ~1 fps lief; und bei Demo 2 statische Schnitte gegen die Variable-Datei neu abwägen statt die Entscheidung von Demo 1 zu übernehmen. **Non-Goals eingehalten:** Schrift-Auswahl, `9vw` und die Icons sind unangetastet geblieben. **Verifiziert:** `npm run build` grün (Next 16.3.2, 14 Routen als Static, inklusive der neuen Icon-Route). **Head-Vergleich im gebauten HTML statt Annahme:** alle sechs Seiten unter `.next/server/app/demo/` tragen genau EIN Icon-Tag (`<link rel="icon" href="/icon-…svg" sizes="any" type="image/svg+xml">`) und null Treffer auf `favicon`/`apple-touch-icon`; `index.html`, `moeglichkeiten.html`, `impressum.html`, `datenschutz.html` **und** `_not-found.html` tragen unverändert dieselben fünf Tags in derselben Reihenfolge wie vorher. Die ausgelieferte Icon-Route antwortet mit `content-type: image/svg+xml` und ist byte-identisch zur Quelldatei. 16-px-Lesbarkeit im Browser gemessen statt geschätzt (auf 16 px gerastert, dann nearest-neighbour vergrößert): Strich 2,5 px, Glyphe 9 px hoch, als „K" klar erkennbar, im hellen wie im dunklen Tab-Streifen; die Hairline verschwindet unterhalb von 32 px erwartungsgemäß und ist als reine Kante-bei-großer-Darstellung eingeplant. Ein adversarieller Prüflauf (6 unabhängige Prüfer, 18 Findings, jedes von bis zu fünf Gegenprüfern angegriffen) hinterließ **0 bestätigte Findings**; die einzige Substanz war die oben genannte Kommentar-Formulierung zu iOS, die korrigiert wurde. Überlauf-Check im echten Browser **nur dort, wo Text dazugekommen ist** – `/demo/cafe/karte` (neue Kategorie-Notiz) und `/demo/cafe/kontakt` (längere `location.note`), plus `/demo/cafe/ueber-uns` als Gegenprobe: **0 px horizontaler Überlauf** bei 320, 390 und 1280 px, 0 Konsolenmeldungen. Beide neuen Texte brechen sauber in ihrer Spalte um und ragen an keiner Stelle darüber hinaus (`scrollWidth == clientWidth` am Absatz selbst): die Kategorie-Notiz läuft bei 320 px über drei Zeilen (58 px hoch in 280 px Spalte) und ab 390 px über zwei, die `location.note` über vier Zeilen (94 px). Die Notiz sitzt in derselben Versalien-/Laufweiten-Optik wie „bis 14 Uhr" und „Hausröstung, …" – kein neuer Stil. **Non-Goals eingehalten:** keine neuen Bilder, kein Font-Wechsel, keine weitere Umformulierung der User-Texte (der Halbpreis-Satz wurde für die Karte NICHT umgeschrieben, sondern wortgleich übernommen); beim Favicon kein PNG-Satz, kein Bildwerkzeug, keine externe Ressource und keine Zeile Änderung an Leons Icon-Satz. |
| 26.08.2026 | **Session 19: `/demo/cafe` von einer Single-Page auf sechs Routen – Mehrseitigkeit, „Über uns", Rechtsseiten.** **(1) Aufteilung auf drei Routen:** `/demo/cafe` (Kopf, Laufband, Bildreihe, Karten-Auszug), `/demo/cafe/karte` (volle Karte) und `/demo/cafe/kontakt` (Adresse, Zeiten, Telefon, Reservierung) – Segmente rein ASCII, Aufbau nach der frisch erstellten Referenzanalyse. **Reiner Refactor:** kein neuer Fließtext, alles kommt weiter aus `cafe-klee.ts`; einzige Datenergänzung ist ein `nav`-Block mit kurzen Beschriftungen, weil eine mehrseitige Site ohne Navigationslabels nicht existieren kann. `GastroDemo.tsx` entfiel zugunsten von `DemoShell` (Farbwelt, Korn, Navigation, Fuß), das jede Seite um ihren Inhalt legt – bewusst kein `layout.tsx`, weil ein Layout den aktiven Pfad nicht kennt und `usePathname()` ein Client-Hook wäre und damit die Zusicherung „diese Seite braucht kein JavaScript" bräche. Aus demselben Grund sind die Seitenwechsel normale `<a href>` und nicht `next/link` (in Next 16 eine Client-Komponente), und die Navigation ist eine horizontal scrollbare Pille statt eines Burgers. Die Kategorie-Leiste auf `/karte` klebt als zweite Reihe unter der Navigation und arbeitet mit reinen `#`-Ankern; beide Höhen hängen an EINER Variablen `--demo-nav-h`, damit auch `scroll-margin-top` der Überschriften stimmt. schema.org folgt der Aufteilung: `Restaurant` auf der Startseite (mit `hasMenu` als URL auf die Karte), `Menu` auf `/karte`. Die Bildreihe wanderte aus `DemoLocation` in ein eigenes `DemoGallery` auf die Startseite. Zwei Dinge unterwegs korrigiert: das `<`-Escaping im JSON-LD war ein No-op (`'\u003c'` in einem JS-String IST `<`) und ist beim Umzug in `DemoJsonLd` scharf gestellt worden; das Navigationsband bekam einen deckenden Hintergrund, weil links und rechts neben der schmalen Pille die Versalien-Überschriften durchliefen und wie ein Fehler aussahen. **(2) Vierte Seite `/demo/cafe/ueber-uns`:** Lead und drei Blöcke im Bild-Text-Wechsel (Bild links / rechts / links, ab `md` zweispaltig, darunter gestapelt), zum Schluss zwei Links auf Karte und Kontakt – einer gefüllt, einer Ghost. Die Umstellung auf „Bild rechts" läuft über `order`, nicht über einen zweiten Markup-Zweig: im Quelltext steht damit IMMER erst das Bild und dann der Text, die Vorlesereihenfolge bleibt über alle Blöcke gleich. Texte kamen vom User und sprechen den Leser nie direkt an; sie passen dadurch ohne Umbau neben die „Du"-Stellen der Bestandsdaten. **(3) Navigation, vierter Eintrag:** vorher bei 320 px **gemessen statt geschätzt**, indem ein echter vierter Eintrag mit denselben Klassen in die laufende Pille gehängt wurde. Ergebnis: drei Einträge brauchen 287 px und passen mit **1 px** Luft in die verfügbaren 288 px, vier brauchen **401 px**, also 113 px zu viel. Zwei Konsequenzen: `min-width: 0` am `<nav>` (ohne das wächst das Flex-Item auf seine Inhaltsbreite und zieht die ganze SEITE horizontal auf, statt dass `overflow-x` greift – verifiziert, steht als eigene Regel unter „Erkenntnisse & Regeln") und ein Scroll-Hinweis an der rechten Kante, rein über CSS-Verläufe: Schatten am Container (`scroll`), Abdeckung am Inhalt (`local`) – am Ende der Leiste schiebt sich die Abdeckung über den Schatten und der Hinweis verschwindet von selbst, ohne dass jemand mitzählen müsste. Kein JavaScript, kein Burger. **(4) Rechtsseiten:** `/demo/cafe/impressum` und `/demo/cafe/datenschutz`. Der Footer ist dafür schlank geworden – bis dahin stand das volle Impressum auf JEDER Seite, jetzt nur noch Betriebsname plus die zwei Links. Beide Seiten tragen den Muster-/Demo-Hinweis sichtbar ganz oben (abgesetzt mit Akzentkante), nicht klein am Fuß: der Betrieb ist erfunden, und im Kundengespräch darf das nicht untergehen. Die Datenschutzseite beschreibt bewusst den TATSÄCHLICHEN Zustand – keine Cookies, keine Reichweitenmessung (die Gruppe `(demo)` bindet kein Analytics ein, das liegt in `SiteChrome`), keine Fremd-Requests, Auslieferung als statisches HTML bei Vercel, Route/Telefon/Mail nur als Links – statt Standardbausteine zu behaupten, die hier gar nicht zutreffen. Ausdrücklich keine Rechtsberatung. **(5) Bilder:** drei Quellen aus `Downloads` zuerst gemessen, dann motivbezogen beschnitten und NICHT hochgerechnet: `cafe-aussenansicht.webp` (1448×1086 im 4∶3 → 121 px nur unten weg, das war leere Fahrbahn → 1080×720, 100 KB), `cafe-handwerk.webp` (1122×1402 → 2 px auf exakt 4∶5 getrimmt → 1080×1350, 86 KB), `cafe-team.webp` (1536×1024, exakt 3∶2, kein Zuschnitt → 1080×720, 52 KB). Wie schon in Session 18 liegen alle drei Quellen unter den Maßen, die in ihren eigenen Dateinamen gefordert waren – hochgerechnet wurde trotzdem nichts. **(6) Zwei inhaltliche Widersprüche gemeldet statt still korrigiert** (als offener Punkt ins TODO, aufgelöst erst in Session 20): die Röstung („eine Straße weiter" / „Hausröstung" gegen „kommt von einer kleinen Rösterei") und die Lage („Zweiter Hinterhof" gegen „in dieser Straße" plus ein Foto mit Straßenfront und Bordstein); dazu der weichere Punkt, dass der Halbpreis-Satz nur auf „Über uns" stand. **Verifiziert:** 0 px horizontaler Überlauf auf allen sechs Demo-Seiten bei 320/390/1280 px, 0 Konsolenmeldungen, 0 Fremd-Requests, Tap-Flächen 44–64 px; die klebende Kategorie-Leiste sitzt exakt unter der Navigation, Anker-Sprünge landen 3 px darunter statt darunter zu verschwinden. Ohne JavaScript nachgewiesen (per `fetch`/`curl`, das kein Skript ausführt): alle sechs Seiten liefern vollständiges HTML, jede trägt alle vier Navigationsziele plus die zwei Fußlinks als echtes `<a href>`, jeder interne Link antwortet mit 200, jedes Sprungziel der Kategorie-Leiste existiert im Markup, und es gibt weder `href="#"` noch `onclick` noch Buttons als Navigation. Der Scroll-Hinweis wurde im Browser gegengeprüft: bei `scrollLeft 0` sichtbar, am rechten Ende weg. `npm run build` grün (Next 16.3.2, 13 Routen als Static). **Non-Goals eingehalten:** keine neuen Bilder außer den drei für „Über uns", kein Font-Wechsel, keine Änderung an Leons Hauptseite (der Diff berührt ausschließlich `src/**/demo*` und `src/data/demo/`). |
| 26.08.2026 | **Session 18: Echte Fotos in der Café-Demo, gemessene Design-Referenz, Doku-Abschluss.** **(1) Vier echte Fotos für `/demo/cafe`** (Commit `20cc48f`): nach Motiv zugeordnet, beschnitten, herunterskaliert und als WebP q80 nach `public/demo/` – `cafe-gastraum.webp` (1120×1400, 4∶5, 73 KB) → `hero.photo`, `cafe-zimtschnecken.webp` (800×800, 166 KB) → `gallery[0]`, `cafe-cappuccino.webp` (800×800, 37 KB) → `gallery[1]`, `cafe-fensterplatz.webp` (1410×940, 3∶2, 71 KB) → `gallery[2]`. Weil die Boxen den Platz seit Session 17 bereits im richtigen Seitenverhältnis reservieren, hat das Einsetzen **nichts verschoben** – im echten Browser bei 390 px verifiziert: CLS 0, kein horizontaler Überlauf, alle vier Bilder laden, 0 Fremd-Requests. **Bewusst NICHT hochgerechnet:** Hero 1120 statt 1600 px (die Quelle war nur 1122 px breit) und Fensterplatz 1410 statt 1600 px (Quelle 1672×941 im 16∶9 – der Schnitt auf 3∶2 kostet Breite). Auf dem Handy unkritisch (3,2× bzw. 4,0×); an der 768 px breiten Inhaltsspalte am Retina-Desktop erreicht der Fensterplatz 1,84×, der Hero nur 1,46× – Ersatz also erst nötig, falls die Demo je am großen Bildschirm gezeigt wird. Zuschnitte **motivbezogen statt mittig** (der Fensterplatz ausschließlich rechts beschnitten, damit Fenster und Lichteinfall – das eigentliche Motiv – ganz erhalten bleiben). Dabei drei zu niedrige `sizes`-Angaben korrigiert (Hero 640→768, Querformat 660→768, Quadrate 330→378 – sie lagen unter den real gerenderten Breiten) und die `alt`-Texte an die tatsächlichen Motive angepasst. `sharp` wie in Session 11 per `npm install --no-save` → `package.json`/Lockfile unverändert, Originale in `Downloads` unangetastet. **(2) Visuelle Referenzanalyse `docs/REFERENZ-DR-SMUSY.md`** (neu): dr-smusy.com **gemessen statt geschätzt** – Startseite, `/menu` und `/locations`, je bei 1440 px und echten 390 px (DPR 3), alle Werte per `getComputedStyle` ausgelesen. Festgehalten sind Typo-Hierarchie (Rolle → Größe/Gewicht/Zeilenabstand/Laufweite), Farbsystem, Spacing-Skala, Sektionsabfolge inkl. Mobil-Umbau und die Karten-Anatomie für Adresse/Öffnungszeiten. **Kernbefunde:** eine Schriftfamilie (Cairo) in vier Schnitten; Flächen-Schichtung über zwei Töne mit ~2 % Helligkeitsunterschied, komplett **ohne Border und Schatten**; enge Zeilenabstände (1.12–1.16) auf Display gegen 1.28 im Fließtext; Laufweite wächst, wenn die Schrift schrumpft (0 % bei 60–96 px bis 3 % bei 14 px); 24 als durchgehender Radius-/Padding-/Gap-Takt mit großem Sprung auf 64/104/160; responsiv skaliert **nur** die Display-Ebene, alles ab 20 px abwärts bleibt exakt gleich. Die Datei benennt ausdrücklich auch, was **nicht** übernommen wird – die fremde Schriftfamilie, das konkrete Beige-Duo (die Demo hat mit `demo.css` längst ihre eigene warme Papier-/Terrakotta-Palette, übertragbar ist nur das Prinzip) und vor allem der dortige Verzicht auf Preise und Allergene, der für einen Gastro-Kunden gerade **nicht** trägt (eigener Punkt unter „Erkenntnisse & Regeln") – und listet die Inkonsistenzen der Referenz, damit sie nicht mitkopiert werden. **Umgesetzt ist davon noch nichts** – die Datei ist Arbeitsgrundlage, kein Änderungsprotokoll. **(3) Doku-Abschluss:** ein Multi-Agent-Audit (5 parallele Prüfer, 3 Entwürfe, 3 adversarielle Gegenprüfungen) fand **40 Stellen**, an denen die Doku dem Repo hinterherlief. Korrigiert sind u.a. der alte Umlaut-Routenname an sieben Stellen, drei Verweise auf das seit der Migration nicht mehr existierende `src/index.css`, der Widerspruch `src/data/site.ts` ↔ `site-url.ts`, die falsche Aussage „Analytics im Root-Layout" (liegt seit Session 17 in `SiteChrome` – die Demo-Seiten werden bewusst NICHT getrackt), die fehlenden Demo-Fotos in der Asset-Tabelle sowie die veraltete Phasen-Aussage „alle Sektionen außer Projekte" (die Sektion steht seit 09.06.2026). Die Gegenprüfung fand außerdem **zwei echte Fehler in der frischen Referenz-Analyse selbst** – „drei Schnitte" statt der gemessenen vier und die Behauptung „unsere Demo ist Dark-only", obwohl die Café-Demo bewusst hell ist; beide vor dem Commit korrigiert. **Der Bogen der letzten Sessions in je einem Satz** (Details in deren eigenen Einträgen darunter): **Session 14** brachte die Migration von Vite auf Next.js mit App Router, wodurch der selbstgebaute Prerender ersatzlos entfiel und alle Routen beim Build statisch rendern; **Session 15** ersetzte `/möglichkeiten` durch die ASCII-Route `/moeglichkeiten` und stellte die Hero-Terminal-Copy auf den echten Next-Output um; **Session 16** schloss die reduced-motion-Lücke der Entrance-Animationen über ein CSS-Gate, das ausschließlich innerhalb der `prefers-reduced-motion`-Query greift; **Session 17** legte die Demo-Café-Seite `/demo/cafe` als stille Route an und trennte dafür die Seitenhülle in `SiteChrome`, sodass die Demo ohne Navbar/Footer und mit eigenen Tokens auf `.demo-scope` läuft. |
| 25.08.2026 | **Session 17: Demo-Café-Seite `/demo/cafe` als stille Route.** Erstes Schaufenster-Stück für Kundengespräche – ein **erfundener** Betrieb („Café Klee"), bewusst das Gegenteil von Leons Auftritt: warmes Papier statt Near-Black, Terrakotta statt Violett, Display-Serife in Versalien statt Sans. **(1) Layout-Trennung:** Navbar/Footer/ScrollProgress/Analytics sind aus dem Root-Layout in `src/components/SiteChrome.tsx` gewandert und werden nur noch von `(site)/layout.tsx` und `not-found.tsx` genutzt – ein Eltern-Layout lässt sich in Next von unten nicht abwählen, die Hülle musste also eine Ebene tiefer. Die Hauptseite blieb dabei **markup-identisch** (Zeichen-Diff; nur ein interner `useId`-Präfix verschob sich), Kosten gzip **+157–182 Byte/Seite**. **(2) Eigene Tokens:** `(demo)/demo.css` belegt dieselben Variablennamen auf `.demo-scope` neu → alle bestehenden Tailwind-Utilities werden warm, ohne dass `:root` angefasst wird; die Datei landet in einem eigenen CSS-Chunk, den die Hauptseite nicht lädt. **(3) Datengetrieben:** ALLE Inhalte in `src/data/demo/cafe-klee.ts` (Typ in `types.ts`), Komponenten in `src/components/demo/` lesen ausschliesslich daraus – ein zweiter Betrieb ist eine zweite Datendatei + eine `page.tsx`. **(4) Inhalt:** Hero, Karte mit Kategorien/Preisen/Allergenen (Preisliste mit gepunkteten Führungslinien, Tabellenziffern), Öffnungszeiten inkl. Ruhetag, Adresse mit Routen-Link, Telefon zum Antippen, Reservierung als vorbefüllte Mail, Impressum-Platzhalter, dazu ein Laufband (nutzt die bestehende `Marquee` inkl. deren reduced-motion-Gate). **(5) schema.org** Restaurant + Menu als JSON-LD, komplett aus den Daten gebaut (17 MenuItems, Ruhetag korrekt NICHT als openingHours). **(6) Kein JavaScript nötig:** reine Server-Komponenten, Telefon/Route/Mail sind Links, das Laufband läuft über CSS. **Im echten Browser geprüft (Chrome DevTools, 320/390/1280 px):** ein Überlauf gefunden und behoben – „ÖFFNUNGSZEITEN" war bei `9vw` breiter als der Container und zog die Seite horizontal auf (im Build unsichtbar); Fix `8.2vw` + `hyphens: auto`. Ausserdem die Bildreihe von drei auf zwei Spalten umgestellt (Kacheln waren nur 111 px breit). Ergebnis: 0 px horizontaler Überlauf bei 320 px, CLS 0, 0 Konsolenmeldungen, 0 Fremd-Ressourcen, Tap-Flächen 48–64 px. **Non-Goals eingehalten:** keine Webfonts vom CDN (Charakter kommt aus Grösse/Versalien/Laufweite einer System-Serife), kein Karten-Embed (nur ein Link), kein Analytics, kein Cookie-Banner, kein Bestellsystem, keine Änderung an der Hauptseite. |
| 25.08.2026 | **Session 16: reduced-motion-Lücke bei den Entrance-Animationen geschlossen (CSS-Gate).** Nutzer mit `prefers-reduced-motion: reduce` sahen Navbar und Hero trotzdem einfahren – `useReducedMotionSafe` liefert im ersten Render bewusst `false` (Hydration-Regel), und genau dann dispatcht Framer die Mount-Animation; der echte Wert kommt einen Tick zu spät. **Lösung ohne Hook-Umbau:** neue Klasse `.entrance-anim` in `globals.css`, komplett innerhalb `@media (prefers-reduced-motion: reduce)`, die Framers INLINE-Styles per `!important` auf den Endzustand zwingt (`opacity:1; transform:none; filter:none; transition:none; animation:none`) – wirkt ab dem ersten Paint, also **kein Flash**. **Beim Prüfen kam mehr heraus als gemeldet:** neben Navbar + Hero-Terminal hatten auch **Problem, Leistungen, Über mich, Kontakt** und der **Reveal auf /moeglichkeiten** gar kein reduce-Gate (deren `whileInView`-Varianten schauen nie auf `reduce`) – dieselbe Technik, gleiche Klasse, 20 Elemente auf `/` + 6 auf `/moeglichkeiten`. **Bewusst NICHT gegatet:** `ServiceDiagram` (dessen `show`-Zustände sind `translateX(18px)`, `width:82%`, `clipPath` – `transform:none` hätte den Toggle-Knopf verschoben; der bestehende `key`-Remount löst es dort korrekt) sowie Prozess/Projekte/Statement (bereits sauber über `useReducedMotionSafe` gegatet, verifiziert). **Für normale Nutzer nachweislich unverändert:** die Klasse taucht im gebauten CSS nur innerhalb der reduced-motion-Query auf, der Quell-Diff enthält keine einzige Timing-/Varianten-Änderung, und kein gegatetes Element trägt selbst `transition-`/`animation`-Klassen (die liegen auf den Kindern: Card, CTA, Cursor). Hydration-Regel und Hook unangetastet. `npm run build` + `tsc` grün. |
| 25.08.2026 | **Session 15: Stille Route auf ASCII + Hero-Terminal auf den echten Stack.** **(1) `/möglichkeiten` → `/moeglichkeiten`.** Die beiden Ordner aus Session 14 (`m%C3%B6glichkeiten/` kanonisch + `möglichkeiten/` als Re-Export-Sicherheitsnetz) sind durch **einen** sauberen Ordner `src/app/moeglichkeiten/` ersetzt. Damit ist die ganze Encoding-Frage weg: kein percent-encodeter Ordnername, keine Doppelung, keine offene Unsicherheit, wie Vercels Routing-Layer normalisiert. **Bewusst KEIN Redirect** von der alten URL – die Seite war nie verlinkt und soll den Umlaut nicht weiterschleppen; die alte URL liefert jetzt 404 (verifiziert). **Title/Description unverändert** („Möglichkeiten – Leon Lang" + gleiche Description) – der Umlaut gehört in den sichtbaren Text, nicht in die URL. `routes` in `src/data/site-url.ts` und damit die Sitemap angepasst; Route bleibt **still** (nicht in `navItems`/Footer). Nebenbei alle Kommentar-Referenzen in `src/` auf den neuen Pfad gezogen. **(2) Hero-Terminal-Copy auf Next** (nur Textdaten in `src/data/hero.ts`, keine Komponente angefasst): `install` zeigt jetzt `npx create-next-app@latest dein-projekt --ts --tailwind --app` + `framer-motion`/`lucide-react` statt `npm create vite@latest` + `react-router-dom` + `@tailwindcss/vite`; `build` zeigt die Next-Ausgabe (`▲ Next.js – Production Build`, `Route (app)`, `○ … prerendered`, „Alle Seiten statisch – kein Server nötig") statt `vite v8` + `dist/assets`-Zeilen. `deploy` und `whoami` unverändert. **Anonymisierung gilt weiter:** keine echten Konto-/Repo-/Deploy-Identifier (`dein-projekt`, `https://deine-seite.de`); die gezeigten Routen sind generische Dummies – **die stille Route taucht dort bewusst NICHT auf**. **Keine erfundenen Messwerte** mehr (die alten Bundle-Größen/Sekunden sind ersatzlos weg, die neue Ausgabe zeigt nur die Form). Zeilenzahl je Tab bewusst identisch gehalten (build = 7 Zeilen, Default-Tab bestimmt die Panel-Höhe) → keine Layout-Änderung. `npm run build` grün; alle Routen per HTTP-Status gegen `next start` verifiziert, alte URL 404, Alt-Strings (`vite v8`, `dist/assets`, `react-router-dom`) nirgends mehr im Client-Bundle. |
| 24.08.2026 | **Session 14: Migration Vite → Next.js (App Router).** Gleiches Design, gleiche Inhalte, alle vier Routen. **(1) Rendering:** der selbstgebaute Prerender (`renderToString` + `StaticRouter` + `scripts/prerender.mjs` + `src/entry-server.tsx` + `src/main.tsx`-Hydrate-Split) ist ersatzlos entfallen – `next build` rendert alle Routen statisch (`○ Static`), `npm run build` ist jetzt EIN Befehl statt vier. **(2) Head:** `META`-Map → **Metadata API**, neue Single Source of Truth `src/data/meta.ts` (`routeMeta` + `pageMetadata`); Title/Description/OpenGraph je Route **inhaltlich unverändert** (im gebauten HTML Wert für Wert verglichen), Favicon-Satz über `metadata.icons`, `theme-color`/`color-scheme` über den `viewport`-Export; `index.html` entfallen. **(3) Routing:** react-router-dom raus – `Link` → `next/link`, `Layout.tsx` → Root-Layout, `NotFound` → `app/not-found.tsx` (jetzt **echter HTTP-404**); der Scroll-nach-oben-`useEffect` entfiel (App Router scrollt selbst). **(4) Bilder:** die drei statischen Bilder auf `next/image` (Porträt via `fill`, Projekt-Previews mit intrinsischer `width`/`height` + `sizes`). **(5) Neu:** `app/sitemap.ts` + `app/robots.ts` über die Next-Dateikonventionen, Basis-URL env-gesteuert (keine Domain hardcodiert). **(6) Tooling:** Tailwind v4 von `@tailwindcss/vite` auf `@tailwindcss/postcss`, `src/index.css` → `src/app/globals.css`, eine `tsconfig.json` statt drei, `.next/` statt `dist/`. **(7) Env:** `VITE_FORMSPREE_ENDPOINT` → `NEXT_PUBLIC_FORMSPREE_ENDPOINT`. **Größter Stolperstein:** `/möglichkeiten` war build-grün, wurde als `○ Static` gelistet – und lieferte trotzdem **404**. Next übernimmt statische Ordnernamen 1:1 in die Route-Regex, Browser senden aber percent-encodet; Fix = Ordner `src/app/m%C3%B6glichkeiten/` (an drei Stellen als Absicht dokumentiert). Daraus die verschärfte Regel: „im Build vorhanden" ≠ „per URL erreichbar" – jede Route gegen den echten Server statusprüfen. **Hydration-Regel unangetastet** (`useReducedMotionSafe`, kein `window`/Zufall im Render); Design, Tokens, Animationen, Dark-only, Sektionen und Formspree 1:1. **Adversariell reviewt** (3 Agenten – Hydration / reduced-motion / Prerender-Inhalt): **0 Hydration-Bugs**, **0 reduced-motion-Regressionen** (`globals.css` ist ein byte-identischer Move, Guards stehen auch nach der Minifizierung in korrekter Kaskaden-Reihenfolge, alle 9 `useReducedMotionSafe`-Konsumenten diffen NUR auf `'use client'`), und der Prerender-Inhalt ist **Textknoten für Textknoten identisch** mit dem alten Build (der Prerender-Reviewer hat `425ad2d` in einem separaten Worktree mit der alten Pipeline gebaut und gegen `.next/server/app/` diffed: 112/15/15/66 Textknoten je Route, 0 nur-alt, 0 nur-neu, Reihenfolge erhalten; Head je Route exakt gleich, `og:url`/`og:image` auf keiner Route). **4 bestätigte Findings gefixt:** (1) `siteUrl` lag in `site.ts`, das Client-Komponenten importieren → in ein server-only `src/data/site-url.ts` gezogen (sonst still `localhost` im Browser-Bundle – unabhängig von zwei Reviewern gefunden); (2) 20 Dateien hatten beim Einfügen von `'use client'` ihren abschließenden Zeilenumbruch verloren; (3) drei Doku-Stellen nannten weiter `VITE_FORMSPREE_ENDPOINT` und widersprachen dem Rest; (4) der Kommentar in `meta.ts` behauptete, nur og werde ausgegeben – Next leitet aus openGraph automatisch `twitter:card/title/description` ab (nicht abschaltbar, auch `twitter: null` greift nicht – geprüft; ohne og:image inhaltlich neutral). **Nicht gefixt, bewusst:** die Entrance-Animationen von Navbar/Hero greifen für reduced-motion-Nutzer nicht (vorbestehend, byte-identisch; JS-seitig nicht lösbar ohne die Hydration-Regel zu brechen – braucht ein CSS-Gate und damit eine Entscheidung des Users) sowie das ungegatete `scroll-behavior: smooth`; beides als Follow-up in der TODO. `npm run build` grün, alle Routen + Assets + `sitemap.xml`/`robots.txt` gegen `next start` per HTTP-Status verifiziert. **Offen:** Vercel-Settings nachziehen (Env-Rename, Framework-Preset), Browser-Check, Hero-Terminal-Text zeigt noch den Vite-Build (bewusst nicht angefasst – Non-Goal). |
| 27.–28.07.2026 | **Session 13: Pre-Launch-Politur & Go-Live-Vorbereitung (konsolidiert).** (Detail je Schritt im Prompt-Protokoll #24–#28.) **(1) Projekte-Sektion live** (#24): zwei Showcase-Karten (Blumen Lang / Naillery) mit **committeten Vorschaubildern** (`public/blumen-lang-preview.webp` / `naillery-preview.webp`), externen „zur Website"-Links, Live-URLs im Data-Layer. **(2) Hero-Terminal anonymisiert** (#25): alle echten Konto-/Projekt-/Deploy-Identifier (`lngleon`, `leons-webseite`, echte `.vercel.app`) → sprechende Dummies (`dein-projekt`, `deine-seite.de`); nur Textdaten in `src/data/hero.ts`, Look/Tabs/Animation unverändert. **(3) Pre-Launch-Audit (read-only) + 6 Feinschliff-Fixes** (#26): `/möglichkeiten` bekam ein echtes `<h1>` (neue `SectionHeading`-`as`-Prop) + **Folge-Fix der Überschriften-Hierarchie** (Review deckte `h1→h3`-Skip auf → Bento-/Abschnitts-Titel auf `h2`, Tilt-Titel auf `h3`, jetzt `h1→h2→h3`, rein semantisch); benannte Nav-Landmarks (`aria-label`); `Counter` auf `useReducedMotionSafe`; einheitliche Link-Fokusringe (Nav/Footer/Logo/Zurück wie CTAs); totes `site.description` entfernt; `rel="noopener noreferrer"` vereinheitlicht. Adversariell reviewt (1 Finding bestätigt+gefixt). **(4) Favicon-Satz** (#27): finales „LL"+Cursor-SVG + `.ico`/PNG-16/32/`apple-touch-icon` + `theme-color #0a0a0a` im `<head>` ALLER Routen; **Erkenntnis:** Template-Head-Kommentare dürfen kein `<title>`-Literal enthalten (Prerender-Strip-Regex fraß sonst die Favicon-`<link>`s). Finales SVG-Design (zwei L's + Cursor-Block) aus dem Duplikat übernommen, `favicon (1).svg` gelöscht. **(5) Vercel Web Analytics** (#28): `@vercel/analytics` (nachinstalliert – war entgegen Annahme nicht da) einmal zentral im `Layout`; SSR-/prerender-sicher (rendert `null`, Insights-Script erst nach Mount im Browser, 0 Markup im prerenderten HTML), SPA-Auto-Track via History-API; cookielos (kein Cookie-Banner). **Vom User im Vercel-Dashboard aktiviert + als live bestätigt.** **Durchweg:** Dark-only, nur Tokens, SSR-/prerender-sicher, `npm run build` grün + im `dist` verifiziert; jeder Schritt committet + gepusht (Auto-Deploy). **Offen:** Impressum/Datenschutz-Texte (User, e-recht24; Datenschutz muss den cookielosen Analytics-Dienst erwähnen), **Browser-Check-Sammelrunde** (Favicon-Tab, Projekte-Karten, CTA-Lift, mobiler + Link-Fokusringe), Vercel-Pro bei eigener Domain (Hobby = nicht-kommerziell). |
| 09.06.2026 | **Session 12: UI-Feinschliff-Serie (konsolidiert) – Statement · /möglichkeiten-Live-Visual · Scroll-Balken · CTA-Glow/Fluss/Lift · a11y-Fokusring.** Mehrere zusammenhängende Schritte dieser Arbeitssession, zu EINEM Eintrag zusammengeführt (Detail je Schritt im Prompt-Protokoll #18–#23). **(1) Statement-Sektion** (#18) direkt vor „Kontakt" (`src/sections/Statement.tsx`): zwei nacheinander einblendende Zeilen „Nicht noch eine Seite von der Stange." / „Sondern deine." („deine" via `.accent-gradient-text`); Reveal-Mechanik in neuer wiederverwendbarer `TextAnimation` (Vorlagen-API nachgebaut, blur+fade+y), SSR-/reduced-motion-sicher (`useReducedMotionSafe`, `useInView`+`animate`), Text im prerenderten HTML. **(2) Live-Visual auf `/möglichkeiten`** (#19): interaktiver Dot-Globe `Earth` (WebGL via neuer Dependency **`cobe`** ^2.0.1, dynamisch importiert/code-split) + hand-gebaute Canvas-`Sparkles`; nur Violett aus Tokens, SSR-/reduced-motion-/leak-sicher (rAF/Observer/Context abgebaut); cobe-v2-API (kein `onRender`, Canvas-Re-Parenting) sauber adaptiert (eigenes Wrapper-`<div>`). **(3) Scroll-Fortschrittsbalken** `ScrollProgress` (#20): schmal, fix `top-0 z-[60]` über der Navbar, `var(--accent-gradient)`, `scaleX` an `useScroll`; SSR-sicher (startet `scaleX(0)`), reduced-motion direkt gebunden. **(4) Primär-CTA-Politur** (#20–#22): Violett-Glow (`Card`-Glow-Sprache, Ruhe/Hover/`:active`, Fokusring-komponierend) + langsam FLIESSENDE Gradient-Füllung (registrierte `@property --cta-pos`, AuroraText-Weg, nur `--accent-gradient-strong` → weiße Schrift ≥4.5:1) + spürbarer Hover-Lift NUR auf den großen Content-CTAs via `main .cta-gradient` (`translateY(-4px)` + additiver Akzent-Lift-Schatten; Navbar ruhig); rein CSS, reduced-motion-sicher, nur `index.css`. **(5) a11y** (#23): Fokusring am mobilen Navbar-CTA ergänzt (war als einziger ohne) → WCAG 2.4.7; alle 5 CTAs tragen jetzt denselben `focus-visible:ring`-Satz. **Durchweg:** Dark-only, nur Akzent-Tokens (kein Hex), SSR-/prerender-sicher, Startseite/andere Sektionen unangetastet; jeder Schritt adversariell reviewt (Multi-Agent), bestätigte Findings gefixt; `npm run build` durchweg grün, im `dist` verifiziert. **Browser-Status:** vom User LIVE bestätigt → Statement, `/möglichkeiten` (Globus/Tilt/CoolMode/Marquee), „Über mich"-Foto, Scroll-Balken, CTA-Glow, fließende Füllung. NOCH NICHT browser-geprüft (nur build-grün + gepusht) → stärkerer `-4px`-Hover-Lift + a11y-Fokusring (letzte 2 Commits). **Offen:** Lighthouse-Zahl ungemessen (Platzhalter „—" bleibt), Navbar-Link zu `/möglichkeiten` bewusst noch still. |
| 09.06.2026 | **Session 11: Echtes Porträt in „Über mich".** Den 4∶5-Platzhalter (gerahmte Hülle mit „LL" / „Porträt folgt") durch das echte Foto als `object-cover`-`<img>` ersetzt. Quelle `Leon-me.png` (1122×1402, exakt 4∶5) fürs Web optimiert: auf 768×960 skaliert (2× der 384px-Box → retina-scharf) und als **WebP** (Quality 80) nach `public/leon-portrait.webp` (~20 KB statt 1,78 MB PNG; `sharp` einmalig via `npm install --no-save` → KEINE neue Projekt-Dependency, `package.json`/Lock unverändert). 4∶5-Box bleibt erhalten (kein Layout-Shift; `bg-muted` als ruhiger Platzhalter, bis das Bild da ist), `loading="lazy"` + `decoding="async"` (below the fold), `width`/`height` gesetzt, deutscher Alt-Text „Porträtfoto von Leon Lang". Statisches `<img>` aus `public/` → prerender-/SSR-sicher (im gebauten `dist/index.html` verifiziert). Dark-only/Layout/Responsive unverändert; KEIN Logo/Favicon (separater Task), keine andere Sektion angefasst. `npm run build` grün. |
| 08.06.2026 | **Session 10: SSG live + AuroraText-Clip-Fix + Per-Route-Meta + stille Route `/möglichkeiten` (konsolidiert).** Mehrere zusammenhängende Schritte dieser Session, zu EINEM Eintrag zusammengeführt (Detail je Schritt im Prompt-Protokoll #14–#17). **(1) SSG/Prerendering – live.** Alle Routen fallen beim Build als statisches HTML mit echtem Inhalt raus (H1 + Sektions-Text ohne JS im Quelltext → LCP/SEO). Verfahren: eigenes `renderToString` + `StaticRouter` (react-router v7 *Library*-Mode) + `scripts/prerender.mjs` – NICHT `vite-react-ssg` (peert auf react-router-dom ^6), NICHT Framework-Mode, NICHT Puppeteer (würde Reveals einfrieren). `build` = `tsc -b && vite build && vite build --ssr src/entry-server.tsx --outDir dist-ssr && node scripts/prerender.mjs`; neu: `src/entry-server.tsx`, `scripts/prerender.mjs`, `src/hooks/useReducedMotionSafe.ts`; `main.tsx` macht `hydrateRoot` (prerendert) bzw. `createRoot` (Dev); `dist-ssr/` gitignored. Hydration-Regel „Server-Frame = erstes Client-Frame" via `useReducedMotionSafe` (Hero/Terminal/ServiceDiagram/Prozess), Hero-H1 ohne Entrance-Opacity, deterministischer Hero-BG. **Vercel Build Command auf `npm run build` gesichert** (sonst liefe nur `vite build` ohne Prerender) → SSG ist live. **(2) AuroraText + @property-Clip-Fix.** Das fließende Hero-Akzentwort „spürbar" (`AuroraText`, reine CSS-@keyframes, nur `--accent-gradient`) wurde an der H1 live geschaltet, rendete live aber transparent/unsichtbar. Ursache: DIREKT animierte `background-position` auf `background-clip:text` lässt Chromium die geclippte Glyphen-Maske fallen (kein Lightning-CSS-/Prefix-/Deploy-Problem – empirisch ausgeschlossen; DevTools-Strikethrough von `-webkit-background-clip` ist nur ein Alias-Artefakt). Fix: Flow über eine **registrierte `@property --aurora-pos`** animieren statt `background-position` direkt, + `display:inline-block`, Clip-Technik 1:1 wie `.accent-gradient-text`; reduced-motion = statisch sichtbar, Fallback nie unsichtbar. **(3) Per-Route `<head>`.** Schluss mit dem geteilten Head: jede Route bekommt eigenen `<title>` + `<meta description>` + Basis-OpenGraph (`og:title`/`og:description`/`og:type`) über die `META`-Map in `prerender.mjs` (dependency-frei; NICHT React-19-Component-Metadata, da `renderToString` nur den Body liefert → Tags lägen nicht im `<head>`). `og:image`/`og:url` ausgelassen (kein Logo/Domain). **(4) Stille Route `/möglichkeiten`** („Was möglich ist", prerendert + eigener Title/Meta, BEWUSST nicht in der Navbar; react-router v7 dekodiert den Pathname → Umlaut-Route matcht auch percent-encoded). Aufbau: `SectionHeading`-Kopf → **Bento** (große Zelle „Diese Seite selbst" = handgebaute STATISCHE Mono-Grafik mit sichtbarem Perf-Platzhalter „—"; drei Zellen via `ServiceDiagram` wiederverwendet) → „verspielt vs. seriös" → subtile 3D-Tilt-Karte → `Marquee` (echter Tech-Stack). 4 neue Komponenten in `src/components/ui/`: `Marquee`, `InteractiveHoverButton`, `CoolMode` (Klick-Partikel; bunte hsl-Farben als EINZIGE gekapselte Ausnahme; SSR-safe; `#_coolMode_effect`+rAF beim Route-Verlassen restlos abgebaut – kein Leak), `Tilt` (Aceternity-Mechanik on-brand, max. ~6°). **Durchweg:** alles SSR-/reduced-motion-/hover-safe, keine erfundenen Daten, AuroraText/SSG/Meta-Map jeweils unangetastet weitergetragen; `npm run build` grün und im gebauten `dist`-HTML verifiziert (H1 sichtbar, per-Route-Title/Meta unterschiedlich, `/möglichkeiten` mit vollem Inhalt). **Offen:** `/möglichkeiten` im Browser visuell prüfen (Tilt/Partikel/Marquee/reduced-motion). |
| 08.06.2026 | **Session 9: AuroraText (fließendes Hero-Akzentwort).** Das Hero-Akzentwort „spürbar" bekommt statt des statischen Violett-Gradients einen langsam fließenden „Aurora-Text"-Effekt (Magic-UI-Stil, API `<AuroraText>Wort</AuroraText>`). Neue wiederverwendbare Inline-Komponente `src/components/AuroraText.tsx` (Konvention wie `CodeTag`/`SectionHeading`). Effekt als **reine CSS-@keyframes** (`aurora-flow`: `background-position` über einen verbreiterten Gradient `background-size:200%`, sanftes `alternate`/ease-in-out, 8 s – langsam/edel, kein Flackern, kein Seam), **nicht** Framer-Motion-getrieben → SSR-/hydration-sicher (kein JS-State, kein Hydration-Mismatch; das Wort steckt im prerenderten HTML, durchgehend sichtbar = LCP, im Build verifiziert). **Ausschließlich Violett:** nutzt `--accent-gradient` (Source-of-Truth) verbatim – kein Blau/Cyan/Pink von Magic UI, nichts hardcoden. Soliden Fallback (`var(--accent)` via `@supports`) behalten; `prefers-reduced-motion` → der jetzige statische Gradient. Bewusst NUR am Hero-Wort – Eyebrows, Hero-Zähler und andere Gradient-Stellen bleiben statisch via `.accent-gradient-text`. `npm run build` grün. |
| 08.06.2026 | **Session 8: Look & Foundation (Violett · Dark-only · Schaubilder · Terminal · Code-Tags).** Mehrere Schritte dieser Session, zu einem kohärenten Eintrag zusammengeführt: **(1) Finale Akzentfarbe Violett** – Platzhalter #c8a96a ersetzt, ausschließlich über zentrale CSS-Variablen. **(2) Hero-Umbau** – 2-spaltig (Text/Zähler links, Terminal rechts; mobil gestapelt), code-basierter generativer Violett-Hintergrund (`color-mix`-Blobs + statisches SVG-`feTurbulence`-Korn, CSS-Keyframe-Drift, kein Neon/keine Bilder) + „Terminal", das echten Build-Output tippt (4 Tabs install/**build**/deploy/whoami, blinkender Cursor, Pfeiltasten-Tabs, keine Refresh-/Download-Buttons); bestehender Inhalt erhalten (Claim, Unterzeile, 4 hochzählende Zähler, CTA → `#kontakt`), Counter-Logik unverändert. **(3) Leistungen → lebende Schaubilder** – je Karte ein ehrliches Mini-Schaubild (Browser-Build-up · App-Reaktion · **Redesign Vorher/Nachher-Wisch** · KI-Chat-Stream), ein gemeinsamer ruhiger Rahmen (`ServiceDiagram.tsx`), einmal beim Reinscrollen + Hover-Replay, keine Dauerloops; Card/SectionHeading/2×2 + Titel/Texte unverändert. **(4) Dark-only + Violett-Gradient** – Light Mode + Theme-Toggle + dead Light-Code entfernt (ein `:root`-Token-Satz, `color-scheme: dark`, `html` ohne Klasse; ThemeProvider/useTheme/ThemeToggle/Theme-Typ + Terminal-`.dark`-Scope raus); Gradient-Tokens `--accent-gradient` (Showcase-Text-Clip mit solidem Fallback) + `--accent-gradient-strong` (CTA-Füllung, weiße Schrift ≥4.5:1 = min. 5.05:1, objektiv verifiziert); flaches `--accent`/`--accent-solid` + Fokusring bleiben flach (Ring nie Gradient); Gradient nur auf Showcase (Hero-Akzentwort „spürbar", 4 Hero-Zahlen, primäre CTAs, Eyebrow). **(5) Code-Tag-Politur** – dekorative Inline-`CodeTag` (Mono, flacher Akzent, kein Gradient) sparsam auf vorhandene Begriffe (`Web-Apps`, `Buchungssystem`/`Dashboards`, `Code`), keine erfundenen Claims/Zahlen. Durchweg reduced-motion-sicher, a11y/Fokusringe intakt; Terminal/4 Schaubilder/Hero-Hintergrund/Counter nicht kaputtgemacht. Adversariell reviewt (mehrere Workflows → keine offenen Findings). `npm run build` durchweg grün. **Offen/Follow-up:** echter LCP-Gewinn bräuchte Prerender/SSG (Seite ist reines CSR). |
| 07.06.2026 | **Sektionen-Sprint – Zusammenfassung dieser Session.** Gebaut: **Leistungen**, **„Über mich"**, **Prozess** (inkl. **Reload-Bugfix** der Prozess-Animation) und **Kontakt** (Formspree-Formular + Direkt-Buttons, adversariell a11y-geprüft). Details je Schritt in den Einträgen unten + im Prompt-Protokoll. **Größter Stolperstein – Formspree live:** Das Formular lief lokal sauber (Endpoint in `.env.local`), live auf Vercel aber NICHT – `.env.local` ist per `.gitignore` ausgeschlossen und gelangt daher nie zu Vercel. Symptom: Fehler OHNE Network-Request (`fetch` gegen eine `undefined`-URL). **Fix:** `VITE_FORMSPREE_ENDPOINT` zusätzlich als Vercel-Env-Variable (alle Environments) gesetzt + Redeploy. Jetzt **end-to-end bestätigt** – lokal UND live, Mail kommt an. Außerdem Vercel-Status nach Dashboard-Verifikation wieder auf ✅ korrigiert (war zwischenzeitlich fälschlich als „kein Projekt" notiert). |
| 07.06.2026 | **Session 7: Doku-Korrektur Vercel-Status.** Die in Session 6 notierte Einschätzung („Account `lngleon` hat kein Projekt → kein Auto-Deploy") war ein Fehlschluss aus einer CLI-Prüfung im falschen Account-Kontext. Im Vercel-Dashboard (Account „leon's projects") verifiziert: Projekt `leons-webseite` ist mit `main` verbunden, alle Commits dieser Session deployen sauber auf Production, oberstes Deployment 19c48be („Kontakt") = Ready. Vercel-Status wieder auf ✅ gesetzt (Infrastruktur-Status + Verhaltensregeln). Lehre: Dashboard ist maßgeblich; bei CLI-Checks den Account-Kontext (`vercel whoami` / Team) prüfen. Nur Doku. |
| 07.06.2026 | **Session 6: „Kontakt"-Sektion (letzte Sektion).** Zweispaltig (`id="kontakt"`): links Formspree-Formular (`VITE_FORMSPREE_ENDPOINT`, JSON-POST), rechts „Lieber direkt?" mit 3 Buttons (Mail/WhatsApp/Instagram); auf Mobil gestapelt mit Formular oben. State-Machine idle→sending→success/error, Client-Validierung (Pflichtfelder + E-Mail-Format), Erfolg leert das Formular + Erfolgsmeldung, Fehler retrybar. Neues `--destructive`-Theme-Token (beide Modes) für Validierungs-/Fehlerfarben. Geteilte `BrandIcons` (Instagram + WhatsApp) angelegt, Footer darauf umgestellt. **Adversarielles Review-Workflow** über das Formular → 5 bestätigte Findings: a11y-Fixes angewandt (Fokus zum ersten Fehlerfeld bei Validierung [WCAG 3.3.1], Fokus ins Erfolgs-Panel [2.4.3], Doppelsende-Guard via ref, Placeholder-Kontrast `/80`). Offen/Phase 3: Light-Mode-Kontrast der Akzentfarbe als Text + Fokusring (siehe „Bekannte Bugs"). Dark + Light, responsive, Akzent nur über `--accent`, Fade-up. `npm run build` läuft. |
| 02.06.2026 | **Session 5: Bugfix Prozess-Animation.** Beim Neuladen mit der Sektion im Viewport liefen Schritt-Stagger und Linien-Aufbau nicht (beim Reinscrollen schon), nur bei Prozess. Ursache: `whileInView`-Geste propagiert den tiefen Variant-Baum (`ol → li → Linien`) beim „bereits im Viewport beim Mount"-Fall unzuverlässig. Fix: Orchestrierung auf `useInView` + gesteuertes `animate` umgestellt (statt `whileInView`); reduced-motion-Verhalten unverändert. `npm run build` läuft. Siehe „Bekannte Bugs". |
| 02.06.2026 | **Session 4: „Prozess"-Sektion.** Fünfte Sektion auf `/` (`id="prozess"`): vier nummerierte Schritte als sichtbare Abfolge 1→4 (Kennenlernen & Idee → Konzept & Design → Umsetzung → Launch & Betreuung) als `<ol>`/`<li>`-Stepper, NICHT als loses Card-Grid. Verbindungslinie zwischen den Schritten: Desktop horizontal, Mobil vertikal gestapelt (zwei Connector-Elemente je Schritt, `scaleX`/`scaleY`). Akzent-getönte Nummern-Badges. Animation (reduced-motion-sicher via `useReducedMotion`): Schritte blenden gestaffelt beim Scrollen ein, Linie baut sich progressiv auf. Überschrift „So entsteht dein Projekt." + Unterzeile via `SectionHeading`, Content im Data-Layer (`src/data/process.ts`). Dark + Light, responsive, Akzent nur über `--accent`. `npm run build` läuft. |
| 02.06.2026 | **Session 3: „Über mich"-Sektion.** Vierte Sektion auf `/` (`id="ueber-mich"`, Ziel für den Navbar-Anker): zweispaltiges Layout – Foto-Platzhalter links, Text rechts; auf Mobil gestapelt mit Foto oben. `SectionHeading` (links ausgerichtet) für Eyebrow „Über mich" + Überschrift „Die Person hinter dem Code.", darunter 3 Absätze. Porträt-Platzhalter als gerahmte Hülle (Rand + Rundung) im Hochformat (4∶5), `bg-muted` (mode-adaptiv), Initialen „LL" + Caption „Porträt folgt" – bereit, das echte Bild in Phase 3 als `object-cover`-`<img>` einzusetzen. Content im Data-Layer (`src/data/about.ts`). Dark + Light, responsive, Akzent nur über `--accent`, Fade-up + Stagger beim Scrollen. `npm run build` läuft. |
| 02.06.2026 | **Session 2: Leistungen-Sektion.** Dritte Sektion auf `/`: Überschrift „Das baue ich für dich." + Unterzeile, 4 Karten im 2×2-Grid (mobil 1 Spalte), gleicher Spacing-Rhythmus wie die Problem-Sektion. `Card`- und `SectionHeading`-Muster wiederverwendet (Hover nicht neu gebaut). Inhalte: Webseiten · Web-Apps & Tools · Redesign & Modernisierung · KI-Integration. **Karte 4 (KI-Integration) als Highlight:** `Card` um eine dezente `highlight`-Prop erweitert – Akzent dauerhaft aktiv (Akzent-Rand + leiser Glow, kein Badge), Hover bleibt obendrauf. Je Karte ein dezentes Icon (Stil wie Problem-Sektion), Content im Data-Layer (`src/data/services.ts`). Dark + Light, responsive, Akzent nur über `--accent`. `npm run build` läuft. |
| 02.06.2026 | **Session 1: Phase 1 abgeschlossen + Start Phase 2.** **Phase 1 (Infrastruktur):** Repo `lngleon/leons-webseite` (SSH-Alias `github-lngleon`, Branch `main`), Vercel-Auto-Deploy, Formspree-Endpoint in `.env.local`, lokale Umgebung, CLAUDE.md + docs/ angelegt. **Phase 2 gestartet – drei Sektionen + Karten-Muster:** (1) **Grundgerüst:** Vite + React + TS + Tailwind v4 + Framer Motion + react-router-dom, Ordnerstruktur (components/pages/sections/hooks/lib/data/types), zentrales CSS-Variablen-Theming (Dark default + Light per Toggle, `--accent` als einzelner Platzhalter), Navbar (Logo „LL", Anker-Nav, Toggle, CTA, Mobil-Menü) + Footer (Impressum/Datenschutz/Instagram), Routen `/` `/impressum` `/datenschutz` + 404. (2) **Hero:** volle Höhe, Headline „Veränderungen, die spürbar werden." + Unterzeile, 4 beim Sichtbarwerden hochzählende Zähler (2 · 3 · 100 % · 1), CTA → `#kontakt`, reduced-motion-sichere `Counter`-Komponente. (3) **Problem-Sektion:** Überschrift „Kommt dir das bekannt vor?" + 4 Schmerzpunkt-Karten, `SectionHeading`-Komponente, Fade-up beim Scrollen. (4) **Wiederverwendbares `Card`-Hover-Muster** (`src/components/Card.tsx`): dezentes Anheben + Akzent-Rand + weicher Glow, smooth (~200 ms), touch-sicher (`@media (hover: hover)`) und `motion-safe`. Alles Dark + Light, responsive, Akzent nur über `--accent`; `npm run build` läuft. |
| 02.06.2026 | **Session 0: Discovery.** Komplettes Konzept erarbeitet: Positionierung (premium/nahbar, keine Preise, Ziel = Kontakt), Single-Page mit 7 Sektionen, Design (Dark+Light mit Toggle, edel-zurückhaltend, Akzentfarbe offen), Tech-Stack (React/TS/Tailwind/Vite/Framer Motion, Vercel, Formspree, kein Backend), 2 Projekt-Showcases (Blumen Lang, Naillery), Claim „Veränderungen, die spürbar werden.", 4 Hero-Zähler, Prozess-Schritte, Problem-Schmerzpunkte, Kontaktdaten, rechtliche Unterseiten. Systemprompt + Projektdateien erstellt. |

---

## Claude Code Prompt-Protokoll

| # | Beschreibung | Status |
|---|-------------|--------|
| 1 | Projekt-Grundgerüst (Vite/React/TS/Tailwind v4/Framer Motion/react-router-dom, Ordnerstruktur, Theme-Toggle, Navbar, Footer, Routen) | ✅ |
| 2 | Hero-Sektion (volle Höhe, Headline + Unterzeile, 4 hochzählende Zähler, CTA → #kontakt) | ✅ |
| 3 | Problem-Sektion (Überschrift + 4 Schmerzpunkt-Karten, Fade-up beim Scrollen) | ✅ |
| 4 | Feinschliff: wiederverwendbares `Card`-Hover-Muster (Anheben + Akzent-Rand + Glow, touch-/motion-safe) | ✅ |
| 5 | Leistungen-Sektion (4 Karten 2×2, Card/SectionHeading wiederverwendet, KI-Karte als dauerhaftes Akzent-Highlight) | ✅ |
| 6 | „Über mich"-Sektion (2-spaltig, Porträt-Platzhalter Hochformat links / Text rechts, mobil gestapelt) | ✅ |
| 7 | „Prozess"-Sektion (4 nummerierte Schritte als Abfolge, Verbindungslinie horizontal/vertikal, progressiver Aufbau) | ✅ |
| 8 | „Kontakt"-Sektion (Formspree-Formular mit State-Machine/Validierung + 3 Direkt-Buttons, adversariell a11y-geprüft) | ✅ |
| 9 | Hero-Umbau (generativer Violett-Hintergrund + Terminal das Build-Output tippt, 4 Tabs, reduced-motion-sicher) | ✅ |
| 10 | Leistungen-Upgrade: 4 lebende Schaubilder (Browser-Build-up, App-Reaktion, Redesign-Wisch, KI-Chat-Stream), einmal beim Scrollen + Hover, reduced-motion-sicher | ✅ |
| 11 | Dark-only-Umbau + Violett-Gradient-Akzent (Toggle/Light-Code raus, `--accent-gradient`/`-strong`, Showcase-Text-Clip + CTA-Füllung, Kontrast verifiziert) | ✅ |
| 12 | Code-Tag-Politur: dekorative Inline-Code-Tags (Mono, flacher Akzent) auf echte Begriffe im Fließtext (`Web-Apps`, `Buchungssystem`/`Dashboards`, `Code`), sparsam | ✅ |
| 13 | AuroraText: fließendes Hero-Akzentwort „spürbar" (Magic-UI-Stil, `<AuroraText>`), reine CSS-@keyframes (`background-position` auf verbreitertem Gradient), nur Violett (`--accent-gradient` verbatim), solider Fallback + reduced-motion = statisch, SSR-/hydration-sicher, NUR am Hero-Wort | ✅ |
| 14 | SSG/Prerendering: alle 3 Routen beim Build als statisches HTML (`renderToString` + `StaticRouter`, `scripts/prerender.mjs`), `hydrateRoot`/`createRoot`-Split in `main.tsx`, Hydration-sicher via neuem `useReducedMotionSafe`-Hook (Hero/Terminal/ServiceDiagram/Prozess); H1 + Sektions-Text ohne JS im Quelltext, reduced-motion/Reveal bleiben. `npm run build` grün, H1 im `dist`-HTML verifiziert | ✅ |
| 15 | AuroraText-Clip-Bugfix: Hero-Wort „spürbar" war live unsichtbar (Gradient nicht auf die Glyphen geclippt). Ursache: direkt animierte `background-position` auf `background-clip:text` (Chromium droppt die Clip-Maske); KEIN Lightning-CSS-/Prefix-/Deploy-Problem (empirisch ausgeschlossen). Fix: Flow über registrierte `@property --aurora-pos` statt `background-position` direkt + `display:inline-block`, Clip-Technik wie `.accent-gradient-text`. `npm run build` grün, `@property`+Clip im gebauten CSS verifiziert | ✅ |
| 16 | Per-Route-`<head>`: Title + Description + Basis-OpenGraph (`og:title`/`og:description`/`og:type`) je Route via Route→Meta-Map in `scripts/prerender.mjs` (dependency-frei, kein react-helmet, kein Framework-Mode); geteilter Default-Head aus dem Template entfernt, je genau 1 Title/Desc pro Route, im statischen `<head>` aller 3 dist-Dateien verifiziert. `og:image`/`og:url` ausgelassen (kein Logo/Domain). `npm run build` grün | ✅ |
| 17 | Stille Route `/möglichkeiten` („Was möglich ist", NICHT in der Navbar) + 4 UI-Komponenten in `src/components/ui/` (`Marquee`, `InteractiveHoverButton`, `CoolMode`, `Tilt`). Prerendert mit eigenem Title/Meta; Bento mit echten `ServiceDiagram`-Schaubildern + statischer „Diese Seite selbst"-Zelle (Perf-Platzhalter); Tilt subtil; Marquee = echter Tech-Stack. SSR-/reduced-motion-safe, CoolMode leak-frei. `npm run build` grün | ✅ |
| 18 | Statement-Sektion (Scroll-Reveal) direkt vor „Kontakt": zwei nacheinander einblendende Zeilen („Nicht noch eine Seite von der Stange." / „Sondern deine."), „deine" im Akzent-Gradient (`.accent-gradient-text`, nicht hardcoden). Neue wiederverwendbare `TextAnimation`-Komponente (Vorlagen-API nachgebaut, blur+fade+y) – SSR-/reduced-motion-sicher (`useReducedMotionSafe`, `useInView`+`animate`, Text im prerenderten HTML), kein lowercase/capitalize-Transform. Nicht above-the-fold. `npm run build` grün; beide Zeilen + `deine` im `dist/index.html` verifiziert | ✅ |
| 19 | Live-Visual auf `/möglichkeiten`: interaktiver Dot-Globe (`Earth`, WebGL via `cobe`) + aufsteigende Funken (`Sparkles`, hand-gebautes 2D-Canvas) als Können-Demo, eigener Block (NICHT Startseite). Nur Violett aus Tokens (kein blaues Hex, keine englische Copy); deutsch/„Du", kein Reichweiten-Claim. SSR-sicher (cobe dynamisch importiert, stabile Canvas-Hülle), reduced-motion (Globus statisch, Sparkles aus), leak-frei (rAF/Observer/Context abgebaut). Neue Dependency `cobe` (committet). Adversariell reviewt (6 echte Findings gefixt: reduced-Map-Redraw, ResizeObserver, WEBGL_lose_context, IntersectionObserver-Pause). `npm run build` grün; `/möglichkeiten` prerendert verifiziert | ✅ |
| 20 | Primär-CTA-Violett-Glow (zentral in `.cta-gradient`, Card-Glow-Sprache via `color-mix(--accent)`, Ruhe/Hover/`:active`, kein Puls, touch-sicher, Fokusring-komponierend, kein Button-Markup angefasst) + schmaler Scroll-Fortschrittsbalken (`ScrollProgress`, `useScroll`→`scaleX`, fix `top-0 z-[60]` über Navbar, `var(--accent-gradient)`). SSR-sicher (Balken startet `scaleX(0)`), reduced-motion (Balken direkt gebunden, kein Spring), nur Tokens/kein Hex. Adversariell reviewt (4 bestätigt: `skipInitialAnimation` gefixt; color-mix-Fallback + dead `shadow-sm` bewusst belassen). `npm run build` grün; Balken + token-Glow im `dist` verifiziert | ✅ |
| 21 | Fließende CTA-Füllung (alle 5 `.cta-gradient`): registrierte `@property --cta-pos` + `background-size:200%` + `@keyframes cta-flow` (AuroraText-Weg, nie `background-position` direkt), nur `--accent-gradient-strong` (weiße Schrift ≥4.5:1 über den ganzen Fluss), Glow unverändert. Selektiver Lift nur auf großen Content-CTAs via `main .cta-gradient` (Hover `translateY(-2px)`, `:active` zurück; Navbar ohne Lift). reduced-motion: statisch + kein Lift. Rein CSS/SSR-sicher, nur `index.css`, kein Button-Markup. Adversariell reviewt (4 bestätigt: 2 Kommentar-Härtungen; mobile-CTA-Fokusring als separater a11y-Follow-up). `npm run build` grün, im `dist`-CSS verifiziert | ✅ |
| 22 | CTA-Hover-Lift spürbarer: Hover `translateY(-4px)` (statt 2px) + zusätzlicher weicher Akzent-Lift-Schatten als additiver box-shadow-Layer `var(--cta-lift-shadow)` (`color-mix(--accent 30%)` + Token-Fallback, im Ruhezustand `0 0 #0000`); `:active` drückt zurück + Schatten weg. Nur `main .cta-gradient` (Navbar ruhig), Glow/Füllung/weiße Schrift unverändert, animiert (kein Snap), reduced-motion → kein Lift. Nur Akzent-Tokens (kein graues Hex), nur `index.css`. Adversariell reviewt (3 bestätigt, alle „verified correct"/keine Änderung). `npm run build` grün, im `dist`-CSS verifiziert | ✅ |
| 23 | a11y: Fokusring am mobilen Navbar-CTA ergänzt (war als einziger der 5 Primär-CTAs ohne sichtbaren Tastatur-Fokus). Dieselben `focus-visible:ring`-Klassen wie die anderen CTAs (Tokens `--ring`/`--background`, kein Hex, Dark-only, SSR-safe); WCAG 2.4.7. Nur dieser Button. `npm run build` grün | ✅ |
| 24 | Projekte-Sektion (`id="projekte"`, in `Home` zwischen Prozess und Statement): zwei große Showcase-Karten (Blumen Lang / Naillery), mobil gestapelt, KEINE Tabs/Detail-Ansicht (geparkt). Pro Karte: statisches Vorschaubild (voll-bleed oben, `object-cover`, `aspect-[21/10]`, `width`/`height`) + Name + Typzeile + externer Link „zur Website" (`target=_blank` `rel=noopener noreferrer`, distinkte `aria-label`). `Card`/`SectionHeading` wiederverwendet, Live-URLs als änderbare Felder im Data-Layer (`src/data/projects.ts`). Dark-only, nur Tokens, keine erfundenen Zahlen, SSR-/reduced-motion-sicher (`useReducedMotionSafe`-Reveal). Adversariell reviewt (1 bestätigt: `width`/`height` ergänzt). `npm run build` grün; im `dist/index.html` verifiziert | ✅ |
| 25 | Hero-Terminal anonymisiert: alle echten Konto-/Projekt-/Deploy-Identifier durch Dummies ersetzt (nur Textdaten in `src/data/hero.ts`, keine neue Komponente). **install:** `npm create vite@latest dein-projekt …` (Repo-Name `leons-webseite` → `dein-projekt`). **deploy:** `→ GitHub: dein-projekt (main)` (statt `lngleon/leons-webseite`) + `✓ Production: https://deine-seite.de` (statt `leons-webseite.vercel.app`). **build/whoami:** keine echten Identifier (build = generische `dist/`-Pfade; whoami = bereits generische Marken-Copy „Leon Lang" + Rolle, kein echter Handle/E-Mail) → unverändert; Vorname/Marke „Leon Lang" bleibt bewusst. Look/Tabs/Tipp-Animation, Zähler, Tokens, Dark-only, SSR, reduced-motion unangetastet. Daten-Kommentar entschärft (behauptet keine echten Infra-Identifier mehr). `npm run build` grün; `dist` enthält keinen Alt-Identifier mehr (Terminal tippt client-seitig). Browser-Check: lokal `npm run preview` | ✅ |
| 26 | Audit-Feinschliff (6 kleine Pre-Launch-Findings umgesetzt). **(1)** `/möglichkeiten` bekommt ein echtes `<h1>`: `SectionHeading` um optionale `as?: 'h1'\|'h2'`-Prop erweitert (Default `h2`, dynamischer `<Heading>`-Tag) – nur die Showcase-Seite nutzt `as="h1"`, andere Routen unverändert (je genau 1 `<h1>`). **Review-Folge-Fix:** das bloße Höherstufen erzeugte einen `h1→h3`-Level-Skip (keine `h2` auf der Seite) → die 4 Bento-Titel + 4 Abschnitts-Überschriften auf `h2`, die Tilt-Demo-Überschrift auf `h3` gezogen (rein semantisch – alle Größen kommen aus Klassen, Tailwind-Preflight neutralisiert Default-Heading-Größen → 0 visuelle Änderung); Outline jetzt `h1→h2→h3`. **(2)** Header-`<nav>` `aria-label="Hauptnavigation"`, Footer-`<nav>` `aria-label="Rechtliches"`. **(3)** `Counter` von framers `useReducedMotion` auf `useReducedMotionSafe` (Projektregel; Wert nur im `useEffect` gelesen → Verhalten identisch/hydration-safe). **(4)** Fokus vereinheitlicht: Desktop-/Mobil-Nav-Links, Logo, Footer-Links (Impressum/Datenschutz/Instagram) + die drei „Zurück zur Startseite"-Links tragen denselben `focus-visible:ring`-Satz wie die CTAs (+ `rounded-sm`), nur Tokens/kein Hex. **(5)** Totes `site.description` entfernt. **(6)** `rel` externer Links → `noopener noreferrer` (Footer/Kontakt; Projekte-Links hatten es schon). Non-Goals bewusst nicht angefasst (og:image/Favicon, No-JS-Zähler, Vercel-404, ServiceDiagram-„Vorher"-Hex, Earth/Sparkles-Token-Fallbacks). Adversariell reviewt (Multi-Agent-Workflow, 4 Lenses → 2 Findings: 1 bestätigt [`h1→h3`-Skip] gefixt, 1 verworfen [`inline-block`-No-op an NotFound]). `npm run build` grün; im `dist` verifiziert: `/möglichkeiten`-Outline `h1→h2→h3` ohne Skip, alle 4 Routen je 1 `<h1>`, beide `aria-label`, kein blankes `rel="noreferrer"` mehr. Browser-Check: lokal `npm run preview` | ✅ |
| 27 | Favicon-Satz eingebunden: 5 Dateien in `public/` (`favicon.svg` ersetzt Platzhalter + neu `favicon.ico`, `apple-touch-icon.png`, `favicon-32.png`, `favicon-16.png`) im `<head>` ALLER Routen verlinkt (SVG primär, `.ico` Fallback via `sizes="any"`, PNG-Sizes 16/32, `apple-touch-icon`) + `<meta name="theme-color" content="#0a0a0a">`. Links im **Template `index.html`** gesetzt (nicht in `prerender.mjs`) → landen in Dev UND allen 4 prerenderten dist-Heads, nichts doppelt (Prerender strippt nur Title + Description-Meta, Rest des Heads bleibt). **Stolperstein/Erkenntnis:** der erklärende HTML-Kommentar enthielt zunächst den Literal-Text `<title>` → die Title-Strip-Regex `/<title>[\s\S]*?<\/title>/` in `prerender.mjs` fraß vom Kommentar-`<title>` bis zum echten `</title>` und löschte die Favicon-`<link>`s aus dem Build → **Kommentare im Template-Head müssen tag-literal-frei sein** (kein `<title>`/`<meta name="description">` als Fließtext). `og:image`/`og:url` weiter NICHT gesetzt (wartet auf finale Domain). Navbar-Logo („LL"-Text) unangetastet (Non-Goal). `npm run build` grün; Icon-Links + `theme-color` in allen 4 dist-Heads verifiziert (je 7 Head-Zeilen), Assets in `dist/` kopiert. Browser-Check: Tab-Icon prüfen (harter Reload wegen Favicon-Cache). | ✅ |
| 28 | Vercel Web Analytics eingebunden: `@vercel/analytics` (v2, **war entgegen der Task-Annahme NICHT installiert** → nachinstalliert; keine Runtime-Deps) – `<Analytics />` aus `@vercel/analytics/react` EINMAL zentral im `Layout` gemountet (wrappt alle Routen). **SSR-/prerender-sicher (Quelle geprüft):** die Komponente rendert `null` und macht alle `window`/`document`-Zugriffe nur in `useEffect` (+ interner `isBrowser()`-Guard) → im `renderToString`/Prerender KEIN Analytics-Markup, das Insights-Script (`/_vercel/insights/script.js`) wird erst nach Mount im Browser injiziert; Server-Frame = erstes Client-Frame (beide `null`), keine Hydration-Diskrepanz. **SPA-Tracking:** `inject()` läuft mit `framework:'react'` OHNE `route`-Prop → Auto-Track aktiv → Routenwechsel (History-API/react-router) zählen automatisch als Pageviews. Non-Goals eingehalten (kein Cookie-Banner, kein Speed Insights, keine Custom Events). `npm run build` grün; verifiziert: **0** Analytics-Markup in allen prerenderten `*.html`, Injector nur im Client-JS-Bundle (`dist/assets/*.js`). Aktivierung im Vercel-Dashboard nötig; Daten erst nach Deploy + echten Besuchen. | ✅ |
| 29 | **Migration Vite → Next.js (App Router).** Gleiches Design, gleiche Inhalte, alle vier Routen. Entfernt: `index.html`, `vite.config.ts`, `src/main.tsx`, `src/entry-server.tsx`, `scripts/prerender.mjs`, `src/App.tsx`, `src/pages/`, `src/components/Layout.tsx`, `tsconfig.app.json`+`tsconfig.node.json`, `src/vite-env.d.ts`, `dist/`+`dist-ssr/`, Dependencies `vite`/`@vitejs/plugin-react`/`@tailwindcss/vite`/`react-router-dom`. Neu: `src/app/` (Root-Layout + 4 Routen + `not-found.tsx`), `src/data/meta.ts` (Nachfolger der `META`-Map), `src/data/site-url.ts`, `src/app/sitemap.ts`, `src/app/robots.ts`, `next.config.ts`, `postcss.config.mjs`. `Link` → `next/link`, die 3 statischen Bilder → `next/image`, Tailwind v4 auf `@tailwindcss/postcss`, `src/index.css` → `src/app/globals.css`, `VITE_FORMSPREE_ENDPOINT` → `NEXT_PUBLIC_FORMSPREE_ENDPOINT`. Hydration-Regel unangetastet. **Bug unterwegs:** `/möglichkeiten` war build-grün + `○ Static` + HTML vorhanden und lieferte trotzdem **404** (Next nimmt statische Ordnernamen 1:1 in die Route-Regex, Browser senden percent-encodet) → Ordner `src/app/m%C3%B6glichkeiten/` + zweiter Re-Export-Ordner als Sicherheitsnetz für Vercels Routing-Layer. Adversariell reviewt (3 Agenten: 0 Hydration-Bugs, 0 reduced-motion-Regressionen, Prerender-Inhalt textknoten-identisch zum alten Build; 4 bestätigte Findings gefixt, 2 vorbestehende bewusst als Follow-up notiert). `npm run build` grün; alle Routen + Assets + `sitemap.xml`/`robots.txt` per HTTP-Status gegen `next start` verifiziert | ✅ |
| 30 | Stille Route auf ASCII umgestellt: `/möglichkeiten` → `/moeglichkeiten`. Die zwei Ordner aus #29 (percent-encodet + Umlaut-Re-Export) durch EINEN `src/app/moeglichkeiten/` ersetzt; `routes` in `src/data/site-url.ts` + Sitemap angepasst; Kommentar-Referenzen in `src/` nachgezogen. **Kein Redirect** von der alten URL (nie verlinkt) → alte URL jetzt 404, verifiziert. Title/Description unverändert; Route bleibt still (nicht in `navItems`/Footer). Außerdem Hero-Terminal-Copy auf den echten Next-Stack (`src/data/hero.ts`, nur Textdaten): `create-next-app` statt `create vite`, Next-Build-Ausgabe statt `vite v8`/`dist/assets`; Anonymisierung gilt weiter (Dummies, generische Routen – stille Route taucht NICHT auf), keine erfundenen Messwerte mehr, Zeilenzahl je Tab identisch → keine Layout-Änderung. `npm run build` grün; Routen + alte URL + Sitemap + Client-Bundle verifiziert | ✅ |
| 31 | reduced-motion-Lücke der Entrance-Animationen per CSS-Gate geschlossen. Neue Klasse `.entrance-anim` in `globals.css` (nur innerhalb `@media (prefers-reduced-motion: reduce)`) erzwingt via `!important` den Endzustand über Framers Inline-Styles – ab dem ersten Paint, kein Flash. Gesetzt an Navbar-Header, die 3 Hero-`item`-Kinder + Terminal-Wrapper (die gemeldete Mount-Dispatch-Lücke) sowie an Problem/Leistungen/Über-mich/Kontakt/Reveal-auf-/moeglichkeiten (dort fehlte JEDES reduce-Gate). Nicht gegatet: `ServiceDiagram` (Endzustände `translateX`/`width`/`clipPath` → Gate wäre falsch, `key`-Remount löst es dort) und Prozess/Projekte/Statement (bereits korrekt gegatet, verifiziert). Hook und Hydration-Regel unangetastet; für normale Nutzer nachweislich keine Änderung (Klasse nur in der Media Query, keine Timing-/Varianten-Diffs, keine Transition-Konflikte). `npm run build` + `tsc` grün | ✅ |
| 32 | Demo-Café-Seite `/demo/cafe` als stille Route (noindex, nicht in Sitemap, nicht verlinkt) in eigener Layout-Gruppe `(demo)` – ohne Navbar/Footer/ScrollProgress/Analytics. Dafür Hülle aus dem Root-Layout nach `SiteChrome` + `(site)/layout.tsx` verschoben (Hauptseite markup-identisch, gzip +157–182 B/Seite). Eigene helle Tokens auf `.demo-scope` in `(demo)/demo.css` (eigener CSS-Chunk, `:root` unangetastet). Alle Inhalte in `src/data/demo/cafe-klee.ts`, Komponenten rein datengetrieben. Hero, Karte mit Allergenen, Zeiten, Adresse + Routen-Link, Tap-to-Call, Reservierungs-Mail, Impressum-Platzhalter, Laufband; schema.org Restaurant + Menu. Keine Client-Komponente, kein JS nötig. Im Browser bei 320/390/1280 px geprüft: Überlauf durch „ÖFFNUNGSZEITEN" gefunden + gefixt, Bildreihe auf 2 Spalten, danach 0 Überlauf, CLS 0, 0 Fremd-Ressourcen. `npm run build` + `tsc` grün | ✅ |
| 33 | **Doku-Abschluss der Session.** Neue visuelle Referenzanalyse `docs/REFERENZ-DR-SMUSY.md` (dr-smusy.com bei 1440 px und echten 390 px per `getComputedStyle` gemessen: Typo-Hierarchie, Farbsystem, Spacing-Skala, Sektionsabfolge, Karten-Anatomie für Adresse/Öffnungszeiten). Dazu ein Multi-Agent-Doku-Audit (5 Prüfer / 3 Entwürfe / 3 adversarielle Gegenprüfungen, 40 Findings) auf PROJEKT-STAND, CURRENT-SCHEMA und TODO angewandt: Umlaut-Routennamen, tote `src/index.css`-Pfade, `site.ts`↔`site-url.ts`-Widerspruch, Analytics-/ScrollProgress-Mount in `SiteChrome`, Demo-Fotos in der Asset-Tabelle, veraltete Phasen-/Prioritäten-Aussagen; die beiden Browser-Check-Tasks zusammengelegt. Neue Erkenntnis: eine Referenz überträgt Form, nicht Informationsumfang – Preise und Allergene bleiben in der Gastro-Demo. `npm run build` grün (Next 16.3.2, 8 Routen `○ Static`) | ✅ |
| 34 | **Mehrseitigkeit:** `/demo/cafe` von einer Single-Page auf drei Routen aufgeteilt (Start / `karte` / `kontakt`, Segmente rein ASCII). `GastroDemo.tsx` entfiel zugunsten von `DemoShell`, das jede Seite um ihren Inhalt legt – bewusst kein `layout.tsx`, weil ein Layout den aktiven Pfad nicht kennt und `usePathname()` ein Client-Hook waere. Seitenwechsel als `<a href>` statt `next/link` (in Next 16 eine Client-Komponente), Navigation als horizontal scrollbare Pille statt Burger, Kategorie-Leiste auf `/karte` mit reinen `#`-Ankern. schema.org aufgeteilt: `Restaurant` auf Start, `Menu` auf `/karte`. 0 px Ueberlauf bei 320/390/1280 px, ohne JS bedienbar | ✅ |
| 35 | **Vierte Seite + Rechtsseiten:** `/demo/cafe/ueber-uns` (Lead + drei Bloecke im Bild-Text-Wechsel, drei neue Fotos) sowie `/demo/cafe/impressum` und `/demo/cafe/datenschutz`. Pillenbreite bei 320 px vorher gemessen (drei Eintraege 287 px bei 288 px Platz, vier 401 px) → `min-width: 0` am `<nav>` gegen Seitenueberlauf plus reiner CSS-Scroll-Hinweis (`background-attachment: local/scroll`), der sich am Ende selbst ausblendet. Footer schlank: volles Impressum raus, zwei Rechtslinks rein. Datenschutz beschreibt den tatsaechlichen Zustand (keine Cookies, kein Tracking, keine Fremd-Requests, Hosting) statt Standardbausteine; Muster-Hinweis sichtbar oben, keine Rechtsberatung. Zwei inhaltliche Widersprueche in `cafe-klee.ts` gemeldet statt still korrigiert | ✅ |
| 36 | **Die drei gemeldeten Widersprueche in `cafe-klee.ts` aufgeloest** (Entscheidungen vom User). **(1) Roestung:** Betrieb roestet selbst – Block „Was im Becher landet" beginnt jetzt „Geroestet wird eine Strasse weiter, …" statt „Der Kaffee kommt von einer kleinen Roesterei, …"; Rest des Blocks unveraendert. **(2) Lage:** `location.note` von „Zweiter Hinterhof" auf „Ebenerdig direkt an der Strasse, unter der hellen Markise – Kinderwagen und Rollstuhl kommen ohne Stufe herein. Bei gutem Wetter stehen ein paar Stuehle draussen." – passt zum Aussenansicht-Foto (Ladenfront am Gehweg, Markise, zwei Stuehle davor) und nennt Landmarke, Stufenfreiheit und Aussenplaetze. **(3) Halber Preis:** derselbe Satz zusaetzlich als `note` der Kategorie `backstube` – kleinste saubere Stelle, weil `MenuCategory.note` schon existiert, `DemoMenu` es schon rendert und `buildMenuSchema` es schon als `MenuSection.description` ausgibt; **null Zeilen Komponenten-/Typ-Aenderung**, keine doppelte Prosa. Doku: Session 19+20 zu EINEM Session-19-Eintrag zusammengefuehrt (war eine Chat-Session), neue Erkenntnis „`overflow-x` greift auf einem Flex-Item nicht ohne `min-width: 0`". `npm run build` gruen; Ueberlauf-Check im Browser nur auf den beiden Seiten mit neuem Text (plus `ueber-uns` als Gegenprobe): 0 px bei 320/390/1280 px, 0 Konsolenmeldungen | ✅ |
| 37 | **Eigenes Favicon fuer `/demo/*`.** Neu `src/app/(demo)/icon.svg` – handgeschriebenes SVG, „K" aus drei Stroke-Pfaden (PFADE statt `<text>`, damit keine Systemschrift noetig ist) in `#b4441e` auf `#f6f1e7` mit Hairline `#dbcfba`, alle drei Werte aus den Tokens in `(demo)/demo.css`. **Datei allein reichte nicht:** Next uebernimmt dateibasierte Icons nur, wenn `resolvedMetadata.icons` falsy ist – das `metadata.icons` des Root-Layouts gewann, `icon.svg` wurde als Route gebaut, stand aber in keinem `<head>`. Fix: zusaetzlich `icons: null` in `(demo)/layout.tsx`. Im gebauten HTML gegengeprueft: sechs Demo-Seiten genau ein Icon-Tag, kein `apple-touch-icon`; alle (site)-Routen plus `_not-found` unveraendert fuenf Tags. 16-px-Lesbarkeit im Browser gemessen (Strich 2,5 px, Glyphe 9 px). Doku: TODO abgehakt, CURRENT-SCHEMA-Favicon-Zeile getrennt nach (site)/(demo), zwei neue Regeln (Datei-Icons vs. `metadata.icons`; Barrierefreiheits-Angaben sind Zusagen). Adversarieller Prueflauf: 18 Findings, 0 bestaetigt | ✅ |
| 38 | **Browser-Check-Sammelrunde auf der Hauptseite + zwei Motion-Fixes.** **(1)** `scroll-behavior: smooth` in `globals.css` fuer `prefers-reduced-motion: reduce` auf `auto` zurueckgenommen (Ruecknahme im selben `@layer base`, nicht als `no-preference`-Variante) – gemessen: computed `scrollBehavior` springt smooth→auto. **(2)** Mobil-Menue der Navbar reduced-motion-fest ueber `useReducedMotionSafe` statt `.entrance-anim` (Interaktions- statt Mount-Animation; Endzustand ist `height: auto`, nicht „kein Transform“) – nur die Dauer faellt auf 0, gemessen an den React-Props: `{duration:0.25,ease:'easeOut'}` → `{duration:0}`, Endzustand und `AnimatePresence`-Unmount unveraendert. **(3) Sicht-Checks** Dark UND OS-Light, 320/390/1280 px: Fokusringe ueberall `#a78bfa` aus `--ring` (2 px Ring, 2 px Offset in `--background`), kein Ring nach Mausklick; Hero-CTA-Lift `translateY(-4px)` mit violettem Lift-Schatten, Navbar-CTA bleibt `transform: none`; Projekte-Karten `translate: 0 -6px`, Akzent-Rand/Glow, Links `_blank` + `noopener noreferrer`; Favicon auf allen 4 Routen je 5 Tags, alle Dateien 200; keine Hydration-Warnung; dark-only haelt bei OS-light; 0 px Ueberlauf. Zwei Beobachtungen ohne Fix: CLS 0,0021 aus der Hero-Zaehlerzeile, 36 px Tap-Flaechen im Mobil-Menue | ✅ |
| 39 | **Lokale Display-Schrift fuer `/demo/*` + apple-icon.** Aus drei Kandidaten **Fraunces 72pt SemiBold** gewaehlt: Lizenz (alle drei SIL OFL 1.1, Texte bis auf die Copyright-Zeile identisch, Einbettung erlaubt) und Zeichensatz (Ä Ö Ü ä ö ü ß ẞ € vollstaendig) schieden niemanden aus – entschieden hat das **Gewicht**, weil `.demo-display` 600 setzt und nur Fraunces einen echten 600er-Schnitt hat (Young Serif und Instrument Serif nur 400 → Faux Bold). Statischer Schnitt statt Variable-Datei: 18,2 KB WOFF2 nach Latin-Subset statt 122,2 KB fuer vier ungenutzte Achsen. Eingebunden per `next/font/local` (`(demo)/font.ts`), `--font-demo-display` auf dem bestehenden `.demo-scope`-Element in `DemoShell` (kein neuer DOM-Knoten). **0 Fremd-Requests gehalten** (14 Requests, alle eigene Herkunft). **`8.2vw` → `9vw`** nachgezogen: Fraunces braucht 9,198 px je px Schriftgroesse statt 10,34 – bei 320 px am String „OEFFNUNGSZEITEN" neu gemessen (264,9 px bei 280 px Container, Obergrenze waere 9.4vw). **CLS 0 auf allen sechs Demo-Seiten** bei 320/390/1280 px dank `adjustFontFallback: 'Times New Roman'` (`size-adjust: 108.79%`). Dazu `apple-icon.png` 180×180 aus dem vorhandenen K-SVG mit `sharp --no-save` gerastert (opak, ohne Eckenrundung – iOS maskiert selbst); package.json und Lockfile per md5 als unveraendert bestaetigt | ✅ |
| 40 | **Vier kleine Restposten.** **(1) OFL-Klausel 3 geprueft:** Fraunces hat **keinen Reserved Font Name** – die Copyright-Zeile traegt kein `with Reserved Font Name`, die Zeichenkette kommt null Mal vor. Unsere subgesetzte Datei ist zwar eine „Modified Version", Klausel 3 greift mangels reservierter Namen aber nicht → keine Umbenennung. Beleg im Kopf von `(demo)/font.ts`. **(2) CLS auf `/` geschlossen: 0,00211 → 0,000009.** Root Cause war die ZIFFERNANZAHL (0 → 100), nicht die Ziffernbreite; `tabular-nums` allein aenderte nichts (0,00036 gegengemessen). Fix: `inline-block` mit `min-width: <Stellen>ch` plus Tabellenziffern, damit `1ch` fuer jede Ziffer gilt – reservierte 59,959 px gegen tatsaechliche 59,95 px, null Shifts aus den Zaehlern. Rest sind zwei Terminal-Cursor-Shifts (0,000009). **(3) Tap-Flaechen im Mobil-Menue 36 → 44 px:** sechs Eintraege und Menue-CTA `py-2` → `py-3`, Burger `h-9 w-9` → `h-11 w-11`; bei 320 und 390 px nachgemessen, Leiste bleibt 64 px, 0 px Ueberlauf. **(4) Doku:** drei neue Regeln (OFL-Datei begleitet jede Kopie; Animationsdauern nie ueber `requestAnimationFrame` messen, Pruef-Browser lief bei ~1 fps; bei Demo 2 statisch gegen variabel neu abwaegen). Non-Goals gehalten: Schrift-Auswahl, `9vw` und Icons unveraendert | ✅ |
| 41 | **Zweite Demo: A-la-carte-Restaurant „Restaurant Glut" unter `/demo/restaurant` (erste Stufe).** Sechs Seiten analog zum Cafe, JS-frei, 0 Fremd-Requests, `noindex`, nicht in der Sitemap, erfundener Betrieb (reservierter Nummernblock `030 23125 xxx`, `.example`-Domain). Kein Buchungsflow, keine Bilder (sieben Platzhalter mit den Seitenverhaeltnissen der spaeteren Fotos). **Kern war der Typ-Test:** `types.ts` traegt jetzt Cafe UND Restaurant – `cafe-klee.ts` **Zeichen fuer Zeichen unveraendert**, keine Komponente mit Betriebs-Zweig. Drei Entscheidungen: oberste Ebene `menu.categories` bedeutet „Gliederungspunkt der Kartenseite" (Cafe: Kategorie, Restaurant: ganze Karte) – Feldname blieb, damit die Cafe-Datei nicht angefasst wird; neuer Typ `MenuSection` fuer die Gaenge, Reihenfolge allein im Array (kein `position`, auch nicht im JSON-LD); neuer Typ `MenuBundle` mit `courses: string[]` fuer Menues zum Festpreis. `MenuCategory` ist eine **exklusive Union** (`items` XOR `sections`, `bundles` nur im `sections`-Zweig) – `items` wurde dafuer optional, die Union verbietet aber Eintraege ohne Inhalt und Buendel ohne Gaenge. **Angefasst (verallgemeinert, nicht verzweigt):** `DemoMenu`, `DemoMenuTeaser`, `buildMenuSchema`. **Neu:** `DemoMenuBundles.tsx`, `menu.ts` (`leadItems`, `bundleCourses`). **Unangetastet:** `DemoCategoryRail` (zaehlt die drei Karten automatisch auf), `DemoShell`, `DemoNav`, `DemoHours`, `DemoLocation`, `DemoContact`, `DemoAbout`, `DemoHero`, `DemoGallery`, `DemoPhoto`, `DemoLegal`, `DemoLegalPage`, `routes.ts`, `demo.css`. Getrennte Servicezeiten ohne Typaenderung (zwei `hours.entries` mit verschiedenen Labels; neue Regel: Label eindeutig, es ist der React-Key). Weinpreise Glas/Flasche bewusst NICHT modelliert (haette `MenuRow` umgebaut) – geloest ueber zwei Abschnitte mit `note`. **Verifiziert:** `npm run build` gruen (22 statische Routen), alle zwoelf Demo-Seiten HTTP 200 gegen `next start`, `sitemap.xml` ohne `/demo`, alle sechs Restaurant-Seiten `noindex, nofollow`, alle Ressourcen eigener Herkunft. **Byte-Vergleich des gebauten Cafe-HTML vor/nach:** sichtbares Markup und JSON-LD identisch, einziger Unterschied `,null,null` in der RSC-Payload von `/demo/cafe/karte` (~10 Byte). **Offen gemeldet:** Demo-Favicon zeigt auf `/demo/restaurant` noch das Cafe-„K" (Icons liegen auf Gruppen-Ebene), und die 320-px-Messung im Browser konnte nicht laufen (DevTools-Profil gesperrt) | ✅ |
| 42 | **Reservierungs-Attrappe `/demo/restaurant/reservieren` + die zwei Restposten.** Fuenf Schritte (Personenzahl, Tag, Uhrzeit, Kontaktdaten, Bestaetigung), die NICHTS tun: kein Netzwerk-Request, keine Mail, kein Speichern. Im Browser durchgeklickt und gemessen: 0 Requests, localStorage/sessionStorage leer. **Kennzeichnung vierfach auf JEDEM Schritt:** „(Vorschau)" im Tab, klebendes Band im Slot der Kategorie-Leiste (68 px, auf allen Schritten im Bild), Zaehler in der Ueberschrift, ein je Schritt ANDERER Satz darunter, ein Satz unter dem Knopf. Dazu gestrichelter Rahmen als textloses Signal. Keine Vorgangsnummer, kein Restplatzzaehler, kein „Bestaetigung geschickt"; Abschluss im Konjunktiv plus vier Punkte, was nicht passiert ist. **Verfuegbarkeiten aus `hours.entries` gerechnet** (`services[].hoursLabel` zeigt dorthin, Minuten-Arithmetik auf „HH:MM", kein `Date`) – Ruhetage ergeben sich von selbst; erfunden ist nur die Belegung, die Legende sagt es. Beispielwoche aus Wochentagen statt Datum. **JS-Ausnahme benannt und begrenzt:** `DemoBookingFlow.tsx` ist die einzige Datei im Demo-Baum mit `use client`; JS-Chunk-Saetze der zwoelf uebrigen Seiten vor/nach byte-gleich, `/reservieren` genau EIN zusaetzlicher Chunk (2,7 KB gzip, ohne framer-motion/next/link/fetch/Date/Math.random); beim CSS gilt das nicht (demo.css ist eine Datei fuer die Gruppe), Leons Hauptseite aber unberuehrt – im globalen Tailwind-Chunk keine der 494 Klassenregeln neu. Ohne JS: `<noscript><style>` blendet die Strecke aus und einen Erklaerblock mit Telefon und Mail ein (mit abgeschaltetem Scripting verifiziert). **Vorher drei unabhaengige Design-Gutachten + Synthese**, danach ein adversarieller Pruefdurchgang. Daraus u.a.: keine Auto-Advance-Radios (WCAG 3.2.2), `fieldset { min-width: 0 }`, `font-size: 1rem` gegen iOS-Zoom, Fokusring-Luecke in der Farbe des Grundes statt der Fuellung, belegte Zeiten als Nicht-Bedienelemente – und der Fund, dass der Datenschutz-Absatz von Glut durch die Strecke falsch wird (neu formuliert). **Restposten:** Favicons je Betrieb (Cafe „K" per `git mv` byte-gleich, Restaurant „G" aus einer Geometrie – SVG und 180x180-PNG per Distanzfeld ohne Bildwerkzeug, vier Varianten als 16-px-Raster verglichen); **63 Messpunkte Ueberlauf, groesster Wert 0 px** (13 Seiten x 320/390/1280 px plus alle neun Zustaende der Strecke), gemessen ueber einen eigenen headless-Chrome per CDP, weil das MCP-Profil gesperrt war. Cafe-Markup unveraendert; bekannte Einschraenkung dokumentiert: Browser-Zurueck verlaesst die Strecke | ✅ |

---

## Nächste Prioritäten

| # | Aufgabe | Aufwand |
|---|---------|--------|
| 0 | **Vercel-Settings nach der Next-Migration nachziehen** – Env-Rename auf `NEXT_PUBLIC_FORMSPREE_ENDPOINT` ✅ erledigt (User, 26.08.2026); offen: Framework-Preset auf **Next.js** prüfen, Build-Command-/Output-Override entfernen, optional `NEXT_PUBLIC_SITE_URL` setzen. Danach `/moeglichkeiten` und `/demo/cafe` live auf 200 prüfen | Klein |
| 1 | Impressum + Datenschutz: Texte einfügen | Klein |
| 2 | Logo „LL" einbinden (für dunklen Hintergrund) – Favicon-Teil ✅ erledigt 27.07.2026 | Mittel |
| ✅ | ~~Projekte-Sektion (2 Showcases + Detail-Ansicht)~~ → erledigt 09.06.2026 (zwei große Karten Blumen Lang / Naillery mit Vorschaubild + Link „zur Website", Live-URLs im Data-Layer; interaktive Detail-Ansicht bewusst geparkt) | – |
| ✅ | ~~Follow-up: CSR → Prerender/SSG für echten LCP-Gewinn (Mobil)~~ → erledigt 08.06.2026 (Session 10: `renderToString` + `StaticRouter` Prerender, 3 Routen statisch) | – |

---

## Bekannte Bugs

- ✅ **Behoben – Prozess-Animation lief beim Neuladen nicht.** Lag die Sektion beim Laden bereits im Viewport (Reload an dieser Scroll-Position), liefen weder Schritt-Stagger noch Linien-Aufbau; beim Reinscrollen liefen sie. Betraf NUR Prozess. **Ursache:** Die `whileInView`-Geste propagiert den tief verschachtelten Variant-Baum (`ol → li → Verbindungslinien`, zwei Ebenen) beim „bereits im Viewport beim Mount"-Fall nicht zuverlässig. Die flacheren Sektionen (eine Ebene) waren nicht betroffen. **Fix:** Orchestrierung über `useInView` + gesteuertes `animate={inView ? 'show' : 'hidden'}` statt `whileInView`. Reduced-motion-Verhalten unverändert.
- ✅ **Behoben (08.06.2026) – Light-Mode-Kontrast der Akzentfarbe.** Mit der Platzhalter-Akzentfarbe #c8a96a lag Akzent-als-Text auf hellem Grund bei nur ~2.25:1 und der Fokusring unter 3:1 (WCAG 1.4.3 / 1.4.11), u.a. beim „Noch eine Nachricht schreiben"-Button im Kontakt-Formular. **Fix:** finale Akzentfarbe Violett mit per-Mode-Tokens – Light `--accent` = #6d28d9 ist dunkel genug, sodass Akzent-als-Text UND Fokusring auf Weiß ~7.1:1 erreichen (≥ AA 4.5/3). Per Kontrastberechnung verifiziert; kein separates `--accent-strong` nötig. Aufgedeckt durch adversarielles Review der Kontakt-Sektion. **Update 08.06.2026:** seit der Dark-only-Umstellung ohnehin gegenstandslos (kein Light Mode mehr).

---

## Verhaltensregeln für nächsten Chat

- **Repo:** `lngleon/leons-webseite` (GitHub), Branch `main`
- **Git/SSH:** Remote `origin` = `git@github-lngleon:lngleon/leons-webseite.git`. Das Repo ist an den SSH-Alias `github-lngleon` gebunden – Push läuft darüber. Hintergrund: Auf der Maschine existiert ein zweiter GitHub-Account („Dkllang"), deshalb die SSH-Bindung statt globalem Default. Bei Git-Problemen zuerst den Remote/SSH-Alias prüfen.
- **Backend/DB:** keins – reines Frontend, KEIN Supabase, KEINE SQL/RLS. Claude macht Frontend-Code + Design + Content + kurze Claude-Code-Prompts.
- **Vercel:** ✅ Mit Repo verbunden, Auto-Deploy bei Push auf `main`. Das Projekt `leons-webseite` liegt im Vercel-Account „leon's projects" – das **Dashboard ist maßgeblich**; die CLI kann im falschen Account-Kontext stehen (daher zeigte `vercel projects ls` als `lngleon` fälschlich „keine Projekte"). Verifiziert 07.06.2026 im Dashboard: oberstes Deployment 19c48be („Kontakt") = Ready/Production.
- **Formspree:** Endpoint in `.env.local` als **`NEXT_PUBLIC_FORMSPREE_ENDPOINT`** hinterlegt (seit 24.08.2026; vorher `VITE_FORMSPREE_ENDPOINT` – im Vercel-Dashboard ebenfalls umbenennen)
- **Design/Theming:** **Dark-only** (kein Light Mode, kein Theme-Toggle). Akzent **Violett**: flach `--accent`/`--accent-solid` (Ränder/Icons/kleine UI/Fokusring) + Violett-Gradient `--accent-gradient`/`--accent-gradient-strong` NUR auf Showcase-Flächen (Headline-Akzentwort, Hero-Zahlen, primäre CTAs, Eyebrow). Light-Mode-Themen (Toggle, per-Mode-Tokens, Light-Kontrast) sind gegenstandslos. Akzent nie hardcoden; Code-Tags via `CodeTag` (flach, kein Gradient).
- **Phase:** 2 abgeschlossen (alle Sektionen stehen, inkl. Projekte und Statement), Phase 3 angefangen (**Akzentfarbe ✅ Violett**). Offen: Impressum/Datenschutz-Texte, Logo („LL"-Monogramm, dunkler Hintergrund; Favicon ✅ 27.07.2026). **CSR→Prerender/SSG ✅ erledigt** (Session 10): `next build` rendert alle vier öffentlichen Routen plus die stille Demo-Route `/demo/cafe` statisch zu HTML, im Browser Hydration – beim Bauen nicht kaputtmachen (Hydration-Regel: Server-Frame = erstes Client-Frame, `useReducedMotionSafe` benutzen, kein `window`/Zufall im Render).
- **Betriebssystem:** Windows
- **Projektdateien-Pfad:** C:\Users\l.lang\REPOS\leons-webseite
- **Projektdateien (Claude AI):** PROJEKT-STAND.md, CURRENT-SCHEMA.md
- **Repo-Dateien:** CLAUDE.md (Root), docs/PROJEKT-STAND.md, docs/CURRENT-SCHEMA.md, docs/CLAUDE-CODE-TODO.md, docs/REFERENZ-DR-SMUSY.md (visuelle Referenzanalyse, Arbeitsgrundlage für die Demo-Seiten)
- **Nächste Aufgabe:** die offenen Punkte aus Nächste Prioritäten – Vercel-Settings nachziehen (Framework-Preset auf Next.js, Build-/Output-Override entfernen; der Env-Rename ist ✅ erledigt) inkl. 200-Check auf `/moeglichkeiten` und `/demo/cafe`, danach Impressum-/Datenschutz-Texte und Logo

---

## Pricing

Keine Preise auf der Website – bewusste Positionierungs-Entscheidung. Die Seite führt zum Gespräch, nicht zur Preisliste. „Premium ohne Premium-Preis" signalisiert: bezahlbar im Vergleich zu teuren Agenturen, konkrete Preise werden im persönlichen Erstgespräch / individuellen Angebot geklärt.
